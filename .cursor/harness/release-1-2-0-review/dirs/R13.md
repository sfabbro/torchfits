# R13 — scripts/

Findings: r13a-01, r13a-02. No `src/` edits.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r13a-01 | MAJOR | measurement | `scripts/render_bench_highlights.py:TARGET_CASES` | CPU image `case_id`s are `name:operation` (`large_float32_2d:read_full` in the R0 CSV). The table looked up `name::operation`, so those rows were dropped. A GPU row then took `torchfits_dtype_fair_device` (comparable false, 0.001 s) as the torchfits time, and the last mmap row won. A missing specialized time was copied into that column. Before: the CPU label was absent and the CUDA cell was `1.00 ms`. | Look up the ids the benches write. Keep mmap `on` when both passes exist. Exact method names. Specialized cell stays `—`. `pixi run pytest tests/test_patch_bench_docs.py::test_highlights_follow_csv_case_ids -q`. | fixed |
| r13a-02 | MAJOR | measurement | `scripts/render_full_benchmarks_table.py:render_full_table` | The cfitsio join used `case_id.split('::')`. An image id `large_float32_2d:read_full` therefore matched a decoy row of 9 s and missed `cfitsio_direct.csv`'s `large_float32_2d` / `read_full` (123 μs). Table `read_full` never matched the direct bench's `table_read`. | Stem splits on `:` and `::`. Operation aliases: `table_read`, `table_proj`, `table_slice`, `table_scan`, `table_pred`, `cutout`, `cutout_rep`, `random_ext`. `predicate_filter_selective` is not aliased (one C predicate). Test `test_full_table_joins_cfitsio_on_image_case_stem`. | fixed |

## Disposition

| File | Disposition |
|---|---|
| `scripts/aggregate_matrix_bench.py` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/bench_cfitsio_direct.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/bench_deficit_focus.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/bench_exhaustive_local.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/bench_release_scorecard.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/bench_suite.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/build_docs_pages.sh` | clean — pages builder calls zensical |
| `scripts/build_wheels_local.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/canfar_denoise_incontainer.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/canfar_gpu_bench_incontainer.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/canfar_matrix_bench_incontainer.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/check_docs_links.py` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/check_duplicate_cpp.py` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/check_torch_extra_pins.py` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/check_wheel_contents.py` | clean — wheel version, stub symbols, licences, forbidden paths |
| `scripts/ci_local.sh` | clean — same docs contract as GHA docs-contract (integrity tests, sync_docs_examples, zensical build) |
| `scripts/cibuildwheel.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/cibw_before_build.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/cibw_test.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/clean_install_smoke.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/fetch_canfar_bench_vos.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/fetch_cfht_calib_frames.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/fetch_cfht_megacam_sample.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/fetch_cfht_megapipe_sample.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/fetch_example_samples.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/fetch_rgb_sky_samples.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/gen_native_stub.py` | clean — stdlib generator; --check is the stub drift gate |
| `scripts/gpu-bootstrap.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/gpu-env-loader.sh` | clean — sourced; set -u only so it does not change the caller's -e |
| `scripts/import_canfar_bench_artifacts.py` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/launch_canfar_denoise.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/launch_canfar_gpu_bench.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/launch_canfar_matrix_grid.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/patch_bench_docs.py` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/patch_canfar_exhaustive_docs.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/publish_canfar_bench_vos.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/publish_canfar_wheel_bundle.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/release_lane.py` | clean — check-lane passed in preflight |
| `scripts/release_notes.py` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/render_bench_deficits.py` | clean — times come from torchfits_time_s; host label is not the run-id |
| `scripts/render_bench_highlights.py` | fixed (r13a-01) |
| `scripts/render_bench_iopath_table.py` | clean — cell is median wall time; throughput is omitted on purpose |
| `scripts/render_bench_ml.py` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/render_bench_quick.py` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/render_full_benchmarks_table.py` | fixed (r13a-02) |
| `scripts/run_exhaustive_bench_and_patch_docs.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/selfcheck_canfar_launcher.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/sync_docs_examples.sh` | clean — copies examples/*.py and examples/cli into docs/published-examples |
| `scripts/torch_lanes.json` | clean — lane file read by release_lane.py |
| `scripts/update_changelog.py` | clean — changelog-check passed in preflight |
| `scripts/verify_wheel_cuda_canfar.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/verify_wheel_cuda_canfar_incontainer.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |
| `scripts/verify_wheel_matrix.sh` | clean — scan: no unit mix, no missing fail-fast, not a scorecard renderer |

## Gate results

- `pixi run pytest tests/test_patch_bench_docs.py tests/test_bench_suites.py tests/test_bench_ranking_mmap.py -q` → 27 passed.
- `pixi run preflight-push` → PASS.
- `pixi run test` → PASS (2228 passed, 24 skipped, 21 xfailed, 383 warnings in 491.63s). +2 over R12's 2226: the highlight case-id pin and the cfitsio join pin.
