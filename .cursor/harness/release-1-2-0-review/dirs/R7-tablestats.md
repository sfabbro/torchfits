# R7 slice B — `cli/` stats · verify · diff · table · copy · cutout

Round R7 (prefix `r7b`). Files (exclusive src): `cmds_stats.py, cmds_verify.py, cmds_diff.py, cmds_table.py, cmds_copy.py, cmds_cutout.py`.
Owned tests (new): `tests/test_cli_stats_verify.py`, `tests/test_cli_diff_copy_cutout.py` (22 tests, all green: `22 passed in 37.17s`).
Failing-first runs (before any fix):
- `pixi run pytest tests/test_cli_stats_verify.py -q` → `8 failed, 3 passed in 19.51s`
- `pixi run pytest tests/test_cli_diff_copy_cutout.py -q` → `6 failed, 4 passed in 17.14s`
Post-fix: `pixi run pytest tests/test_cli_stats_verify.py tests/test_cli_diff_copy_cutout.py -q` → `22 passed in 32.77s` (then `22 passed in 37.17s` with the empty-table pin).
Pre-existing coverage re-run green (no regressions in my commands):
`pixi run pytest tests/test_cli.py -q -k "cutout or copy or diff or stats or verify"` → `15 passed, 55 deselected in 29.88s`;
`pixi run pytest tests/test_cli_arith_stats.py::test_stats_on_unsigned_convention_image tests/test_cli_arith_stats.py::test_diff_identical_nan_files_is_clean -q` → `2 passed in 4.28s`.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r7b-01 | BLOCKER | 1 wrong result | `cmds_diff.py:_image_record` | `mean` was computed via `tensor.float().mean()` — float64 images silently reduced in float32, so `diff` reported "identical" for files whose true means differ. Repro: `diff` of `[1e8, 1.0, -1e8, 0.0]` vs `[1e8, 0.0, -1e8, 0.0]` (float64; true means 0.25 vs 0.0; identical shape/min/max; float32 means both 0.0) → exit 0. | Compute mean in `stats_t`'s dtype (`float(stats_t.mean())`), same upcast-copy pattern as `cmds_stats`. Test `test_diff_float64_mean_is_computed_in_float64`: before `AssertionError: assert 0 == 1` (returncode=0); after: pass (exit 1, `"mean"` diff line). | fixed |
| r7b-02 | MAJOR | 7 edge / crash | `cmds_stats.py:_stats_one` | Empty-region stats crashed: torch reductions raise on `numel() == 0`. Repro: `torchfits.write(path, torch.zeros((0, 4)))`; `torchfits stats path -f json` → uncaught `RuntimeError: min(): Expected reduction dim to be specified for input.numel() == 0` (traceback, exit 1) — also jsonl/text. This is the decision-3 empty-region case. | Empty tensor → all five stats = NaN, which `emit_records`/`_json_safe` (common.py, slice A) serializes as `null` in `-f json`/`jsonl`. Tests `test_stats_empty_region_json_yields_null`, `test_stats_empty_region_jsonl_yields_null`: before `RuntimeError` traceback in stderr, returncode 1 vs expected 0 + `json.loads` asserts; after: pass, `min/max/mean/std/median` all `None`. | fixed |
| r7b-03 | MAJOR | 7 edge / crash | `cmds_diff.py:_image_record` | Diffing two valid identical empty-region images crashed the same way and was mapped to `IoError` exit 3 ("I/O error") on valid input. Repro: `torchfits diff empty.fits empty.fits` → stderr `min(): Expected reduction dim…`, exit 3. | NaN stats for empty regions; `_values_equal` is NaN-aware so NaN==NaN compares equal. Test `test_diff_empty_images_compare_clean`: before `AssertionError: min():… / assert 3 == 0`; after: pass (exit 0). | fixed |
| r7b-04 | MAJOR | 1/5 wrong exit on valid input | `cmds_diff.py:_diff_pair` | IMAGE-vs-TABLE HDU type mismatch at the same index ran `_image_record` on BOTH files (`if type_a == "IMAGE"` only) — `read_tensor` on a TABLE HDU raises (`RuntimeError: Could not read image parameters`, verified) and the catch-all wrapped it into `IoError` exit 3, so the type difference was never reported. Repro: `diff` of an MEF with an image ext vs a table file → exit 3, no `type` diff line. | Compare image stats only when both sides are IMAGE (`if type_a == "IMAGE" and type_b == "IMAGE"`); the type mismatch itself is already emitted as a diff line. Test `test_diff_type_mismatch_reports_diff_without_crash`: before `assert 3 == 1` (fixture also hit r7b-03 on its empty primary first — both required); after: pass (exit 1, `"type"` in stderr, no traceback). | fixed |
| r7b-05 | MAJOR | 5 error contract / exit-code violation | `cmds_stats.py:_stats_one`, `cmds_table.py:run`, `cmds_verify.py:_verify_one` | Typed `UsageError` from `common.selected_hdu_indices` (`HDU {idx} out of range`) was downgraded to `IoError` exit 3 by the broad `except Exception` wrappers, while `info`/`header` exit 2 for the same input (docs table: 2 = usage error / invalid flags). Repro: `stats img.fits -e 99` → exit 3 (should be 2, as `info img.fits -e 99` gives). | `except CliError: raise` ahead of `except Exception` in all three wrappers. Tests `test_stats_invalid_hdu_exits_usage`, `test_table_invalid_hdu_exits_usage`, `test_verify_invalid_hdu_exits_usage`: before `assert 3 == 2` ×3; after: pass (exit 2, `out of range` in stderr). Slice A matrix agrees `-e 99` → 2 (hub-confirmed). | fixed |
| r7b-06 | MAJOR | 5 error contract | `cmds_stats.py:_stats_one`, `cmds_verify.py:_verify_one` | Engine read/verify failures escaped UNWRAPPED (outside the try) as raw `RuntimeError` → traceback + exit 1 instead of documented exit 3 ("I/O error / invalid FITS structure"). Repro: `stats trunc.fits` (file cut 2880 bytes short) → `RuntimeError: Error reading image data: status=107…`; `verify trunc_cs.fits` (checksummed, truncated) → `RuntimeError: Failed to verify FITS checksums`. | Wrap `read_tensor` / `verify_checksums` per-HDU calls with `except CliError: raise / except Exception → IoError(f"{path}:{index} {exc}")`. Reductions stay outside the wrapper so genuine internal errors still surface as exit 5 (slice A machinery). Tests `test_stats_truncated_image_is_io_error`, `test_verify_truncated_checksummed_is_io_error`: before `assert 1 == 3` + `RuntimeError` traceback in stderr; after: pass (exit 3, `Traceback` absent). | fixed |
| r7b-07 | MINOR | 7 non-finite floats in JSON | `cmds_table.py:run` (text preview block) | Text-mode `preview:` block used `json.dumps(record["preview"], …)` with default `allow_nan=True` → emitted bare `NaN`/`Infinity` tokens = invalid JSON (the block is documented/shown as JSON). Repro: `torchfits table tbl.fits -n 3` on a column `[1.0, nan, inf]` → preview contains `"A": NaN`. | Route through `_json_safe` + `allow_nan=False` (same contract as `-f json`). Test `test_table_text_preview_is_valid_json`: before `json.loads` raises `JSONDecodeError` on the `NaN` token; after: pass (block parses; `null` values). | fixed |
| r7b-08 | MINOR | 7 edge inputs | `cmds_cutout.py:_parse_box` | `--box` edges silently produced empty/clamped outputs with exit 0: `--box 3,3,1,1` (inverted) and `--box 2,2,2,2` (empty) wrote 0×0 FITS files; `--box=-2,-2,2,2` silently clamped the negative origin to `0,0,2,2` (2×2 output — user asked for −2..2). | `_parse_box` rejects negative `x1/y1` and `x1>=x2`/`y1>=y2` with `UsageError` (exit 2); boxes extending past the image edge still clamp (slice semantics — pinned). Tests `test_cutout_inverted_box_is_usage_error`, `test_cutout_empty_box_is_usage_error`, `test_cutout_negative_origin_is_usage_error`: before `assert 0 == 2` ×3 (and output files existed); after: pass (exit 2, no output file). Clamp contract pinned by `test_cutout_box_extends_past_image_clamps`. | fixed |
| r7b-09 | MINOR | 8 docs faithfulness | `docs/cli.md` `table` section (doc owned by slice A) | Documented example `torchfits table catalog.fits -e 1 -c RA,DEC,FLUX -n 10` uses `-c/--columns`, which does not exist on `table` (no such flag in `cmds_table.add_parser`) → the documented example exits 2 `unrecognized arguments: -c RA,DEC,FLUX`. New flags are prohibited, so the doc must change. | Verbatim doc text sent to slice A (R7-CliCore) via hub and recorded below; slice A lands it in `docs/cli.md`. | fixed (via slice A doc commit) |
| r7b-10 | MINOR | perf 2 redundant IO | `cmds_verify.py:_verify_one` | Per-HDU `torchfits.read_header(path, index)` re-opened the file whose HDUList was already open: 1 + 2N file opens per `_verify_one` call (verified count on an 8-HDU MEF: `open`=1, `read_header`=8, `verify_checksums`=8 → 17 opens). | Reuse `hdul[index].header` collected inside the `with torchfits.open(...)` block (same pattern as `_stats_one`); wrapped into the r7b-06 restructure. Count proof (same throwaway script `/tmp/r7b/perf_verify.py`, 8-HDU MEF): `open`=1, `read_header`=8→0, `verify_checksums`=8 → opens 17 → 9. Micro-timing (same host, median of 21×20 calls): 749.5 µs → 613.2 µs (−18%, above the 3% noise floor). | fixed |
| r7b-11 | MINOR | 1 type fallthrough (cross-file: `common.py`, slice A-owned) | `common.py:_json_safe` | `_json_safe` only maps `isinstance(value, float)` non-finite → None; `np.float32("nan")` is not a `float` subclass and would reach `json.dumps(..., allow_nan=False)` via `json_default`'s `.tolist()` → `ValueError`. Latent (unreachable from my records today: stats values are `float(...)`-wrapped, Arrow `as_py()` returns Python natives). | Reported to slice A via hub (they own `common.py`); A is landing numpy scalar/array handling in `_json_safe`/`emit_records` in this round. No edit by me. | fixed (via slice A `common.py`) |
| r7b-12 | MINOR | 1/7 type fallthrough | `common.py:json_default` + `cmds_table.py:_preview_rows` | Table preview values of exotic column types stringify via `json_default`'s `str(value)` fallback: raw bytes → `"b'\\x00…'"`, complex → `"(1+2j)"`. Deterministic and valid JSON but lossy/awkward types; no JSON-native representation exists for either. | Deferred: needs a serialization decision (e.g. bytes as latin-1 text per column TFORM) — no obvious correct target for 1.2.0. | deferred |
| r7b-13 | MINOR | 3 partial artifact on error | `cmds_copy.py:_copy_remote` | A mid-transfer failure leaves a partial OUTPUT file behind (`open(output_path, "wb")` + `shutil.copyfileobj`); exit code is 3 so scripts can detect it. Identical to local `shutil.copy2` partial-file semantics (disk-full etc.), so behavior is currently consistent. | Deferred one-liner: `cmds_copy.py:_copy_remote` — failed guarded fetch leaves partial OUTPUT (same as `shutil.copy2` on local failure; exit 3 signals it). | deferred |

## Exact-decision verification (verify-only items)

| Decision | Result | Evidence |
|---|---|---|
| 3 — `stats`/`table` `-f json`/`jsonl` non-finite floats → `null` | HOLD at HEAD (`emit_records` already routes through `_json_safe` + `allow_nan=False`) and now pinned; empty-region semantics added (r7b-02). Text-mode preview aligned (r7b-07). | `test_stats_nonfinite_floats_serialize_as_null`, `test_table_json_preview_nonfinite_null`, `test_stats_empty_region_json_yields_null`(+jsonl), `test_table_text_preview_is_valid_json` |
| 4 — `copy` same-path refusal = reference for `compress`/`convert`/`decompress` | STABLE, pinned verbatim: `UsageError`, exit **2**, single stderr line `refusing same-path rewrite in place: {src}` (`reject_same_path` in `common.py`, called from `resolve_batch_io_pairs(refuse_same_path=True)` and again in `_copy_one`). Sent to slice C (R7-CliArith) via hub. | `test_copy_same_path_refused_message_and_exit` (asserts the exact line) |
| 6 — per-command exit paths vs `docs/cli.md` exit table | CONFORMING post-fix for my six commands: success 0; usage 2 (`-e notanint` via `parse_hdu_list`, bad `--box`, same-path refusal, `-e 99` post-r7b-05); IO 3 (missing file, truncated file post-r7b-06); diff-found 1; verify-fail 4. Slice A owns the cross-command matrix test (`tests/test_cli_exit_matrix.py`). | tests above + `test_diff_missing_file_is_io_error`, `test_verify_one_corrupt_file_exits_4_under_file_jobs` |
| 7 — `cli-j-vs-J` invariant | HOLDS: `common.run_file_jobs` caps `torch.set_num_threads(1)` per worker when `jobs > 1` (`common.py:263-290`); serial path applies `configure_torch_jobs`. | code inspection + `test_verify_one_corrupt_file_exits_4_under_file_jobs` (`-J 2`) |
| 7 — `is_remote_path` includes ftp | HOLDS: `_REMOTE_PREFIXES = ("http://", "https://", "ftp://", "vos://", "vos:", "vault:")` (`common.py:17`) | code inspection (`test_cli_rejects_ftp_remote_paths` in `tests/test_cli.py` covers it) |
| `cmds_copy.py` remote fetch through R1 guarded opener | CONFIRMED, no family-line edits by me: `_copy_one` calls `guard_fits_path(src)` before any fetch; remote bytes come from `http_open` (`torchfits.http_util`, R1's guarded pinned opener); bare `urlretrieve` is gone from the file; `vos:`/`vault:` refused with `IoError`. `copy-is-binary` also pinned: local `shutil.copy2` byte-identical. | `test_copy_is_byte_identical`, `test_copy_blocks_internal_ssrf` (pre-existing, `tests/test_cli.py`) |

## Doc note text (verbatim) — delivered to slice A (R7-CliCore) via hub

Decision-3 note for the `docs/cli.md` `stats` section (delivered; A confirmed landing verbatim):

> Non-finite values in JSON output: `stats` and `table` serialize `NaN`/`±Infinity` floats as `null` in `-f json` / `-f jsonl` (`--json` / `--jsonl`) — JSON has no NaN or Infinity literals. An empty image region therefore reports `null` for `min`, `max`, `mean`, `std`, and `median`.

`table` example removal (r7b-09) — delete these two lines from the `table` section:

> ```
> # Select specific columns
> torchfits table catalog.fits -e 1 -c RA,DEC,FLUX -n 10
> ```

`cutout` note (r7b-08 user-visible change) — add after the two coordinate-format bullets:

> `--box` values must be integers with non-negative `x1,y1` and `x1 < x2`, `y1 < y2` (0-based, half-open); an empty, inverted, or negative-origin box is a usage error (exit 2). A box that extends past the image edge is clamped to the image, so the output region is the box intersected with the image.

## Per-file disposition

| file | depth | finding IDs | status |
|---|---|---|---|
| `src/torchfits/cli/cmds_stats.py` | deep | r7b-02, r7b-05, r7b-06 | fixed |
| `src/torchfits/cli/cmds_verify.py` | deep | r7b-05, r7b-06, r7b-10 | fixed |
| `src/torchfits/cli/cmds_diff.py` | deep | r7b-01, r7b-03, r7b-04 | fixed |
| `src/torchfits/cli/cmds_table.py` | deep | r7b-05, r7b-07, r7b-09 (docs), r7b-12 | fixed; r7b-12 deferred |
| `src/torchfits/cli/cmds_copy.py` | deep | r7b-13 (deferred); decision-4 reference pinned | fixed; r7b-13 deferred |
| `src/torchfits/cli/cmds_cutout.py` | deep | r7b-08 | fixed |
| `tests/test_cli_stats_verify.py` (new, owned) | deep | covers r7b-02/05/06/07 + decision-3 pins + empty-table pin | fixed |
| `tests/test_cli_diff_copy_cutout.py` (new, owned) | deep | covers r7b-01/03/04/08 + decision-4 pin + `copy-is-binary` pin | fixed |
