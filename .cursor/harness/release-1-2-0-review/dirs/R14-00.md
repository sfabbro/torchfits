# R14-00 — examples/ slice 0

Paths: `splits/examples.chunk00` (20 files, `_plotting.py` through `example_manga_logcube.py`).

Findings in this slice: r14a-01 (`cli/make_rgb_demo.py`), r14a-03, r14a-04 (`desi_shaped_spectrum.py`), r14a-05. See `dirs/R14.md`.

`example_cutout_wcs_write.py` shifts `CRPIX*` by the cutout origin and says so. It does not claim a WCS engine. `example_image_cube.py` stores `CTYPE*` cards the caller supplied. The DESI healpix URL is the survey's directory layout, not a torchfits feature.
