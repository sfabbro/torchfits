# R1 — `src/torchfits/` root modules (+ HTTP/SSRF family step)

Slices: A `dirs/R1-root-a.md` (r1a-*), B `dirs/R1-root-b.md` (r1b-*), C `dirs/R1-http.md` (r1c-*).

## Totals

28 findings: 5 BLOCKER, 6 MAJOR, 9 MINOR, 2 CLEANUP-class records (+ partial deferrals).
Fixed in-round: 22. Deferred: r1b-08 (docs read_extname hdu= default → R15), r1b-09
(`_READ_EXC_TYPES` over-narrowing policy), r1b-10 (`table.to_astropy(Path)` → R5),
r1c-11 (docs where-grammar 3VL text → R15), r1c-12 (security.h `|`/`sh://` Python mirror → R10),
r1c-13 (`_string_decode` errors='ignore' decode parity decision), r1c-14 (CFITSIO residual-TOCTOU
doc note → R15).

## Blockers fixed

- r1c-01/02 `_where`: quoted 'null'/'none' literals parsed as SQL NULL; BETWEEN/IS NULL/AND
  rewrites corrupted quoted literals.
- r1c-03 `where`: `X != 5` vs `NOT (X == 5)` row-set divergence on NaN/None (3VL restored).
- r1c-06 `http_util`: DNS-rebinding TOCTOU on all Python fetches incl. redirect hops —
  per-hop re-guard + guard-time pinning (original Host/SNI).
- r1c-07 `cmds_copy`: bare `urllib.request.urlretrieve` on ftp branch (SSRF) — all fetches
  now route through the guarded pinned opener.

## Majors fixed

r1a-01/02 (DeprecationWarning-on-first-IO + unlocked phantom-config global write), r1b-01/02/04
(to_astropy dict/PathLike fidelity; case-variant FITS keyword drops), r1c-04 (typed ValueError),
r1c-05 (`_string_decode` 4MB-copies-per-4KB-window: count proof 4,000,000→4,000 bytes/call,
micro-timing 9,316,607µs→63µs median), r1c-08 (typed 416 → subset fallback), r1c-09 (narrowed
resume-validator except).

## Evidence

Failing-first outputs pasted in the slice files (`dirs/R1-root-a.md`, `dirs/R1-root-b.md`,
`dirs/R1-http.md`). Combined-tree targeted run before commit: 193 passed. Integration gates:
`pixi run preflight-push` green (after `chore(typing)` + `style:` follow-ups owned by
integration), `pixi run test` green (recorded below).

## Plan deviations (integration owner)

- Changelog-diff commit subject `chore(changelog): R1 round fixes` instead of the plan's
  `docs: changelog for <round>`: `scripts/update_changelog.py` collects subject-only bullets
  one run late (no self-exclusion), so a `docs:` subject fails the very next `changelog-check`
  in `preflight-push`. Repo's current convention is `chore(changelog):` (ALWAYS_SKIP).
- Integration-owned follow-ups: `style: format test_http_guard with ruff` and
  `chore(typing): satisfy mypy strict for the R1 http/where code` (subagents skip
  formatters/mypy per Method).

## Gate results

- `pixi run preflight-push` → PASS (ruff check, ruff format --check, mypy 95 files, compileall,
  check-lane, changelog-check).
- `pixi run test` → PASS (1612 passed, 24 skipped, 21 xfailed, 382 warnings in 293.33s; baseline 1584 passed → +28 net tests).
