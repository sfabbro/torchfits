# R10 — native infrastructure and build

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r10a-01 | MAJOR | 6 — security contract parity | `security.h:check_fits_filename_security`; `_io_engine/paths.py:guard_fits_path` / `http_util.py:guard_cfitsio_remote_path` | R1 r1c-12 re-derived item-for-item. Native CFITSIO-open security rejects: leading `|` and trailing `|` after trimming standard whitespace, and case-insensitive `sh://`, after any leading forced-overwrite `!` plus whitespace. Python pre-open security rejects private/loopback/link-local/reserved/multicast/unspecified `http://`, `https://`, and `ftp://` destinations after case-folding and `!`/whitespace stripping. `http/https/ftp` and `| / sh://` are complementary, not duplicated: the former needs DNS/IP classification before any Python or CFITSIO network path; the latter must be enforced natively at every CFITSIO open, including raw `_C` consumers. | Keep the split. Extend `tests/test_security.py` with the exact hostile-name matrix and public-network non-regression so both sides of the contract are CI-visible. No Python cross-file edit is needed. | fixed (coverage/decision) |
| r10a-02 | MINOR | 8 — test wiring | `security.h:has_cfitsio_extended_filename_syntax`; `tests/cpp/test_bracket_detection.cpp` | Repository search found the direct C++ bracket detector probe only as a manual comment/instruction; no test task or Python test invoked `tests/cpp/test_bracket_detection.cpp`. Python `cfitsio_base_path` behavior is covered separately, but the native helper itself was outside CI. | Add a Python wrapper in `tests/test_security.py` that compiles and runs the existing standalone C++ probe during the native test build. The probe covers terminal filters, multiple sections, bracket literals in directories, and empty/plain paths. | fixed |
| r10a-03 | BLOCKER | 7 — unaligned input / UB | `internal_utils.h:bswap16_copy, bswap32_copy, bswap64_copy` and offset variants | SIMD bulk loads are unaligned-safe on x86 and AArch64, but every scalar tail dereferenced `src[i]` through `const uintNN_t*`. FITS table rows can place a multi-byte column at an odd address. Exact UBSan probe `/tmp/r10a_bswap_ubsan.cpp` called `bswap32_copy(storage + 1, dst, 3)` under `-fsanitize=undefined` and exited 1 at the old `internal_utils.h:175`: `load of misaligned address ... for type 'const uint32_t', which requires 4 byte alignment`. Normal optimized x86 output happened to work, masking the language-level UB. | Accept byte-addressed `void*` inputs, use byte NEON/x86 unaligned intrinsics, and use `std::memcpy` for scalar tails (defined for unaligned source and destination). Added `tests/test_native_infra.py` over deliberately odd table-column offsets, non-multiple SIMD lengths, NaN payloads/signs, ±Inf, signed zero, denormals, and every unsigned 16/32-bit offset value. After the fix, scalar, SSSE3, and AVX2 UBSan probes all exited 0; public regression passed `4 passed` even before rebuilding because this host's generated code already tolerated the hardware fault. | fixed |
| r10a-04 | CLEANUP | 2 — effective build flags | `cpp_src/CMakeLists.txt:Release optimization` | Active Ninja proof before the fix: `_C` source flags ended `... -O3 -mssse3 -fno-stack-protector -Os ...`; nanobind's later `-Os` overrode torchfits' requested Release `-O3`. The configuration also repeated `-O3` through global options, `CMAKE_CXX_FLAGS`, and `CMAKE_CXX_FLAGS_RELEASE`. | Removed the redundant directory-wide `add_compile_options(-O3)` and added final `_C`-private Release `-O3` after `nanobind_add_module`. Post-rebuild `build.ninja` for `bindings.cpp`, `fits_bindings.cpp`, `fits_file.cpp`, `table_bindings.cpp`, `table_ops.cpp`, and `hardware.cpp` all end their optimization choice `... -fno-stack-protector -Os -O3 ...`. | fixed |
| r10a-05 | MAJOR | 8 — sanitizer coverage | `cpp_src/CMakeLists.txt:CFITSIO target` | `.github/workflows/sanitizer.yml` injects ASan+UBSan through `CMAKE_CXX_FLAGS`; vendored CFITSIO is C and therefore received only the pyproject's `-O3`, not sanitizer instrumentation. Thus the workflow did not actually sanitize the native C dependency it exercises. | Detect `-fsanitize=*` options in configured base/build-type C and CXX flags and apply them to vendored `cfitsio`. Exact configure proof in `/tmp/r10a-sanitize-config` printed `Applying sanitizer instrumentation to vendored CFITSIO: -fsanitize=address;-fsanitize=undefined`; generated Ninja shows both flags on C++ `_C` sources and every C `cfitsio` source, plus `-fsanitize=address` on the module link. | fixed |
| r10a-06 | — | 8 — no-SIMD fallback / portability | `hardware.cpp`; `internal_utils.h` byte-swap dispatch | Re-derived: `hardware.cpp` owns only POSIX mmap RAII; it has no SIMD resolver to fall back. Byte swapping is compile-time dispatched in `internal_utils.h`: NEON, AVX2, SSSE3, then the same scalar loop. With no SIMD macro the scalar loop handles every `i`; after r10a-03 it also handles unaligned storage without UB. All supported release targets are GCC/Clang and provide the `__builtin_bswap*` helpers. | No separate hardware fallback implementation is needed. The exact ASan+UBSan probe `/tmp/r10a_bswap_ubsan.cpp` checks every helper for `n=0..65`, source/destination skews `0..7`, and compares each result to a byte-level oracle. Scalar (SIMD macros explicitly removed), SSSE3, and AVX2 builds each printed `r10a byteswap probe: all lengths/offsets passed` and exited 0. | fixed (verified) |
| r10a-07 | — | 5/8 — ABI and sealing | `bindings.cpp`; `torch_compat.h`; `torchfits_torch.h`; `hardware.h` | Full pass found the native module surface still only exposes `HAS_BZIP2` plus the sealed fit/table binding families; ABI check accepts only exact major.minor boundaries. Tensor conversion preserves the PyTorch-owned `DLManagedTensor` lifecycle by renaming the consumed capsule. MMap handles are move-only and release mapping/fd once. | No edit. Existing `cpp-seal-all` and mismatched-ABI tests are the behavioral pins; targeted tests run after rebuild. | fixed (verified) |

## Rebuild and validation

- Serialized rebuild: `pixi run dev` succeeded (41.25s); `pixi run -e test -- pip install -e . --no-build-isolation` succeeded (43.17s).
- Required targeted suites: `pixi run pytest tests/test_security.py tests/test_io_invariants.py tests/test_native_infra.py -q` → `68 passed, 2 warnings` (warnings are the pre-existing deprecated `torchfits.cpp` test calls). Post-import cleanup, `tests/test_security.py` alone → `21 passed`; `tests/test_byteswap.py` → `26 passed`; `tests/test_output_parity.py` → `69 passed`.
- Native sealing/ABI pins: four selected tests from `test_package_isolation.py` and `test_public_boundary.py` → `4 passed`.
- Duplicate C++ check: `pixi run python scripts/check_duplicate_cpp.py` → `No duplicate file-scope functions detected across .cpp files`.
- Release Ninja proof and sanitizer configure proof are recorded in r10a-04/r10a-05 above.

## Per-file disposition

| File | Disposition |
|---|---|
| `src/torchfits/cpp_src/internal_utils.h` | Full review complete. Fixed unaligned scalar-tail UB across all 16/32/64-bit signed, unsigned-offset, IEEE, no-SIMD, zero/tail/extreme-width paths (r10a-03/r10a-06); env parsing, ndarray contiguity/overflow guards, and string padding were also reviewed. |
| `src/torchfits/cpp_src/security.h` | Full review complete; complementary-layer decision and CI-visible bracket probe recorded as r10a-01/r10a-02. |
| `src/torchfits/cpp_src/hardware.h` | Full RAII/endian shim review complete; no source finding. |
| `src/torchfits/cpp_src/hardware.cpp` | Full POSIX mmap lifetime review complete; no SIMD code exists here (r10a-06). |
| `src/torchfits/cpp_src/torch_compat.h` | Full dtype/ownership/error review complete; no source finding. |
| `src/torchfits/cpp_src/torchfits_torch.h` | Full ATen fallback shim review complete; current dtype uses are covered. |
| `src/torchfits/cpp_src/bindings.cpp` | Full module/ABI/seal review complete; no new public binding. |
| `src/torchfits/cpp_src/CMakeLists.txt` | Full build review complete. Restored effective Release `-O3` after nanobind's `-Os` (r10a-04) and propagated ASan/UBSan to vendored C/CFITSIO with generated Ninja proof (r10a-05). PGO/finite-math, LTO, SSSE3/NEON, bzip2, CUDA fallback, and install paths remain intact. |
