# R4 — `src/torchfits/_io_engine/`

Slices: A `dirs/R4-read.md` (r4a-*), B `dirs/R4-write.md` (r4b-*), C `dirs/R4-core.md` (r4c-*).

## Totals

45 findings (r4a 15, r4b 15, r4c 15): 39 fixed in-round (incl. r4c-15 wrapper), 6 deferred/routed.

Blockers fixed (8): r4a-01 batch silent fallthrough/misalignment (now RuntimeError naming the bad
path; len(result)==len(inputs) enforced at all four C++ batch call sites); r4b-01 copied table
headers forged NAXIS2 rows / TFORM relabel (3-row write read back 5 with garbage); r4b-02
HISTORY/COMMENT corrupted and lost on every write path (now card-faithful replay); r4b-03 dict-HDU
form silently dropped non-'data'/'header' keys; r4b-14 (integration) the card replay re-injected
compressed-image Z*/tile skeletons onto fresh compressions — rewrites read back as table dicts;
r4c-01 (A-18) HDUList.close unlocked registry RMW lost-update race (authorized narrow edit to
`_hdu/hdu_list.py` close() block → locked `caches._unregister_open_hdulist`); r4b-13 LONGSTRN
'&'+CONTINUE not reassembled at `HDUList.fromfile` (ROOT → R6 as named BLOCKER; r4c-15 in-wave
mitigation wraps `hdu_api.open_hdulist`, covering 100% of in-repo opens).

Majors (23): r4a-03 (HTTP cutout double header walk hoisted — count proof 2→1, 119.9→12.4µs),
r4a-05 (typed exception dispatch replaces `"truncated" in str(exc)` matching; IO errors
propagate), r4a-06 (fallback discarded-header read removed — 232→83µs), r4a-07 (subset/HTTP
parity matrix incl. blank-nulval pinned — no divergence at HEAD), r4b-04..r4b-09 (quantize=
silently ignored on compressed dict forms; int8 columns inconsistent rejection; dict-image
crash; write(header=) ignored; stale CHECKSUM/DATASUM cards; verify_checksums false failures),
r4b-15 (source TFORM spellings discarded on copy/replace — astropy-conformant bare type chars
preserved verbatim), r4c-02..r4c-11 family (exact-literal deprecation warnings for the two dead
knobs; validate_device malformed forms; MPS downcast warnings (A-13); stream probe narrowing;
phantom-HDU probe; negative meta caching with count proof 200→0 walks, 25.3→3.2µs; read_table
except narrowing; _resolve_mmap string acceptance).

Minors/CLEANUP: r4a-02/04/08/09/10/11, r4b-10/11/12, r4c-12/13 (`_normalize_cpp_chunk` verified
absent at HEAD — nothing to delete).

## Deferred / routed

- **r4b-13 ROOT → R6 (BLOCKER)**: `_hdu/hdu_list.py:122` `Header(info.header)` builds HDU headers
  from cpp card tuples without LONGSTRN '&'+CONTINUE reassembly (open() truncates >68-char values
  while read_header() returns the full 80; every HDUList write/insert/replace/delete rewrite
  corrupts long strings). R6-A fixes at construction with a shared reassembly helper
  (card.py/header.py wheelhouse) and must NOT re-fix the r4c-15 wrapper. Strict-xfail pin lives in
  `tests/test_writing.py` (flips normal when the root fix lands — currently pinned post-r4c-15).
- r4a-12 negative image_meta direct-call caching (under 3% threshold — measured, not landed).
- r4a-14 dead `image_meta._cache_get/_cache_set` + `subset.read_subset` (outside the named
  dead-code list — 2.0 sweep candidates).
- r4a-15 owed docs list (read_batch contract change etc. → R15).
- EngineCore deferred list (negative `get_cached_hdu_type` sentinel contract; stale-hit
  invalidation lock contention; `_clone_read_value` nested-dict aliasing; `find_first_hdu`/paths
  probe fallbacks kept).
- Routed onward: `update_rows` mmap='auto' swallow → R5 (`_table/mutation.py:400`).

## Evidence

Failing-first pasted in slice files (A: 5+ capture blocks; B: 14 failing-first tests; C: per-fix
captures). Perf: r4a-03 (2→1 walks; 119.94→12.39µs/cutout, 89.7%), r4a-06 (1→0 header reads;
232.37→82.78µs/read, 64.4%), r4c-09 (200→0 header walks per 200 calls; 25.3→3.2µs, 8×),
r3→r4 make_loader (earlier round). Combined targeted runs: A 57+57+3; B 126 passed + oracle 130
passed; C 37 passed/12 skipped + sweeps 59/11/43/134.

## Plan deviations / integration notes

- `chore(changelog): R4 round fixes` subject convention (as R1-R3).
- Cross-file authorizations used: (1) r4c-01 narrow `_hdu/hdu_list.py` close() block; (2) r4b-13
  routed instead of edited (file ownership preserved for R6's root fix).
- Slice A respawned once (provider repetition-truncation failure at first tool call; zero state
  lost; `R4-ReadPath2` completed the slice).

## Gate results

- `pixi run preflight-push` → PASS (after integration-owned `style: ruff pass over R4 code; mypy-strict typing fixes in write helpers`).
- `pixi run test` → PASS (1862 passed, 24 skipped, 21 xfailed, 381 warnings in 310.15s; +73 over R3's 1789). Combined R4 evidence surface before commit: 439 passed, 16 skipped.
