** ROUTED TO R6 — BLOCKER r4b-13 **

# R4 slice B — `_io_engine` write path (write_api, _write_helpers, _hdu_rewrite, quantize, checksum_api)

Baseline: HEAD 1bb6958 + R1–R3 (worktree at review time). All IDs `r4b-NN`. Evidence = regression test added first, run against unfixed code (failure pasted), fix, run again (pass pasted). Targeted runs only (`pixi run pytest tests/<file> -q`).

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r4b-01 | BLOCKER | 1 | `write_api.py:write`; `_write_helpers.py:_prepare_unsigned_table_data_for_write` consumers; `_hdu_rewrite.py:_sanitize_table_header_for_write` | Copied table headers override data-derived cards: `write(dst, {"NEW": int32[3]}, header=read_header(other_table))` wrote `NAXIS2=5` (astropy read 5 rows: `[0,1,2,0,0]` — 2 forged garbage rows), renamed the column to the source's `TTYPE1`, and relabeled `TFORM1` from `1J` to `1K` (int64 reads of 4-byte cells: `[1, 8589934592, 0, 0, 0]` garbage). Same via `_TableHDUWriteProxy` (compressed dict-table) and `TableHDU`+`insert_hdu`. | New `_write_boundary_header(header, table=True)` drops the structural block + `TTYPE*/TFORM*/TSCAL*/TZERO*` (kept: TUNIT/TDIM/TNULL annotations) at every table write boundary (`write()` dict-table, `_TableHDUWriteProxy`, `_sanitize_table_header_for_write`). Test `test_write_fidelity.py::test_copied_table_header_cannot_forge_rows_or_relabel_columns` (astropy oracle). Before: `NAXIS2 5 / names ['OLDNAME'] / col [1, 8589934592, 0, 0, 0]`; after: `NAXIS2==3, names==['NEW'], TFORM1=='1J', values [0,1,2]` exact. | fixed |
| r4b-02 | BLOCKER | 1 | `write_api.py:write`; `_write_helpers.py:_cpp_header_mapping,_merged_write_header`; `_hdu_rewrite.py:_write_hdus_uncompressed,_write_hdus_with_optional_compression` | HISTORY/COMMENT corruption + loss on every write path. The C++ dict writers run commentary keys through `fits_update_key`, producing value-style garbage cards (`HISTORY = 'h2      '` — raw cards 06-14 of the repro file show `COMMENT = 'c1      '` **clobbering CFITSIO's standard COMMENT block line 1**) and the mapping view collapsed duplicate HISTORY to the last one (HDUList writes lost `h1` entirely). The compressed overlay `Header(dict(first.header)); merged.update(dict(header))` lost `h1` and re-appended `h2`/`c1` (4 duplicated cards). `_write_hdus_uncompressed`/rewrite paths had no card replay at all. Repro: `Header` with `add_history("h1"); add_history("h2"); add_comment("c1"); add_comment("c2")` + long string, written 6 ways. Before: `hist == ["= 'h2      '", 'h1', 'h2']` (plain), `hist == []`/`['hh1']→["= 'hh1     '"]` (HDUList), compressed missing `h1` + duplicates. | Commentary keys are excluded from every C++ dict-writer mapping (`_cpp_header_mapping`); exactly one full-card replay per output HDU (`write_hdu_header_cards` → `fits_write_history`/`fits_write_comment`) now runs inside `_write_hdus_uncompressed` and `_write_hdus_with_optional_compression` (write() keeps its own replay for the direct `write_fits_file`/`write_fits_table` paths); compressed overlay replaced by card-faithful `_merged_write_header` (value cards replace, commentary appends, comments/order preserved). Tests `test_header_commentary_cards_every_write_path`, `test_multi_hdu_commentary_independent`. After: `hist == ["h1","h2"]`, user comments once each, no `= '`-style cards, on all 6 write forms + rewrite-insert. | fixed |
| r4b-03 | BLOCKER | 1 | `write_api.py:write`; `_write_helpers.py:_coerce_compressed_hdu_item,_require_image_hdu_dict_keys` | Dict-HDU image form silently dropped keys other than `data`/`header`: `write(p, {"data": zeros(2,2), "flux": ones(2)}, compress=True)` wrote an image extension and **`flux` vanished with no error** (same for `[{"data","flux"}]` sequence items). | Dict-HDU payloads must contain exactly `data`/`header`; extra keys raise `ValueError` naming them (`HDU dictionary may contain only 'data' and 'header' keys; unexpected keys: ['flux']`), enforced in `_coerce_compressed_hdu_item` and the plain sequence path. Test `test_dict_image_extra_keys_rejected_not_silently_dropped` (plain + compressed). Before: file written, keys dropped. After: `RuntimeError` naming `flux`, no file left. | fixed |
| r4b-04 | MAJOR | 1 | `write_api.py:write`; `_write_helpers.py:_TableHDUWriteProxy` | `quantize=` silently ignored on compressed dict-table writes and dict-image forms. `write(p, {"F": float32[4096]}, compress=True, quantize={"F":"robust"})` stored `TFORM=['1E']` (float) while the identical uncompressed call stored `['I']` + TSCAL/TZERO; `write(p, {"data": x}, compress=True, quantize="robust")` stored BITPIX=-32 instead of packed 16. Accepted-and-ignored user kwarg = silent wrong storage convention. | `_TableHDUWriteProxy(raw_data, header, quantize=)` applies `_prepare_quantized_table_data_for_write` and emits TSCAL/TZERO/TNULL cards; dict-image branches apply `_apply_image_quantize` (single form now also honors `quantize` uncompressed — see r4b-06). Tests `test_quantized_dict_table_compressed_matches_plain` (plain/comp parity, astropy TFORM/TSCAL/TZERO/TNULL), `test_quantize_int16.py::test_quantize_honored_on_compressed_dict_writes`. Before: `TFORM=['1E']`, `bitpix=-32`. After: `TFORM2∈{I,1I}` + `TSCAL2/TZERO2/TNULL2`, `bitpix==16`, NaN→BLANK/TNULL −32767 round-trips. | fixed |
| r4b-05 | MAJOR | 7 | `_write_helpers.py:_can_use_cpp_table_writer,_vla_item_signature` | int8 table columns deterministically rejected on valid input, inconsistently: `torchfits.write(p, {"I8": int8arr})` and `torchfits.table.write(p, {"I8": int8arr})` raise `RuntimeError("Dictionary table writes currently require CFITSIO-native column types...")`, yet the same column alongside any uint16 column writes fine and round-trips as the pinned TBYTE convention (`table_ops.cpp:424-427` maps Int8→TBYTE natively; `test_table_write_roundtrip_dtype_matrix` pins it). | int8 added to the cpp-writer gate (numpy `kind=="i", itemsize∈{1,2,4,8}`; `torch.int8`) and to `_vla_item_signature` (`torch.int8→("i",1)`, numpy int8). Test `test_table_write_int8_column_tbyte_convention` (astropy ground truth: raw uint8 bytes, `.view(int8)` recovers). Before: RuntimeError on both solo forms. After: astropy `uint8 [128,255,0,1,127]` → `view(int8) == [-128,-1,0,1,127]`. | fixed |
| r4b-06 | MAJOR | 5/7 | `write_api.py:write` | Uncompressed single dict-image form `{"data": tensor, "header": {...}}` was parsed as a *sequence* (dicts iterate as keys): `write(p, {"data": zeros(2,2)})` raised `RuntimeError: ... Unsupported HDU item type: str`; with `quantize=` it raised a misleading `QuantizeError: ...not multi-HDU sequences` for a single dict. The compressed branch accepted the same form. | Dedicated `isinstance(data, dict)` branch treats `{"data","header"}` as a single image HDU (quantize honored, `TypeError("HDU dictionary 'data' must be a torch.Tensor")` for non-tensor payloads — same message as sequence items). Test `test_dict_image_hdu_form_uncompressed`. Before: `Unsupported HDU item type: str`. After: 1-HDU file, values + `OBJECT` round-trip. | fixed |
| r4b-07 | MAJOR | 1 | `write_api.py:write` | `write(header=)` silently ignored for HDUList and sequence inputs on the plain path (compressed path overlaid it): `torchfits.write(p, HDUList([...]), header={"USERTOP":"yes"})` wrote no USERTOP. Accepted-and-ignored user kwarg = silent metadata loss. | `header` now overlays the first HDU on every path (card-faithful `_merged_write_header`), applied before payload synthesis so synthesized BSCALE/BZERO always win; never mutates the caller's HDUs (`_hdu_with_header` copies). Test `test_write_header_param_overlays_first_hdu_every_path` (plain/compressed × seq/HDUList; also asserts HDU 2 is untouched). Before: `assert None == 'yes'`. After: USERTOP on HDU 0 only, all 4 forms. | fixed |
| r4b-08 | MAJOR | 1 | `write_api.py:write`; `_write_helpers.py:_write_boundary_header`; `_hdu_rewrite.py:_sanitize_header_for_compressed_write` | Stale `CHECKSUM`/`DATASUM` cards copied from a source header were written verbatim (`write(p, data, header={"CHECKSUM":"abcdEFGH","DATASUM":42})` — image *and* dict-table paths; `write_hdus`/`write_table_hdu` C++ loops have no skip for them while `write_hdu_header_cards`/the compressed writer do). The fresh file then *fails its own verification* (`verify_checksums` → `"fail"`) — false corruption for archivists. | CHECKSUM/DATASUM dropped at every write boundary (they describe the source payload; `checksum=True` and rewrite `restamp_checksums` stamp fresh ones — verified working: `insert_hdu`/`delete_hdu` on checksummed files re-verify `"ok"` on every HDU). Test `test_stale_checksum_cards_are_not_replayed` (image + table). Before: `verify=fail, CHECKSUM='abcdEFGH'`. After: `"no_checksums"`, cards absent. | fixed |
| r4b-09 | MAJOR | 1 | `checksum_api.py:verify_checksums` | Verification API reported corruption where none exists: a file with a correct DATASUM and no CHECKSUM (valid per CFITSIO `ffvcks`: `1`/`0`) mapped to `status="fail", ok=False`. Repro: `write(..., checksum=True)`, blank the CHECKSUM card in the raw bytes, `verify_checksums`. Before: `datastatus=1 hdustatus=0 status=fail`. | Mapping now fails only when a *present* checksum is wrong (`any < 0`), `"no_checksums"` only when both absent, `"ok"` for any correct-subset (docstring updated to match). Test `test_verify_datasum_only_reports_ok_not_fail`. After: `status="ok", ok=True, present=True`; corrupted cases stay `"fail"` (`(1,-1)` and `(0,-1)` pinned by `test_file_checksum_roundtrip_and_corruption`). | fixed |
| r4b-10 | MINOR | 1 | `_hdu_rewrite.py:_sanitize_header_for_compressed_write` | `bytes.decode("ascii", errors="ignore")` silently shortened non-ASCII bytes header values on the compressed *rewrite* path (the only path without a card replay): `replace_hdu(p, 0, TensorHDU(t, header={"RAW": b"\xff\xfe"}), compress=True)` wrote `RAW=''` with no error (the write() paths happen to fail later via the replay's `Unsupported FITS header value type`). | Strict `decode("ascii")` → `UnicodeDecodeError` at the boundary (docstring: bytes values must be ASCII strictly). Test `test_bytes_header_value_rejected_on_compressed_rewrite`. Before: `DID NOT RAISE`. After: `UnicodeDecodeError` raised. | fixed |
| r4b-11 | MINOR | 7 | `checksum_api.py:_validate_hdu` | `isinstance(hdu, int)` rejected numpy integers: `write_checksums(path, hdu=np.int64(0))` → `TypeError: hdu must be a non-negative integer`. | `operator.index` acceptance (bool still rejected first). Test `test_checksum_api_accepts_numpy_integer_hdu`. Before: `TypeError`. After: stamps + verifies `"ok"`. | fixed |
| r4b-12 | MINOR | 3 | `_hdu_rewrite.py:insert_hdu,replace_hdu` | `insert_hdu`/`replace_hdu(..., header=...)` mutated the caller's HDU object (`new_hdu._header = Header(header)`), silently rewriting `item.header` in the caller. | `_hdu_with_header` builds a header-replaced shallow copy (TensorHDU/TableHDU/TableHDURef/internal proxies); caller object untouched. Test `test_rewrite_ops_do_not_mutate_input_hdu`. Before: `assert {'NEW': 1} == {'MINE': 'orig'}`. After: `{'MINE': 'orig'}` preserved. | fixed |
| r4b-14 | BLOCKER | 1 | `_write_helpers.py:_write_boundary_header`; `_hdu_rewrite.py:_write_hdus_uncompressed,_write_hdus_with_optional_compression` (replay) | Integration regression from r4b-02's replay: the card replay re-injected a COMPRESSED image source's raw compression-bintable skeleton (ZIMAGE/ZCMPTYPE/ZBITPIX/ZNAXIS*/ZTILE*/ZNAME*/ZVAL* and tile-column TFORM/TTYPE cards) onto the freshly compressed extension — `write_hdu_header_cards`' skip list covers the structural block but not Z*/TFORM cards. The mangled tile layout made `torchfits.read(path, hdu=N)` surface the extension as a table dict: `test_insert_hdu_compressed_image_only` / `test_delete_hdu_compressed_image_only` died with `TypeError: equal(): argument 'input' (position 1) must be Tensor, not dict` after `insert_hdu`/`delete_hdu(..., compress=True)` on a `compress=True`-written file (mixed-HDU cases passed: their sources carry no Z*/tile cards). | `_write_boundary_header` now uniformly drops the CompImage Z* set (`_COMPRESSION_CARD_KEYS`/`_COMPRESSION_CARD_PREFIXES`, shared with `_strip_compression_cards`) plus the table-descriptor prefixes for every HDU kind — so neither the C++ mapping nor the replay can write derived compression/column cards. Regression evidence: `pixi run pytest tests/test_hdu_file_ops.py -q` — before: `TypeError: equal(): ... must be Tensor, not dict` ×2 (+ the r4b-15 pair); after: `tests/test_hdu_file_ops.py tests/test_complex_header.py tests/test_longstr_header_reassembly.py` → `23 passed, 1 skipped`. | fixed |
| r4b-15 | MAJOR | 1 | `_write_helpers.py:_preserved_table_tform_cards,_prepared_tform_code`; `_hdu_rewrite.py:_prepare_table_hdu_for_payload` | Integration regression from r4b-01's boundary drop: LEGIT source/user TFORM spellings were discarded on the rewrite/copy path and C++ re-derived explicit-repeat forms. astropy spells repeat-1 formats as bare type chars (ground truth measured by Main: `TFORM1='J'`, `TFORM2='I'`), so `table.write(schema={"QUAL": {"format": "I"}})` + `hdul.write` lost `'I'` → `'1I'` (`test_hdulist_write_preserves_table_extension_metadata`: `'1I'.startswith('I')` False) and `replace_column_file(..., format="J")`'s engine rewrite (`_table/mutation.py:_rewrite_table_hdu_with_schema` → `torchfits.replace_hdu` → this slice's `_prepare_table_hdu_for_payload`) lost `'J'` → `'1J'` in the file header while the in-memory mirror kept `'J'` (`test_replace_column_file_mirrors_tform`: `assert '1J' == 'J'`). Per Main's verdict this is a real contract (mirror/preserve user-specified TFORM spelling; astropy-conformant), fixed in code, tests untouched. | `_preserved_table_tform_cards` re-adds source `TFORMn` verbatim per column when the prepared storage still matches the TFORM type char (`_prepared_tform_code`: B/I/J/K/E/D/L/C/M; bit/string/VLA spellings `X`/`A`/`P`/`Q` always preserved). Decoded scaled columns whose storage left the TFORM's domain (e.g. TSCAL/TZERO-scaled ints materialized as floats) still re-derive — the r4b-01 corruption guard stays intact (its own test's incidental `'1J'` literal replaced with a semantic astropy-decoded dtype pin per test policy). Before: `assert '1J' == 'J'`, `assert False ... '1I'.startswith('I')`; after: named suite `23 passed, 1 skipped`, owned suites + oracle `257 passed, 2 skipped`. | fixed |
| r4b-13 | BLOCKER | 1 | `_hdu/hdu_list.py:122` (`HDUList.fromfile`) + `cpp_src/fits_file.cpp` `open_and_read_headers` (OUTSIDE slice B; cross-slice) | LONGSTRN `&`+CONTINUE chains are never reassembled on the `HDUList.fromfile` path. `torchfits.read_header(p)` returns the full 80-char value but `torchfits.open(p)[0].header` returns 68 (67 chars + restored `&`, CONTINUE card left unjoined). Every HDUList.write / `insert_hdu` / `replace_hdu` / `delete_hdu` rewrite therefore silently truncates >68-char string values at the first card (repro: `header={"LONGSTR":"A"*80}` → write → `insert_hdu` → astropy reads 67). The CONTINUE cards ARE present in the card tuple list from `open_and_read_headers`; `header_parser.py` already implements the chain logic for `read_header`. Fix sketch: chain-reassembly pass over the card tuple list at `fromfile`/`open_hdulist` using the `&`+CONTINUE convention (shared helper fits in `card.py`/`header.py`, R6-A's wheelhouse incl. `header-delitem-history`/`setkey-no-rewrite` invariants). | Out of slice B's exclusive file set — Main DECISION: routed to R6 as named BLOCKER (root fix at `_hdu/hdu_list.py:122` `Header(info.card tuples)` construction with a shared LONGSTRN `&`+CONTINUE reassembly helper — `card.py`/`header.py` is R6-A's wheelhouse; R6 must NOT re-touch the `open_hdulist` wrapper). In-wave mitigation owned by R4-EngineCore as **r4c-15** (post-`fromfile` reassembly in `hdu_api.open_hdulist`, covering all in-repo opens incl. `_hdu_rewrite.materialize`; residual = external direct `HDUList.fromfile` callers). Pin survives in `test_write_read_identity.py::test_rewrite_preserves_long_strings` — landed as `xfail(strict=True)` while the reader was broken, flipped to a normal passing test on the r4c-15 landing ping (it now pins the full `A*80` round-trip through `insert_hdu`, along with `test_header_commentary_cards_every_write_path`'s rewrite case). | deferred: routed to R6 (BLOCKER) |

## Evidence log (before → after)

All 14 new tests failing before the fix (excerpt of `pytest` run on unfixed code — `14 failed`):

```
FAILED test_table_write_int8_column_tbyte_convention - RuntimeError: Failed to write FITS file '.../i8_write-dict.fits': Dictionary table writes currently require CFITSIO-native column types ...
FAILED test_dict_image_extra_keys_rejected_not_silently_dropped - Expected regex: 'flux' / Actual message: "Failed to write FITS file '.../extra_plain.fits': Unsupported HDU item type: str"  (compressed variant silently dropped flux; no error)
FAILED test_copied_table_header_cannot_forge_rows_or_relabel_columns - assert 5 == 3  (NAXIS2 forged 5 rows on a 3-row write) / names ['OLDNAME'] / col [1, 8589934592, 0, 0, 0]
FAILED test_stale_checksum_cards_are_not_replayed - assert 'fail' == 'no_checksums'
FAILED test_quantized_dict_table_compressed_matches_plain - comp: TFORM2=1E   (quantize ignored)
FAILED test_header_commentary_cards_every_write_path - HISTORY [...] != ['h1', 'h2']  (+ '= \'h2      \'' mangled cards; LONGSTR truncated on rewrite)
FAILED test_multi_hdu_commentary_independent - - 'hh1',  + "= 'hh1     '",
FAILED test_write_header_param_overlays_first_hdu_every_path - AssertionError: plain-seq  assert None == 'yes'
FAILED test_dict_image_hdu_form_uncompressed - RuntimeError: Failed to write FITS file '.../dictimg.fits': Unsupported HDU item type: str
FAILED test_rewrite_ops_do_not_mutate_input_hdu - assert {'NEW': 1} == {'MINE': 'orig'}
FAILED test_bytes_header_value_rejected_on_compressed_rewrite - Failed: DID NOT RAISE UnicodeDecodeError
FAILED test_verify_datasum_only_reports_ok_not_fail - AssertionError: assert 'fail' == 'ok'
FAILED test_checksum_api_accepts_numpy_integer_hdu - TypeError: hdu must be a non-negative integer
FAILED test_quantize_honored_on_compressed_dict_writes - assert -32 == 16
14 failed
```

After the fixes (before r4c-15 landed):

```
tests/test_quantize_int16.py tests/test_checksum.py tests/test_write_fidelity.py tests/test_write_read_identity.py tests/test_writing.py
126 passed, 2 skipped (CUDA/MPS), 1 xfailed (r4b-13 pin) in 2.64s
```

Final state after the r4c-15 landing (r4b-13 pin flipped to a normal test):

```
127 passed, 2 skipped (CUDA/MPS)
```

Named oracle + write-contract suites (decode/write changes must satisfy these):

```
pixi run pytest -q tests/test_output_parity.py tests/test_hdu_table_contracts.py tests/test_io_invariants.py tests/test_compressed_nulls.py
130 passed in 9.89s
```

Integration re-check (r4b-14/r4b-15, after all R4 slices landed):

```
pixi run pytest tests/test_hdu_file_ops.py -q            # before fixes
FAILED tests/test_hdu_file_ops.py::test_replace_column_file_mirrors_tform - AssertionError: assert '1J' == 'J'
FAILED tests/test_hdu_file_ops.py::test_hdulist_write_preserves_table_extension_metadata - '1I'.startswith('I') fails
FAILED tests/test_hdu_file_ops.py::test_insert_hdu_compressed_image_only - TypeError: equal(): argument 'input' (position 1) must be Tensor, not dict
FAILED tests/test_hdu_file_ops.py::test_delete_hdu_compressed_image_only - TypeError: equal(): argument 'input' (position 1) must be Tensor, not dict
4 failed, 11 passed, 1 skipped

pixi run pytest tests/test_hdu_file_ops.py tests/test_complex_header.py tests/test_longstr_header_reassembly.py -q   # after fixes
23 passed, 1 skipped

pixi run pytest -q tests/test_quantize_int16.py tests/test_checksum.py tests/test_write_fidelity.py \
  tests/test_write_read_identity.py tests/test_writing.py tests/test_output_parity.py \
  tests/test_hdu_table_contracts.py tests/test_io_invariants.py tests/test_compressed_nulls.py
257 passed, 2 skipped
```

Verified areas (reviewed, no defect to fix):
- `QuantizeError`/`UInt64WriteError` typed contract (A-04 area) confirmed holding: `except (UInt64WriteError, QuantizeError)` re-raises typed before the `RuntimeError` wrap; no `str(e)` matching anywhere in the slice.
- `int16-robust-quantize` invariant confirmed: percentile bulk range + clip (never global min→max default), `BLANK_CODE=-32767` cannot collide with packed codes (`_SHRT_MIN_EFF=-32766` clamp), non-finite samples become the BLANK/TNULL sentinel (round-trips to NaN on read/subset/`table.read_torch`/Arrow IS NULL — pinned by `test_io_invariants.py`, `test_hdu_table_contracts.py`), degenerate `hi<=lo` packs to exact constant, `keep_zero` keeps BZERO=0. One cosmetic stat gap kept as-is (all-non-positive `keep_zero` counts only non-finites in `n_clipped`; shared path counts negatives too — `n_clipped` is advisory).
- Checksum DATASUM recomputation on rewrite confirmed: `insert_hdu`/`delete_hdu`/`replace_hdu` restamp via `_hdus_have_checksums` → `_write_all_checksums`; all HDUs re-verify `"ok"` after rewrite (before-evidence of this is the r4b-08 P7 probe: `initial=('ok','ok') after_insert=['ok','ok','ok'] after_delete=['ok','ok']`).
- Compressed rewrite scale survival: uint16/uint32 pseudo-unsigned and quantized packs keep BSCALE/BZERO/BLANK via the (now unconditional) card replay — also fixes compressed `insert_hdu(..., compress=True)` which previously lost BZERO (probe: `27232` read instead of `60000` at HEAD; covered post-fix by `test_compressed_uint16_roundtrip_exact` + the r4b-04 tests).
- 80-char card / CONTINUE / HIERARCH write-side limits confirmed: `fits_update_key_longstr` splits >68-char values into CONTINUE cards on all three C++ dict writers and `write_hdu_header_cards` (HIERARCH long keywords round-trip both torchfits and astropy). The only long-string defect found is on the READ side (r4b-13, cross-slice).
- ASCII widths / string padding: all-empty string columns write `1A` and round-trip `["", "", ""]`; wide-string truncation to ASCII field width on append/update matches astropy (pinned `test_ascii_string_width_handling_matches_astropy`); non-ASCII str/bytes string values raise `table string value must contain only printable ASCII characters` (C++ typed) — `_decode_col_string`'s `errors="replace"` mangled text is always caught by that check before writing (no silent loss).
- uint64 writes raise typed `UInt64WriteError` (images + table columns); `write(..., '.bz2')` typed refusal; overwrite atomicity/mode preservation and failed-overwrite preservation unchanged (pinned).

## Per-file disposition

| file | depth | finding IDs | status |
|---|---|---|---|
| `src/torchfits/_io_engine/write_api.py` | deep (full review; write() restructure) | r4b-01, r4b-02, r4b-03, r4b-04, r4b-06, r4b-07, r4b-08 | fixed |
| `src/torchfits/_io_engine/_write_helpers.py` | deep (all normalizers/gates/proxies) | r4b-01, r4b-02, r4b-03, r4b-04, r4b-05, r4b-08, r4b-12, r4b-14, r4b-15 | fixed |
| `src/torchfits/_io_engine/_hdu_rewrite.py` | deep (rewrite + compressed/uncompressed writers) | r4b-02, r4b-08, r4b-10, r4b-12, r4b-14, r4b-15, r4b-13 | fixed (r4b-13 deferred: routed to R6) |
| `src/torchfits/_io_engine/quantize.py` | deep (robust/minmax/dequantize + spec parsing) | — (verified: `int16-robust-quantize` + BLANK/TNULL sentinel rules hold) | clean |
| `src/torchfits/_io_engine/checksum_api.py` | deep (typed errors, ffvcks mapping, edge inputs) | r4b-09, r4b-11 | fixed |

Deferred (one line each):
- r4b-13 — `_hdu/hdu_list.py:122` `HDUList.fromfile` drops LONGSTRN `&`+CONTINUE reassembly (evidence: `open()[0].header` = 68 chars vs `read_header()` = 80 for a `A*80` value; every rewrite truncated it) — routed to R6 as named BLOCKER per Main (root fix at `Header(info.card tuples)` construction; R6 must NOT re-touch the `open_hdulist` wrapper). In-wave mitigation r4c-15 (R4-EngineCore) landed; my pin passes since.
