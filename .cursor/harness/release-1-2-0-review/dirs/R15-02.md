# R15-02 — docs/ slice 2

Paths: `splits/docs.chunk02` (23 files, `docs/cli-recipes.md` through `docs/torchfits-logo.png`).

Findings in this slice: r15a-01 (`docs/compatibility.md`), r15a-04 (`docs/cli.md`, `docs/examples.md`), r15a-05 and r15a-08 (`docs/release.md`). See `dirs/R15.md`.

`docs/cli.md` already had exit code 5 and JSON `null` for non-finite `stats`/`table` values. `docs/install.md` already explains `KMP_DUPLICATE_LIB_OK`. `docs/parity.md` Supported rows were not re-run; gaps go to the R18 REPORT. Logos, CSS, and `katex.js` are existence checks.
