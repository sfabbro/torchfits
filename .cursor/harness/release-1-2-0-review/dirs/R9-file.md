# R9 slice B — C++ FILE family

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r9b-01 | BLOCKER | 1 — silent data loss | `fits_detail.h:cfitsio_float_nulval_ptr` | Passing a non-null nulval for compressed float/double storage makes CFITSIO's `fnan()` replace infinities and exponent-zero values (including signed zero and subnormals) with the nulval. Probe `exp_r9b.py` probe 2d wrote a RICE_1 image containing `-0.0`, `±Inf`, and NaN; Astropy and `read_full` preserved the special values, while the four shared-helper binding paths did not. Slice A's failing-first pin reported `read_full_numpy compressed float32: bytes differ`, 5/1024 mismatches including infinities, signed zeros, and the subnormal. | Key the nulval decision on storage BITPIX, not compression: native float/double storage always passes null; integer storage promoted to float still passes NaN for BLANK. Contract: all read paths preserve finite values, signed zero, infinities, subnormals, and NaN placement; NaN payload bits are not promised across codecs. Final pin and all six owned/compression paths pass. | fixed |
| r9b-02 | BLOCKER | 7 — hostile input / UB | `fits_file.cpp:SubsetReader::init_from_hdu, ensure_data_mmap`; `fits_file.h` | A 2-D BITPIX=8 header with `NAXIS1=6148914691236517205` and `NAXIS2=3` has `SIZE_MAX` pixels. `map_len = nbytes + page_delta` wrapped, the mapping appeared file-sized, and the first 4×3 cutout copied past CFITSIO/file storage. Final failing-first `test_hostile_naxis_subset_reader_never_crashes` subprocess ended `returncode=-11` with empty stdout/stderr. | Validate the complete NAXIS product during construction using the shared overflow-checked product, and separately check both mmap address additions before `mmap`. Zero axes remain legal because clipped empty windows return an empty tensor without native I/O. Contract: hostile/absurd products raise `RuntimeError` containing `NAXIS product overflow`; zero-pixel empty windows preserve their non-degenerate dimension. Final owned suite passes. | fixed |
| r9b-03 | BLOCKER | 4 — concurrency / lifetime | `fits_file.cpp:FITSFile::*`; `fits_file.h:FITSFile::io_mutex_` | One persistent CFITSIO handle has one mutable CHDU cursor and mutable per-handle caches. Concurrent `read_full(fh, hdu, ...)` calls moved that cursor between metadata selection and pixel decode, returning another HDU's pixels; close could free the handle during decode. Dead-agent probe 7 reported nonzero `race anomalies`; the finalized failing-first subprocess (three threads × 500 reads) ended `returncode=-11`. | Serialize every public `FITSFile` operation and `close()` with one recursive per-handle mutex. Contract: concurrent reads on one handle return only their requested HDU; close cannot invalidate an in-flight read, and operations after close raise `RuntimeError`. Final subprocess race regression passes. | fixed |
| r9b-04 | BLOCKER | 1/3 — wrong generation / lifetime | `fits_detail.h:get_shared_raw_fd`; `fits_file.h:SubsetReader::shared_meta_`; `fits_file.cpp:SubsetReader` | Shared metadata snapshotted the original path generation, but a lazy raw-fd open after atomic replacement could return the replacement inode. Probe: open a 100×100 all-ones image, atomically replace the path with a 200×200 all-sevens image, then read the existing handle. The pre-fix path could serve the replacement (`shape=(200,200)`, unique `[7]`) instead of open-time snapshot bytes (`shape=(100,100)`, unique `[1]`). The first post-fix run also showed a `SubsetReader` could reacquire a replacement fd after shared-cache invalidation. | After opening/lazy acquiring the raw fd, compare `fstat` inode, size, and mtime with the shared metadata snapshot; on mismatch return null so the caller falls back to its already-open CFITSIO generation. `SubsetReader` captures its shared metadata generation at construction and never reacquires a fresh generation during lazy mmap. Contract: an existing handle never mixes generations; truncated native data raises. Both replacement tests pass after round 2. | fixed |
| r9b-05 | CLEANUP | dead state | `fits_detail.h:SharedReadMeta::current_fits_hdu`; `fits_file.cpp:FITSFile::ensure_hdu` | `current_fits_hdu` was assigned by `ensure_hdu` but had no reader anywhere in `src/` or `tests/`; it duplicated the authoritative private per-handle `current_hdu_` and was removed with its invalidation write. | Repository-wide grep after removal returns zero `current_fits_hdu` references. The r9b-03 mutex, not shared selector state, is the concurrency contract. | fixed |
| r9b-06 | MINOR | 3 — lifetime / defense in depth | `fits_rw.h:open_fits_for_write` | Considered adding speculative partial-handle cleanup on failed write opens. Exact `/proc/self/fd` probe `exp_fdleak.py` performed 200 warmed failing opens and printed `FDDELTA_FAILING_OPENS 0`; the current CFITSIO version leaves no descriptor behind. | No source change. This is defense in depth for a leak not reproduced on the supported CFITSIO path; the exact probe is recorded to prevent speculative churn. | deferred |

## Deferred / no-action candidates

- `r7a-08` HDU-count under-report: fixed by slice A in `fits_bindings.cpp:checked_num_hdus`; do not duplicate here.
- Failed `open_fits_for_write` descriptor hardening: deferred as defense in depth with exact probe above.
- `tests/test_read_pipeline_fallback.py` is absent at this revision; no fallback command was run.

## Rebuild and validation

- Two serialized source rebuilds were required: the first exposed the incomplete `SubsetReader` generation snapshot, and round 2 included that fix. Both `pixi run dev` and `pixi run -e test -- pip install -e . --no-build-isolation` completed successfully.
- Failing-first evidence: hostile NAXIS subprocess `returncode=-11`; concurrent handle subprocess `returncode=-11` after the finalized 3-by-500 harness; compressed-float pin byte mismatch (5/1024) before the helper edit; first snapshot run's `SubsetReader` replacement test did not raise before pinning its metadata generation.
- Passing evidence: owned suites `30 passed`; slice-A contracts `79 passed`; `tests/test_output_parity.py` `69 passed`; adjacent image consumers `114 passed`; explicit compressed-IEEE, hostile-NAXIS, thread-race, and two replacement tests `5 passed`.
- `current_fits_hdu` repository-wide grep after removal: no matches.

## Per-file disposition

| File | Disposition |
|---|---|
| `src/torchfits/cpp_src/fits_file.cpp` | Full review complete. Fixed hostile-NAXIS mmap overflow (r9b-02), serialized persistent-handle operations and close (r9b-03), pinned `SubsetReader` generation (r9b-04), and removed dead selector state (r9b-05). Existing constructor partial-handle cleanup, BScaleGuard RAII, zero-HDU handling, and network-open security coverage remain intact. |
| `src/torchfits/cpp_src/fits_detail.h` | Full review complete. Fixed compressed float IEEE nulval selection (r9b-01) and generation-checked shared raw-fd acquisition (r9b-04). Shared metadata invalidation, UID rotation, refcounted descriptor lifetime, NAXIS product checks, and BLANK promotion remain intact. |
| `src/torchfits/cpp_src/fits_file.h` | Full review complete. Added only internal per-handle serialization state and the `SubsetReader` generation snapshot state required by r9b-03/r9b-04; no public API symbols added. |
| `src/torchfits/cpp_src/fits_handle.h` | Full review complete. Move-only `FitsHandleGuard` correctly closes owned CFITSIO handles once; no finding or edit. |
| `src/torchfits/cpp_src/fits_rw.h` | Full review complete. `open_fits_for_write` callers provide security guards and the helper's failed-open path was empirically descriptor-clean; no source edit. |
