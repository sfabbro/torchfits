# R3 — `src/torchfits/data/`

Slices: A `dirs/R3-datasets.md` (r3a-*), B `dirs/R3-remote.md` (r3b-*).

## Totals

22 findings (r3a 17 incl. record-only r3a-17, r3b 8): 19 fixed, 3 deferred/record-only.
Blockers fixed (3): r3a-01 cube `spectral_slice`/`slice_index` sliced the channel axis instead of
the spectral axis; r3a-02 staged-cutout delete-under-open race + duplicate-URL re-download (this is
B's r3b-08 fix site — A landed it under r3a-02; the r3b-08 repro passes as a regression test);
r3b-02 zero-byte bodies silently promoted to the permanent cache.

Majors fixed (13): r3a-03..r3a-13 (duplicate-path label collapse; label_key= raw-URI double fetch;
discover_bands silent band drops; band_zeropoints unselected bands; flux_scale silent EXPTIME
omission; rank-out-of-contract silent shard duplication; iterable companion arity; table-spectrum
arm drops; staged dup-URL re-download 2→1 count proof 106.1ms→54.7ms; make_loader staged
double-fetch 2→1 count proof 405ms→203ms (split with r3b-06); cutout_size≤0 empty cutouts);
r3b-01 shared .partial writer race; r3b-03 resume-416 permanent wedge; r3b-04 temp-name leaks;
r3b-06 make_loader prefetch skip.

Minors (4): r3a-13 `cutout_size≤0`, r3a-15 `from_bands` docstring, r3b-05 malformed
Content-Length, r3b-07 stale docstrings. CLEANUP: r3a-14 worker-split ×3 →
`_shard_work_plan` (zero-behavior, pinned).

## Sharding evidence (plan-named checks)

`TestTableSharding::test_partition_exact_for_uneven_and_oversized_worlds` over (1,1)(2,2)(3,2)
(4,3)(16,2) × 8 rows (uneven + world_size·num_workers > nrows); `TestIterableFileSharding` (8,3)
over 5 files; `len()` vs yield (`test_iterable_epoch_yield_matches_map_len`); shuffle determinism
and rank/worker disjointness (`test_shuffle_deterministic_and_shards_disjoint`). DQ `bad_bits`
convention re-verified end-to-end vs R2's `transforms/mask.py` (True=valid, 0-based) — existing
pins suffice.

## Deferred (→ backlog at R17)

- r3a-16 `FitsSpectrumDataset._read_table_arm` ignores `mmap=` (accepted-and-ignored flag) —
  plumb or document at R15.
- r3a-17 record-only routing (already owned by later rounds): `read_images_batch` fallthrough at
  `_io_engine/batch.py:43` + `_read_pipeline.py:432-443` → R4; NAXIS2→0 swallows at
  `_hdu/table_hdu.py:202-207`, `_hdu/table_hdu_ref.py:54-58` → R6, `_table/mutation.py` ×4 → R5.
- r3b-08 FIXED via r3a-02 (kept in ledger against datasets.py).

## Evidence

Failing-first pasted in slice files (A: 14 failed + 3 failed on first runs; B: 6 failing-first).
Combined-tree targeted run before commit: **155 passed** (5 R3 suites; no stale xfail markers —
the r3b-08 repro XPASS window closed when A's fix landed).

## Plan deviations / integration notes

- Same `chore(changelog): R3 round fixes` subject convention as R1/R2.
- Integration-owned follow-up: `style: ruff pass over R3 tests` (formatter pass only; mypy clean).

## Gate results

- `pixi run preflight-push` → PASS.
- `pixi run test` → PASS (1789 passed, 24 skipped, 21 xfailed, 386 warnings in 297.47s; +34 over R2's 1755).
