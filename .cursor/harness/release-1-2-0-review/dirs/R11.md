# R11 — marker policy and release-gate list diff

R11-Policy. Slice findings: `dirs/R11-00.md`, `dirs/R11-01.md`, `dirs/R11-02.md`, `dirs/R11-03.md`, `dirs/R11-04.md`.

## Marker policy

- `pixi.toml` task `test` still runs `pytest tests/` with no `-m` filter. The comment on that task states: test and release-gate run the full suite including performance markers; the GHA PR matrix excludes them.
- `pixi.toml` task `release-gate` is unchanged (full file list, no `-m` filter).
- `.github/workflows/ci.yml` job `test`, step `Pytest`, is `pytest tests/ -q -m "not performance"`.
- `wheel-smoke`, `bench-smoke`, `docs-contract`, and `release-gate` do not use that filter.

## Release-gate pytest file lists

Compared the pytest arguments of the `pixi.toml` `release-gate` task (the `pytest … -q` segment only) with `.github/workflows/ci.yml` job `release-gate`, step `Release gate tests`.

**Identical.** 20 files, same order. only-in-pixi: none. only-in-GHA: none. Lists were not edited.

1. `tests/test_fitsio_upstream_smoke.py`
2. `tests/test_astropy_upstream_smoke.py`
3. `tests/test_package_isolation.py`
4. `tests/test_docs_integrity.py`
5. `tests/test_fits_schema.py`
6. `tests/test_read_policy.py`
7. `tests/test_examples_runner.py`
8. `tests/test_scale_on_device.py`
9. `tests/test_data.py`
10. `tests/test_transforms.py`
11. `tests/test_transforms_e2e.py`
12. `tests/test_api.py`
13. `tests/test_arrow_table_api.py`
14. `tests/test_security.py`
15. `tests/test_release_smoke.py`
16. `tests/test_cli.py`
17. `tests/test_public_boundary.py`
18. `tests/test_http_probe_fixture.py`
19. `tests/test_mps.py`
20. `tests/test_io_invariants.py`

## Round totals

Five slices over the 101 precomputed chunk files, plus the marker-policy edit. 17 fixed, 4 deferred. No `src/` edits. Numeric oracle `tests/test_output_parity.py` was not relaxed.

Fixed: cwd fixtures under `tmp_path` (ascii, complex header, read_header, validation, writing, patch-bench CSVs, vendored-CFITSIO header path); `@pytest.mark.performance` only where pass/fail is wall-clock or RSS; prefetch and RSS-sampler sleeps replaced with `threading.Event`; int16 arith asserts the rails `32767`/`-32768`; signed-zero checks use `np.signbit`; repr-only transform tests and the private scale-helper absence pin removed. The 1.1 s sleep in `test_hdu_file_ops.py` stays: it waits out the shared-meta 1000 ms re-stat interval.

Deferred: source-text bench pins (`r11a-07`), vacuous cache-clear (`r11a-08`), misnamed `disk=True` smoke (`r11a-09`), duplicated invalid-mode block (`r11a-10`).

## Post-chunk tests

32 test modules were added in R1–R10 after `splits/tests.chunk*` was cut, so they were not in a slice. A scan found no wall-clock or RSS assertion and one `time.sleep(0.2)` in `tests/test_remote_resume.py:_FakeVosClient.copy`, after a `threading.Barrier(2)`, to keep both writers inside `copy()`. Left as written. Ledger rows for those files are scan-depth, not a full hygiene pass.

## Gate results

- `pixi run preflight-push` → PASS (before the full suite).
- `pixi run test` → PASS (2221 passed, 24 skipped, 21 xfailed, 383 warnings in 474.31s). 13 fewer than R10's 2234: the 12 repr-only transform methods plus `test_scale_on_device_dead_helpers_removed`.
