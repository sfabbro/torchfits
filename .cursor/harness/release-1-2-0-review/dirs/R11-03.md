# R11-03 — tests chunk 03 + marker-policy files

Agent `R11-Policy`. Chunk: `splits/tests.chunk03` (21 files, `tests/test_performance.py` through `tests/test_table_filter_grammar.py`), plus exclusive edits to `pixi.toml` and `.github/workflows/ci.yml`. IDs `r11d-NN`. Policy record: `dirs/R11.md`.

`tests/cpp/test_bracket_detection.cpp` was not edited. `test_native_cfitsio_bracket_detector_probe_runs` still compiles `tests/cpp/test_bracket_detection.cpp` and still requires `all checks passed` on stdout.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r11d-01 | MINOR | CI hygiene | `pixi.toml:test`, `.github/workflows/ci.yml` job `test` step Pytest | `@pytest.mark.performance` is declared and was unused, so the GHA PR matrix ran wall-clock and RSS asserts. Local `test` and `release-gate` must keep running them. | Comment on the `pixi.toml` `test` task. Job `test` step `Pytest` is `pytest tests/ -q -m "not performance"`. No other job gained that filter. `release-gate` file lists untouched (identical; see `dirs/R11.md`). | fixed |
| r11d-02 | MINOR | CI hygiene | `tests/test_performance.py:TestPerformance`, `TestPerformanceRegression`; `tests/test_security.py:test_header_large_dict_construction_fast`; `tests/test_table.py:TestTablePerformance.test_chunked_reading_performance`, `test_memory_efficiency` | Pass/fail asserts wall-clock or RSS: image read bounds (`< 5/10/15/30 s`, cache `<= 2×`, baseline `< 2 s`), RSS `< 3×` file or data size, table scan `< 30 s` and RSS `< 5×` file size, `Header(2000)` `elapsed < 0.5`. Timers that do not decide pass/fail were left unmarked. `test_tzero_floating_point_imprecision` asserts uint16/uint32 only. `test_table_streaming_memory_budget` asserts `total_rows == 10000` only. | `@pytest.mark.performance` on both performance classes, the Header timing test, and the two table methods. Collect-only below (12 selected). Security rejection pins and the bracket-probe wrapper were not changed. | fixed |
| r11d-03 | MINOR | test hygiene | `tests/test_read_header.py:test_read_header` | Fixture path was the relative name `test_read_header.fits`, created in the process cwd and removed in `finally`. A failure before `finally` leaves the file in the repo. | Write `tmp_path / "test_read_header.fits"`. Header assertions unchanged. Chunk pytest left no `test_read_header.fits` in the cwd (absent before and after). | fixed |

## Marker collect-only

`pixi run pytest tests/test_performance.py tests/test_security.py tests/test_table.py -m performance --collect-only -q`

```
tests/test_performance.py::TestPerformance::test_zero_copy_performance
tests/test_performance.py::TestPerformance::test_memory_efficiency
tests/test_performance.py::TestPerformance::test_batch_reading_performance
tests/test_performance.py::TestPerformance::test_cache_performance
tests/test_performance.py::TestPerformance::test_large_file_performance
tests/test_performance.py::TestPerformance::test_accelerator_transfer_performance
tests/test_performance.py::TestPerformance::test_subset_reading_performance
tests/test_performance.py::TestPerformanceRegression::test_read_performance_baseline
tests/test_performance.py::TestPerformanceRegression::test_memory_usage_baseline
tests/test_security.py::test_header_large_dict_construction_fast
tests/test_table.py::TestTablePerformance::test_chunked_reading_performance
tests/test_table.py::TestTablePerformance::test_memory_efficiency

12/52 tests collected (40 deselected) in 1.54s
```

`test_tzero_floating_point_imprecision` is among the 40 deselected.

## Not changed (reviewed)

- `tests/test_shared_meta_staleness.py` sleeps `_VALIDATE_INTERVAL_S` (1.2 s) three times. That waits out `SharedReadMeta`'s documented 1000 ms re-stat interval, not a scheduling race. Sleeps stay.
- `tests/test_security.py` allowance tests (`test_forced_overwrite_prefix_allowed`, `test_valid_filenames_allowed`) still accept a missing file as long as the error is not `Security Error`. Live path today is `RuntimeError` without that text (`Could not open FITS file`). The `except Exception: pass` fallback was not narrowed, so the R10 allowance pin is unchanged.
- `torchfits.cpp` uses in this chunk are the deprecation-warning pin (`test_public_boundary.py`) and the SSRF pin on the legacy alias (`test_read_blocks_private_cfitsio_http_url`). Neither was migrated or weakened.
- No test was deleted. No assertion was relaxed to match a product bug.

## Chunk pytest

`pixi run pytest` on the 21 chunk files, `-q`: **197 passed, 2 skipped, 9 warnings in 19.34s**. The two skips are the no-accelerator guards (`test_accelerator_transfer_performance`, `test_read_uint16_fits_native_dtype_accelerator`). cwd check: `test_read_header.fits` absent before and after.

## Disposition

| File | Disposition |
|---|---|
| `tests/test_performance.py` | fixed (r11d-02): class markers on every test method; each asserts wall-clock or RSS |
| `tests/test_public_boundary.py` | clean; `torchfits.cpp` deprecation pin kept |
| `tests/test_public_where.py` | clean |
| `tests/test_quantize_int16.py` | clean; int16 quantize numeric pins kept |
| `tests/test_read_header.py` | fixed (r11d-03): fixture moved under `tmp_path` |
| `tests/test_read_policy.py` | clean |
| `tests/test_release_lane.py` | clean; reads repo files, no cwd writes |
| `tests/test_release_smoke.py` | clean; `NamedTemporaryFile` |
| `tests/test_remote_http_range.py` | clean; fixtures under `tmp_path`; process race uses join, not sleep |
| `tests/test_rgb.py` | clean |
| `tests/test_scale_on_device.py` | clean; CPU path asserts int8 vs fitsio; uint16 accelerator skips without a device |
| `tests/test_security.py` | fixed (r11d-02) for `test_header_large_dict_construction_fast` only; R10 rejection pins and bracket-probe filename contract kept |
| `tests/test_shared_meta_staleness.py` | clean; 1.2 s sleeps stay (cache-validate interval) |
| `tests/test_skinny_meta.py` | clean |
| `tests/test_staged_prefetch.py` | clean; concurrent case uses `threading.Event` |
| `tests/test_stream_table_and_cache.py` | clean |
| `tests/test_subset_3d.py` | clean |
| `tests/test_table.py` | fixed (r11d-02) on the two RSS/wall-clock methods; `test_tzero_floating_point_imprecision` unmarked |
| `tests/test_table_docs_smoke.py` | clean; `NamedTemporaryFile` |
| `tests/test_table_file_ops.py` | clean; `NamedTemporaryFile` / `tmp_path` |
| `tests/test_table_filter_grammar.py` | clean |
| `pixi.toml` | fixed (r11d-01): `test`-task comment only; `test` and `release-gate` commands unchanged |
| `.github/workflows/ci.yml` | fixed (r11d-01): job `test` Pytest filter only |
