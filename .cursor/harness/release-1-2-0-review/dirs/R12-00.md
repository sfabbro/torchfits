# R12-00 — benchmarks chunk 00

Owned: `splits/benchmarks.chunk00` (15 files, `benchmarks/__init__.py` through `benchmarks/bench_import_boundary.py`). IDs `r12a-NN`.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r12a-01 | MAJOR | measurement | `benchmarks/bench_fits_io.py:_benchmark_cutout_rows`, `_benchmark_repeated_cutout_rows`, `_benchmark_random_extensions` | `*_mb_s` and `size_mb` used `path.stat().st_size`. A 100×100 cutout, 50 windows, and 200 full-HDU reads were labeled MB/s of the whole file. Before, with the timer forced to 1.0 s: cutout `size_mb` 1.535 vs window 0.038; repeated 0.258 vs 50 windows 1.907; random-ext 0.058 vs 200 planes 0.049. Rice HDUs store the image type in `ZBITPIX`, not the tile-table `BITPIX`. | `window_payload_bytes` / `plane_payload_bytes` / `mb_from_bytes`. `pixi run pytest tests/test_bench_ranking_mmap.py tests/test_bench_suites.py tests/test_patch_bench_docs.py -q` → 25 passed. | fixed |
| r12a-02 | MINOR | measurement | `benchmarks/bench_fits_io.py:_benchmark_headers` | `unit` was `ops/s` while `throughput` was empty, so the row claimed a rate it did not compute. Rankings use `time_s`. | `unit` is `""`. Covered by `test_header_row_does_not_claim_ops_per_second`. | fixed |
| r12a-03 | MINOR | measurement | `benchmarks/bench_arrow_tables.py:_time` | `_time` returns a median (docstring says so) but the CSV column was `mean_s`. No in-repo reader of that column. | Column renamed `median_s`. `std_s` is still the sample stdev. | fixed |
| r12a-04 | MINOR | measurement | `benchmarks/bench_gpu_transports.py:run_gpu_transport_rows` | GPU `cutout_100x100` and repeated-cutout rows set `size_mb` from the file size and left `throughput` empty while `unit` was `MB/s`. Same numerator bug as r12a-01. This host has no CUDA/MPS, so the path was not executed. | Same payload helpers. `throughput` is payload/time. CPU tests cover the helper on the fits_io call sites. | fixed |
| r12a-05 | MINOR | measurement | `benchmarks/bench_fits_io.py:_mb_per_second`, `benchmarks/bench_gpu_transports.py` read_full rows | Full-image `size_mb` / MB/s stays on-disk file bytes. For a compressed image that is not the decoded payload. Throughput on the GPU read_full row is left blank so a compressed file is not published as payload MB/s. Rankings use `time_s`. | Left as written. | deferred |

`compute_deficits` still emits every lag and sets `significance` to `noise` or `significant`. `tests/test_bench_ranking_mmap.py` expects `[]` only when there is no external peer. Warmup in `time_median` is owned by chunk 01 and was not edited here. `read_nrows` / `predicate_filter` strings in `bench_fitstable_io.py` were not moved (r11a-07).

## Disposition

| File | Disposition |
|---|---|
| `benchmarks/__init__.py` | clean |
| `benchmarks/bench_all.py` | clean — `gpu_only` skips CPU image/table suites; `run_gpu_transport_rows` returns `[]` on CPU |
| `benchmarks/bench_arrow_tables.py` | fixed (r12a-03) |
| `benchmarks/bench_cache.py` | clean — warmup excluded; not a scored contract CSV |
| `benchmarks/bench_contract.py` | clean — floors label significance, they do not drop rows |
| `benchmarks/bench_cpp_backend.py` | clean — reports both `mean_s` and `median_s` |
| `benchmarks/bench_denoise.py` | clean — throughput is `size_mb / time_s` of the stage payload; code-lines rows use `unit=lines` |
| `benchmarks/bench_fits_io.py` | fixed (r12a-01, r12a-02); r12a-05 deferred for full-image on-disk bytes |
| `benchmarks/bench_fits_write.py` | clean — payload MB/s of the written float32 array |
| `benchmarks/bench_fitstable_io.py` | clean — `unit=rows/s` is `nrows / time_s` |
| `benchmarks/bench_fixtures.py` | clean |
| `benchmarks/bench_gpu_memory.py` | clean — allocation probe, not a mislabeled MB/s |
| `benchmarks/bench_gpu_transports.py` | fixed (r12a-04); read_full throughput stays blank (r12a-05) |
| `benchmarks/bench_http_stream.py` | clean — cutout payload MB/s |
| `benchmarks/bench_import_boundary.py` | clean — cold-start seconds, not MB/s |
