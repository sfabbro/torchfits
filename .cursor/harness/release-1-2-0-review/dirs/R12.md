# R12 — benchmarks/

Slices: `dirs/R12-00.md` (r12a-01..05), `dirs/R12-01.md` (no code change).

Cutout, repeated-window, and 200-extension rates were whole-file MB/s. They now use the bytes read (Rice images via `ZBITPIX` / `ZNAXIS`). Header rows no longer claim `ops/s`. The Arrow CSV column is `median_s`. Full-image on-disk MB/s is unchanged (r12a-05).

## Gate results

- Targeted: `pixi run pytest tests/test_bench_ranking_mmap.py tests/test_bench_suites.py tests/test_patch_bench_docs.py -q` → 25 passed.
- `pixi run preflight-push` → PASS.
- `pixi run test` → PASS (2226 passed, 24 skipped, 21 xfailed, 383 warnings in 490.13s). +5 over R11's 2221: the cutout-payload and header-unit pins.
