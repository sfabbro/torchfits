# R19 — regression audit of 1bb6958..HEAD

Pass 2. Input is the review's own diff (93 commits, 267 files, +20738/−1856 at the start of the pass). Mandate: bugs the fixes introduced, missed callsites of contracts those fixes migrated, tests that do not assert, and a re-check of pass-1 "already correct" verdicts. Deferred items were not re-opened except where a later check showed the deferral was already closed or the doc was still false.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r19a-01 | MAJOR | missed callsite | `hdu_api.py:read_header_fast` | `fast_header=False` returned raw `cpp.read_header` triples. `torchfits.read(..., return_header=True, fast_header=False)` on an 80-char keyword kept the `&` marker (`'AAAA…&'`). The fast parser and `read_header` already joined it. Before: `test_read_fast_header_false_rejoins_longstr` failed on that inequality. | Slow fallback now runs `card._reassemble_longstr_cards`. | fixed |
| r19a-02 | MINOR | docs faithfulness | `docs/cli.md` `### setkey` | The setkey example passed `--comment`. `cmds_setkey.py` has no such flag (r7c-24, still open after R15). Before: `test_owed_behavior_notes_match_the_implementation` failed because `--comment` was in the section. | Those two example lines are gone. | fixed |

## Re-derived, not changed

- Double reassembly (`HDUList.fromfile` via `card.py`, then `open_hdulist`'s wrapper) does not fuse an orphan CONTINUE. Probe: `open()` keeps `NAXIS` and `Card('CONTINUE', 'orphan segment', '')`. The remaining split is typing (`NAXIS` is `"0"` on `HDUList`, `0` on `read_header`), which is the r6a-06 residual and stays deferred.
- `update_rows_mmap` calls `ensure_column_scale` before the scaled-column throw, and that throw happens before `open`/`mmap`. The r8a-02 no-op guard is not back.
- Every test function added since `1bb6958` contains `assert` or `pytest.raises` (0 exceptions in `tests/`).
- `{"HISTORY": ["h1", "h2"]}` still dies in `nb::cast<std::string>` (`std::bad_cast` → `RuntimeError`). The cast dates from `e9a9a483` (before this review). Still r7c-21, not a regression.
- `docs` `read_extname(path, hdu=1)` is an explicit extension example next to `read_hdu_type(path, hdu=0)`. Left as written; the default remains `hdu=0` (r1b-08).
- Pre-existing duplicate ledger row for `hdu_api.py` (the older R4-C line) was not collapsed.

## Gate

- `pixi run preflight-push` → PASS (426 files already formatted, mypy 95 files, check-lane, changelog-check).
- `pixi run test` → 2239 passed, 24 skipped, 21 xfailed, 383 warnings in 481.57s (EXIT:0). +1 over R16's 2238: `test_read_fast_header_false_rejoins_longstr`.
