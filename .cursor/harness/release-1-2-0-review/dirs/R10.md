# R10 — `cpp_src/` infra + native build (final Wave A round)

Single slice: `dirs/R10-infra.md` (r10a-01..07). 7 fixed, 0 deferred, no new public symbols.

Blocker: r10a-03 unaligned scalar-tail UB in EVERY 16/32/64-bit byteswap and pseudo-unsigned
variant (SIMD paths now use explicitly unaligned byte loads/stores; scalar tails `memcpy`).
Regressions: odd-column offsets, non-multiple tails, IEEE payload/denormal/inf/signed-zero/
unsigned-bit patterns + an ASan+UBSan probe across scalar/SSSE3/AVX2 for n=0..65, skews 0..7.

Majors: r10a-04 nanobind's `-Os` silently overrode the pyproject Release `-O3` (generated Ninja
now proves `-Os -O3` per TU); r10a-05 ASan/UBSan never reached vendored C/CFITSIO (propagation
proven in the sanitizer Ninja on C and C++ sources + the link line); r10a-02 the native bracket
detector test was manual-only — now compiled and run from `tests/test_security.py` in CI.

Decisions: r10a-01 — **r1c-12 parity resolved as a documented complementary-layer split**: Python
owns private-network classification (http/https/ftp schemes) while native CFITSIO security owns
pipe and `sh://` command-injection rejection including raw-extension consumers; hostile-name
coverage now exercises BOTH the Python façade and raw `_C`.

Verified (no change): r10a-06 no-SIMD fallback parity (probe), r10a-07 torch dtype/ABI shims,
DLPack ownership, MMap move-only RAII, `cpp-seal-all`.

## Gate results

- Integration combined sweep: 184 passed (security/io-invariants/native-infra/byteswap/parity/http).
- `pixi run preflight-push` → PASS.
- `pixi run test` → PASS (2234 passed, 24 skipped, 21 xfailed, 383 warnings in 487.21s; +16 over R9's 2218).
