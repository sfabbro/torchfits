# R5-interop — slice C review+fix record (`_table/interop.py`, `arrow_convert.py`, `cache.py`, `__init__.py`)

HEAD base: R5 worktree (after R1–R4). Oracle: `tests/test_output_parity.py` run post-fix — **69 passed**. Owned suites post-fix: **103 passed, 1 skipped (`No accelerator available` GPU test), 1 xfailed (strict pin, by design)**.

** ROUTED TO R6 — r5c-09 ** (see row + deferred section; strict-xfail pin landed).
** ROUTED TO R6 — r5c-15 ** (`_normalize_row_slice` dedupe; see row + deferred section).

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r5c-01 | BLOCKER | 1 silent wrong result | `_table/interop.py:_arrow_column_to_astropy` | Vector TNULL columns silently distorted: masked ints became **float64 NaN with no mask** (element nulls live in the FixedSizeList child, invisible to `chunked_arr.null_count`), vs astropy ground truth `MaskedColumn` int `(N, repeat)` + element mask. Repro: FITS `3J` + `TNULL=8` (rows `[[1,8,3],[8,5,6],[7,8,9],[1,2,8]]`) → `to_astropy` gave `Column float64 [[1.0,nan,3.0],…]`; astropy `Table.read` gives `MaskedColumn >i4 (4,3)` mask `[[F,T,F],[T,F,F],[F,T,F],[F,F,T]]`. Same for pure-Arrow `list_<int32>[2]` child-null input (object/NaN degradation). | New `_fixed_size_list_to_astropy`: offset-aware child slice, element + list-level masks, typed fills, exotic types → object MaskedColumn; scalar typed null path preserved. Tests `tests/test_interop.py::test_to_astropy_vector_tnull_matches_astropy` (astropy GT pin: dtype kind/itemsize, shape, mask, unmasked values) + `::test_to_astropy_arrow_fsl_nulls_masked_not_object`. Before: `AssertionError: expected MaskedColumn, got Column` (both). After: green. | fixed |
| r5c-02 | MAJOR | 5/7 deterministic failure on valid input | `_table/interop.py:_materialize_arrow_table`, `write_parquet`, `write_csv`, `write_ipc`, `to_pandas`, `to_polars` | Routed r1b-10: `torchfits.table.to_astropy(Path(...))` raises `TypeError: 'PosixPath' object is not iterable` (str-only dispatch fell into `pa.Table.from_batches(list(data))`); identical failure in every interop entry point with `pathlib.Path` data. | `_as_input_path()` (`str`/`os.PathLike` → `os.fspath`) at every entry; `_materialize_arrow_table` reads via `read(path, **kwargs)`. Tests `test_table_to_astropy_accepts_pathlike`, `test_interop_pathlike_data_inputs` (to_pandas + write_csv with Path). Before: `TypeError: 'PosixPath' object is not iterable` (both). After: green. | fixed |
| r5c-03 | MAJOR | 1 wrong result / shape contract | `_table/arrow_convert.py:_numpy_to_arrow_array`, `_uint8_matrix_to_fixed_bool_list`; consume: `_table/interop.py:_fixed_size_list_to_astropy` | Backlog "Arrow width-1 chunk route can surface `FixedSizeList<T>[1]` where the schema maps repeat==1 to scalar" re-derived BOTH directions. Produce: `_chunk_to_record_batch({"A": (3,1)}, fits_tform="1J")` → `fixed_size_list<int32>[1]` while every decode path + astropy/fitsio give scalar `(N,)` for repeat-1; `1X` (N,1) → `fixed_size_list<bool>[1]` vs scalar bool. Consume: `to_astropy` of `list_<int32>[1]` → `(N,1)` vs the repeat-1↔scalar mapping. | Produce: `_tform_is_scalar` gate (non-VLA repeat 1, codes `L B I J K E D C M`; `A`/`X` keep width semantics per the pinned "string matrices are never squeezed" contract; `1X` → scalar bool in the bit decoder). Consume: width-1 FixedSizeList surfaces scalar. Tests `tests/test_arrow_table_api.py::test_chunk_repeat1_vector_surfaces_scalar`, `::test_chunk_repeat1_tensor_surfaces_scalar`, `::test_chunk_repeat1_bit_surfaces_scalar_bool`, `::test_width1_fixed_list_to_astropy_is_scalar`, `::test_repeat1_round_trip_agrees_with_astropy` (vs astropy GT). Before: 5 failed (`FixedSizeList<item: int32>[1] != int32`; `(3,1) != (3,)`). After: green. | fixed |
| r5c-04 | MAJOR | 5 silent no-op | `_table/interop.py:to_astropy`, `_materialize_arrow_table`, `to_polars`, `to_duckdb`, `to_pandas`, `write_parquet/csv/ipc` | `to_astropy(**kwargs)` delegation contract violated: non-IO kwargs were **silently dropped** (`to_astropy(path, nonsense_kwarg=3)` returned a table) and in-memory inputs silently ignored `hdu=`/`columns=`. Same silent drop in `to_polars`/`to_duckdb`/writers for in-memory data; `to_pandas` silently dropped I/O kwargs on in-memory inputs. | Delegation contract: kwargs are read()/scan() I/O kwargs — path input forwards **verbatim** (unknown → `TypeError` from `read`); in-memory input + any kwargs → `TypeError` (`_reject_detached_kwargs`). `to_pandas`: I/O kwargs on in-memory data → `TypeError`; pandas kwargs still delegate. Test `test_to_astropy_rejects_unknown_kwargs` (path typo kwarg + in-memory `hdu=`). Before: `Failed: DID NOT RAISE TypeError`. After: green. All pre-existing call patterns in `tests/test_arrow_table_api.py` unaffected (70 tests green). | fixed |
| r5c-05 | MAJOR | 7 zero-row edge | `_table/interop.py:_materialize_arrow_table`, `write_parquet` (non-stream copy) | Zero-batch `RecordBatchReader` from a valid 0-row FITS table crashes every consumer: `to_astropy(reader(empty.fits))` → `ValueError: Must pass schema, or at least one RecordBatch` (reader yields 0 batches; `from_batches([])` needs the reader's schema). | `pa.Table.from_batches(list(data), schema=getattr(data, "schema", None))` in `_materialize_arrow_table` and `write_parquet`'s copy. Test `test_to_astropy_empty_reader`. Before: `ValueError: Must pass schema, or at least one RecordBatch`. After: green (empty Table, colnames preserved). | fixed |
| r5c-06 | MAJOR | 5/7 silent no-op on IO | `_table/interop.py:write_parquet`, `write_csv`, `write_ipc` (stream), `to_pandas` | Streaming writes of empty sources produced **no output file at all** (writer created lazily on first batch → zero batches → nothing written): `write_csv(out, empty.fits, stream=True)` → `out` never created — silent no-op on IO. Also `to_pandas(reader(empty))` lost the schema (`DataFrame()` with no columns). | Eager writer creation from `getattr(data_iter, "schema", None)` in all three stream writers (valid empty file with schema/header); `to_pandas` empty frames fall back to `Table.from_batches([], schema=…).to_pandas()`. Tests `test_stream_write_empty_source_creates_file` (csv header + parquet 0-row round-trip + ipc file), `test_to_pandas_empty_reader_keeps_columns`. Before: `assert o.csv.exists()` False; `assert [] == ['A']`. After: green. | fixed |
| r5c-07 | MINOR | 7 empty-result dtype | `_table/interop.py:_arrow_column_to_astropy` (old zero-chunk branch) | Zero-chunk FixedSizeList fallback hardcoded `np.empty((0, list_size), dtype=np.int64)` — empty results would lose the value dtype (e.g. float32 → int64). Latent on this pyarrow (`pa.table` over an empty typed array still yields one chunk / combine returns an Array) — public repro green before and after. | Folded into the r5c-01 rework: dtype derived from `arr_type.value_type` via `pa.array([], type=…).to_numpy().dtype`. Pin `test_to_astropy_empty_fixed_list_dtype` (public contract). | fixed |
| r5c-08 | MINOR | 5 error contract | `_table/interop.py:_astropy_fits_column_meta`, `_arrow_column_to_astropy`; `_table/arrow_convert.py:_coerce_null_sentinel`, `_decode_uint8_matrix_to_arrow` | Broad `except Exception` decode fallbacks; `_astropy_fits_column_meta` swallowed **IO errors** (`read_header` OSError) and silently dropped TUNIT/TDIM. Repro: monkeypatch `torchfits.read_header` → `OSError` → `to_astropy` returned a unit-less table silently. | Narrowed: meta extraction `except (KeyError, IndexError, TypeError, ValueError)` (IO/decode re-raise); sentinel coercion `(TypeError, ValueError, OverflowError)`; `to_numpy`/`pc.cast` fallbacks to `pa.ArrowInvalid/ArrowNotImplementedError/ArrowTypeError` (specific decode exceptions; fallback behavior preserved). Test `test_to_astropy_meta_ioerror_propagates`. Before: `Failed: DID NOT RAISE OSError`. After: green. | fixed |
| r5c-09 | MAJOR | 5 error contract | `_hdu/table_hdu_ref.py:306` `to_arrow`/`scan_arrow`/`reader_arrow` | ** ROUTED TO R6 — r5c-09 ** `TableHDURef.to_arrow(columns=…)` kwarg collision. Repro: `with torchfits.open(p) as hdul: hdul[1].to_arrow(columns=["A"])` → `TypeError: torchfits._table.read.read() got multiple values for keyword argument 'columns'` — the method forwards `columns=…`/`row_slice=…`/`hdu=…` explicitly then `**kwargs` collides. File is outside slice C's exclusive set (R6-B owns `_hdu/table_hdu_ref.py`); per Main's routing decision this is not edited here. **Fix sketch (contract decided here): REJECT.** `to_arrow`/`scan_arrow`/`reader_arrow` must raise a clear `TypeError` naming the conflict when `hdu`/`columns`/`row_slice` appear in `kwargs` (e.g. `"to_arrow() got an unexpected keyword argument 'columns'; the ref's column selection and row window apply — narrow the ref (ref[cols], ref.head/tail) or call torchfits.table.read directly"`), and each method docstring must state this contract. Merge/intersect semantics were rejected: silently narrowing a result set is another silent-wrong-result path, and dict-like selection already exists (`ref["col"]`). | Strict-xfail pin landed: `tests/test_table_squeeze_and_ref_cache.py::test_tablehduref_to_arrow_columns_kwarg` (`@pytest.mark.xfail(strict=True, reason="r5c-09 to_arrow(columns=) kwarg collision at _hdu/table_hdu_ref.py:306; R6-B flips this")`) — currently `xfailed`; goes **red on XPASS** when R6-B lands the fix (loud by design; R6-B flips the test to plain passing). | deferred: routed to R6 (MAJOR) |
| r5c-10 | CLEANUP | 3 lifetime | `_table/cache.py`, `_table/__init__.py` | Routed candidate "_table/cache.py should be absent post-Option A — confirm residue" RE-DERIVED: **not residue — live acquisition seam**. Repo-wide grep proof: `from .._table.cache import _acquire_cpp_handle, _acquire_cpp_reader` at `_read_scan.py:11` (used `:487, :528, :556`), `_read_where.py:15` (used `:201`), re-exported by `_table/__init__.py:3-6`; tests patch it (`tests/test_stream_table_and_cache.py:22`, `tests/test_where_and_batch_errors.py:41,85`). Deleting it would break three modules. Module docstring already documents the Option-A role honestly (per-call private handles, no cross-thread cache, owner-closed lifetimes) and `guard_fits_path` runs before each open. | Zero-behavior-change confirmation only; no code change required. | fixed |
| r5c-11 | MINOR | 3 lifetime/RAII | `_tensor_buffer.tensor_to_arrow_array` seam, `_C.echo_tensor` DLPack (`cpp_src/torch_compat.h`) | DLPack round-trip lifetime reviewed (bug class 3): `python_to_tensor` renames the capsule to `used_dltensor` before ATen ownership (no double-deleter) and `at::fromDLPack` retains the exporting storage; `pa.py_buffer(tensor.numpy())` chains buffer → ndarray → `.base` tensor → storage. No defect found; behavior pinned so a future refactor cannot silently break it. | Pins (green before and after): `tests/test_dlpack_roundtrip.py::test_echo_tensor_survives_source_free`, `tests/test_interop.py::test_to_arrow_keeps_tensor_storage_alive` — both assert the source `untyped_storage` weakref survives `del`+`gc.collect()` while the result is alive, and values read back correctly. | fixed |
| r5c-12 | MINOR | 8 docs/API faithfulness | `docs/api-tables.md` vs `torchfits/table.py.__all__`, `_table/interop.py` | Doc gaps recorded (docs land at R15): (1) `write_csv`/`write_ipc` are in `table.py.__all__` but have **no docs section** (only `write_parquet` is shown at api-tables.md:482); (2) r5c-04's strict-kwargs contract (unknown / in-memory-detached kwargs raise `TypeError`) needs a line in the Interop section; (3) r5c-03's width-1→scalar mapping and r5c-01's vector-TNULL→MaskedColumn behavior need to_astropy notes. All documented call patterns (api-tables.md:382-482, api.md:65-66) verified to still work post-fix. `_table/__init__.py` exports (private `_acquire_cpp_*` seam) are correctly undocumented — no mismatch. | Recorded; no src change in slice C (docs owned by R15). | deferred |
| r5c-13 | MINOR | 8 docs/compatibility | main decode path note (`_read_scan`/C++ squeeze — outside slice) | Ground-truth divergence re-derived while pinning r5c-03: for `TFORM=1J` + `TDIM=(1)`, astropy `Table.read` keeps shape `(N, 1)` while torchfits maps repeat==1 to scalar `(N,)` on every path (dict/Arrow/astropy). The scalar mapping is the pinned house convention (`tests/test_table_squeeze_and_ref_cache.py`) and matches fitsio's repeat-1 rule; behavior unchanged in slice C. | Recorded as an owed `docs/compatibility.md` note at R15 (astropy TDIM (1) vs torchfits scalar mapping). | deferred |
| r5c-14 | BLOCKER | 1 silent data loss | `_table/arrow_convert.py:_numpy_to_arrow_array`, `_chunk_to_record_batch._tform_for` | Routed from R5-ReadTable (Main's "r5c-14"): `'B'` byte-vector columns decoded as **strings** — silent corruption. Repro: `fits.Column(name="BV", format="8B", array=np.arange(24,dtype=np.uint8).reshape(3,8))` → `torchfits.table.read(path)["BV"]` returned garbage strings `['', '\x08\t\n\x0b\x0c\r\x0e\x0f', …]` (strip/NUL-trim mangled the bytes) while `table.schema(path)` says `fixed_size_list<uint8>[8]` and `read_torch` gives `(3,8) uint8` — data/schema disagreement, bug class 1. Root cause: the 2-D uint8 dispatch sent every non-bit matrix through the char ('A') string/bytes decoder whenever `decode_bytes=True`, and with `decode_bytes=False` gave `fixed_size_binary[8]` — only `A` may take that path. Additionally `column_tforms` is plumbed only when `decode_bytes=True` (`_read_scan.py:324-328`), so `decode_bytes=False` reads carried no TFORM at the dispatch at all. | Dispatch now consults the TFORM code (`_tform_code`): `'X'` → bool list, unknown/`'A'` → strings/bytes (schema-less input keeps the documented `decode_bytes` contract), `'B'`/other numeric → generic `FixedSizeList<uint8>[w]` tail (sentinel masking included) in **both** decode modes; `_tform_for` falls back to `null_meta` (field metadata carries `fits_tform` per `_build_fits_metadata`) so non-decode reads resolve TFORMs too. Schema-vs-data agreement pinned on decode×mmap matrix + `read_torch` agreement + char-column dispatch regression pin. Tests `tests/test_arrow_table_api.py::test_byte_vector_column_is_uint8_fsl`, `::test_chunk_byte_vector_vs_char_dispatch`. Before: `AssertionError: … DataType(string)` (2 failed). After: 2 passed; owned suites 105 passed; oracle `test_output_parity.py` 69 passed. | fixed |
| r5c-15 | CLEANUP | duplication | `_table/utils.py:91` `_normalize_row_slice` (module fn — surviving copy); `_hdu/table_hdu_ref.py:158` `_normalize_row_slice` (method — duplicated copy) | Routed candidate " `_normalize_row_slice` duplicated ×2" re-derived: the two copies are NOT both in one slice's files — `_table/utils.py` is slice B's file (consumers `_read_scan.py:288,390,600`, `mutation.py:309,437` import it) and the `_hdu/table_hdu_ref.py:158` method (used at `:203, :234`) is R6-B's file. Zero-behavior mechanical dedupe cannot land inside slice C's exclusive set. (Renumbered from r5c-14 to r5c-15 to free r5c-14 for the routed '8B' BLOCKER per Main.) | Sketched fix for R6-B: delete the `table_hdu_ref.py:158` method and call `torchfits._table.utils._normalize_row_slice(row_slice)` at `:203/:234` (identical semantics; the module copy is the surviving one). Behavior pinned by existing consumers (`tests/test_table_squeeze_and_ref_cache.py`, row-slice tests in `tests/test_arrow_table_api.py`). | deferred: routed to R6 (CLEANUP) |

## Evidence summary (before → after)

Failing-first run 1 (`pixi run pytest tests/test_interop.py tests/test_dlpack_roundtrip.py tests/test_table_squeeze_and_ref_cache.py -q`, unfixed):
```
FAILED tests/test_interop.py::test_to_astropy_vector_tnull_matches_astropy - AssertionError: expected MaskedColumn, got Column
FAILED tests/test_interop.py::test_to_astropy_arrow_fsl_nulls_masked_not_object - AssertionError: expected MaskedColumn, got Column
FAILED tests/test_interop.py::test_table_to_astropy_accepts_pathlike - TypeError: 'PosixPath' object is not iterable
FAILED tests/test_interop.py::test_interop_pathlike_data_inputs - TypeError: 'PosixPath' object is not iterable
FAILED tests/test_interop.py::test_to_astropy_rejects_unknown_kwargs - Failed: DID NOT RAISE TypeError
FAILED tests/test_interop.py::test_to_astropy_empty_reader - ValueError: Must pass schema, or at least one RecordBatch
FAILED tests/test_interop.py::test_stream_write_empty_source_creates_file - AssertionError: assert False (o.csv exists)
FAILED tests/test_interop.py::test_to_pandas_empty_reader_keeps_columns - AssertionError: assert [] == ['A']
FAILED tests/test_interop.py::test_to_astropy_meta_ioerror_propagates - Failed: DID NOT RAISE OSError
9 failed, 20 passed, 1 xfailed
```
Failing-first run 2 (`pixi run pytest tests/test_arrow_table_api.py -q -k "repeat1 or width1 or chunk_repeat"`, unfixed):
```
FAILED tests/test_arrow_table_api.py::test_chunk_repeat1_vector_surfaces_scalar - assert fixed_size_list<item: int32>[1] == int32
FAILED tests/test_arrow_table_api.py::test_chunk_repeat1_tensor_surfaces_scalar - (same, tensor route)
FAILED tests/test_arrow_table_api.py::test_chunk_repeat1_bit_surfaces_scalar_bool - assert fixed_size_list<item: bool>[1] == bool
FAILED tests/test_arrow_table_api.py::test_width1_fixed_list_to_astropy_is_scalar - assert (3, 1) == (3,)
FAILED tests/test_arrow_table_api.py::test_repeat1_round_trip_agrees_with_astropy - assert (3, 1) == (3,)
5 failed
```
Post-fix (`pixi run pytest tests/test_interop.py tests/test_arrow_table_api.py tests/test_table_squeeze_and_ref_cache.py tests/test_dlpack_roundtrip.py -q`):
```
105 passed, 1 skipped, 1 xfailed, 2 warnings in 2.60s
```
r5c-14 failing-first (`pixi run pytest tests/test_arrow_table_api.py -q -k "byte_vector or byte_dispatch"`, unfixed):
```
FAILED tests/test_arrow_table_api.py::test_byte_vector_column_is_uint8_fsl - AssertionError: (True, True, DataType(string))
FAILED tests/test_arrow_table_api.py::test_chunk_byte_vector_vs_char_dispatch - AssertionError: (True, DataType(string))
2 failed
```
r5c-14 post-fix: `2 passed` (same command); full owned suites `105 passed, 1 skipped, 1 xfailed`; oracle `tests/test_output_parity.py`: `69 passed`.
Oracle (`pixi run pytest tests/test_output_parity.py -q`): `69 passed in 2.09s`.
Neighbor seams (`tests/test_interop_import.py tests/test_stream_table_and_cache.py tests/test_where_and_batch_errors.py`): `22 passed`.

Note: two new-test assertions were corrected mid-run to match pyarrow's CSV header quoting (`"A"` vs `A`) — a test-expectation fix only (writer formatting is pyarrow's contract).

## Deferred (one line each)

- r5c-09: `_hdu/table_hdu_ref.py:306` `to_arrow(columns=…)` → `TypeError: read() got multiple values for keyword argument 'columns'`; reject-collision contract + docstring in R6-B; strict-xfail pin `tests/test_table_squeeze_and_ref_cache.py::test_tablehduref_to_arrow_columns_kwarg` flips red-on-XPASS until then. (** ROUTED TO R6 — r5c-09 **)
- r5c-12: `docs/api-tables.md` needs `write_csv`/`write_ipc` sections + strict-kwargs / width-1-scalar / vector-TNULL→MaskedColumn notes (R15).
- r5c-13: `docs/compatibility.md` needs the astropy `TDIM=(1)` `(N,1)` vs torchfits scalar-mapping note (R15).
- r5c-15: `_hdu/table_hdu_ref.py:158` `_normalize_row_slice` method duplicates `_table/utils.py:91` (surviving copy); R6-B deletes the method and imports the module fn at `:203/:234` — zero-behavior dedupe pinned by existing consumers.

## Per-file disposition

| file | depth | finding IDs | status |
|---|---|---|---|
| `src/torchfits/_table/interop.py` | full line-by-line review + fix | r5c-01, r5c-02, r5c-03 (consume), r5c-04, r5c-05, r5c-06, r5c-07, r5c-08, r5c-12 | fixed (r5c-12 deferred to R15) |
| `src/torchfits/_table/arrow_convert.py` | full line-by-line review + fix | r5c-03 (produce), r5c-08, r5c-14 (routed from R5-ReadTable) | fixed |
| `src/torchfits/_table/cache.py` | full review + repo-wide caller grep | r5c-10 | fixed (zero-change: live seam, docs honest) |
| `src/torchfits/_table/__init__.py` | full review + docs cross-check | r5c-10 (re-export seam), r5c-12 | fixed / r5c-12 deferred |
