# R11 chunk 02 — `tests/` hygiene (`test_header_value_typing.py` … `test_pathlike_acceptance.py`)

IDs `r11c-NN`. Test-suite hygiene only. No `src/` edits. Wave A at `775ec7f`.
Oracle `tests/test_output_parity.py`: bitwise / IEEE / dtype / numeric pins were not deleted or relaxed (diff empty).

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r11c-01 | MINOR | perf-marker | `tests/test_integration.py:TestPerformanceIntegration.test_memory_efficiency_large_file` | The test's pass/fail is an RSS bound (`memory_increase < 3 * file_size` via `psutil.Process().memory_info().rss`). It had no `@pytest.mark.performance`. Sibling methods in the same class (`test_large_image_10k`, `test_large_table_1M_rows`, `test_gpu_memory_transfer`) assert shape/dtype/values only; `test_header_versioning.py:test_header_remove_all_many_history_is_linear` mentions delete time in the docstring and does not time the call. Those stay unmarked. The 3× bound was not changed (comment still says "2x"; the assert is the pin). | Marker only. `pixi run pytest tests/test_integration.py -m performance --collect-only -q` → `tests/test_integration.py::TestPerformanceIntegration::test_memory_efficiency_large_file` / `1/8 tests collected (7 deselected)`. Full chunk run includes it and passes. | fixed |
| r11c-02 | MINOR | fixture isolation | `tests/test_patch_bench_docs.py:_run_dir` | Fixture CSVs were written to the machine-global `Path("/tmp/pdoc_test") / name`. `test_cuda_run_labels_cuda` and `test_real_mps_device_still_labels_mps` both use `exhaustive_cpu_20260801_000000`; `test_mps_named_run_without_device_is_cpu` and `test_cpu_host_token_wins_over_mps_tag` both use `exhaustive_mps_20260801_000000`. Parallel workers clobber one `results.csv`. Not `tmp_path` and not `TemporaryDirectory`. | `_run_dir(base, name, ...)` writes `base / name` with `base=tmp_path` (one directory per test). Label assertions unchanged (`_host_label` reads CSV columns only). `pixi run pytest tests/test_patch_bench_docs.py -q` → `4 passed`. | fixed |
| r11c-03 | MINOR | cwd path | `tests/test_longstr_and_scaled_tables.py:test_vendored_cfitsio_has_niobuf_mindirect_guards` | `Path("extern/cfitsio/fitsio.h")` and `fitsio2.h` are process-cwd relative. From a non-repo cwd the headers exist in the tree but the test false-skips and never checks the `#ifndef NIOBUF` / `#ifndef MINDIRECT` guards. Before, `cd /tmp && pixi run pytest …::test_vendored_cfitsio_has_niobuf_mindirect_guards -rs`: `SKIPPED …:254: vendored CFITSIO not materialized`. | Paths are `_REPO_ROOT / "extern/cfitsio/…"` (`Path(__file__).resolve().parents[1]`). Guard text asserts unchanged. After, the same `/tmp` invocation: `1 passed`. From the repo: included in `5 passed` with `test_patch_bench_docs.py`. | fixed |
| r11c-04 | CLEANUP | impl-pin | `tests/test_longstr_and_scaled_tables.py:test_scale_on_device_dead_helpers_removed` | The whole test was `assert not hasattr(rp, "_apply_scale_on_device")` and the same for `_apply_unsigned_offset`. A rename or reintroduction of a private helper breaks it with no user-visible change. Live scale behavior is already asserted by `tests/test_scale_on_device.py` (`test_read_int8_fits_matches_fitsio`, `test_read_uint16_fits_native_dtype_accelerator`). | Deleted the test. `--collect-only -q` on the file lists `test_vendored_cfitsio_has_niobuf_mindirect_guards` and does not list `test_scale_on_device_dead_helpers_removed`. Chunk pytest green. | fixed |
| r11c-05 | CLEANUP | impl-pin | `tests/test_integration.py:TestRealDataIntegration.test_large_table_1M_rows` | `assert hasattr(table_hdu, "materialize")` pins a method name and does not call it. Column length and `head(1000)` in the same test, and `materialize()` in `test_multi_extension_file` (`assert table.num_rows == 10000`), already cover the behavior. | Removed the hasattr (and its "lazy access" comment). Remaining asserts unchanged. Covered by the chunk pytest. | fixed |

## Evidence

Marker collect (`pixi run pytest tests/test_integration.py -m performance --collect-only -q`):

```
tests/test_integration.py::TestPerformanceIntegration::test_memory_efficiency_large_file

1/8 tests collected (7 deselected) in 1.33s
```

Vendored-header guard, before (cwd `/tmp`):

```
SKIPPED [1] …/tests/test_longstr_and_scaled_tables.py:254: vendored CFITSIO not materialized
1 skipped in 1.32s
```

After the repo-root anchor, same command from `/tmp`: `1 passed`.

Chunk (`pixi run pytest` on all 19 files, `-q --tb=line`):

```
257 passed, 13 skipped, 4 warnings in 34.86s
```

Skips: `test_gpu_memory_transfer` (no CUDA/MPS on this host) and the 12 `@mps_required` tests in `test_mps.py`. Warnings are pre-existing (`ReadOptions.handle_cache_capacity` deprecation on the mocked batch path; `get_header` fast-path fallback on the two malformed-magic tests). Not changed.

`git diff -- tests/test_output_parity.py` is empty.

## Kept on purpose (not defects)

- `test_output_parity.py`: every bitwise / `assert_array_equal` / dtype / IEEE / fuzz pin left as written, including `_assert_exact` and the macOS CompImage `atol` of one dtype eps. No edit.
- `test_header_versioning.py` `_version` sequence (including no-op `setdefault` still bumps): mutation-counter contract used for cache invalidation (R6). Not an incidental wording pin.
- `test_io.py` `mock.patch("torchfits.io.cpp")`: documented seam in `io.py` (`_LazyNativeModule`, mock.patch of `torchfits.io.cpp` must keep working). This is not the removed `torchfits.cpp` alias. Dispatch asserts kept.
- Security / exception matches (`test_http_probe_fixture.py` private-network text, uint64 `ValueError`, ImportError strings, device / quantize / truncation messages, `cpp-seal-all` `HAS_BZIP2` in `test_io_invariants.py`): R1–R10 contract pins. Not deleted.
- No `time.sleep` in the chunk. No scheduling-race sleep to replace.
- `NamedTemporaryFile` in `test_integration.py` / `test_interop.py` uses the system temp dir and is unlinked. Left in place.
- `test_header_remove_all_many_history_is_linear`: 20_000 HISTORY cards, asserts the surviving card list only. Unmarked.

## Per-file disposition

| File | Disposition |
|---|---|
| `tests/test_header_value_typing.py` | clean — dtype/type pins (incl. A-01 numpy scalars) kept; writes already under `tmp_path` |
| `tests/test_header_versioning.py` | clean — `_version` and HISTORY/COMMENT contracts kept; linear-delete test does not assert wall-clock, unmarked |
| `tests/test_http_probe_fixture.py` | clean — SSRF rejection pins kept |
| `tests/test_integration.py` | fixed r11c-01, r11c-05 |
| `tests/test_interop.py` | clean — numeric/null/pathlike contracts; temp files already unlinked |
| `tests/test_interop_import.py` | clean — ImportError contract pins |
| `tests/test_io.py` | clean — `torchfits.io.cpp` mock seam kept (not the deprecated `torchfits.cpp` alias) |
| `tests/test_io_invariants.py` | clean — where/quantize/BLANK/IEEE/CLI pins kept; no timer |
| `tests/test_longstr_and_scaled_tables.py` | fixed r11c-03, r11c-04 |
| `tests/test_malformed_fits.py` | clean — truncation must raise; no cwd writes |
| `tests/test_mps.py` | clean — MPS body skipped on this host; CPU downcast warnings are contract pins, not RSS/time |
| `tests/test_multichunk_buffered_read.py` | clean — astropy equality on a >16 MiB table; `tmp_path_factory`; no timer |
| `tests/test_native_stub.py` | clean — stub drift and runtime return types; fixtures under `tmp_path_factory` |
| `tests/test_no_external_fits_backends.py` | clean — package import scan via `__file__` |
| `tests/test_open_table_reader.py` | clean — reader vs `read_torch` equality; `tmp_path` |
| `tests/test_output_parity.py` | clean — oracle pins not relaxed (no diff) |
| `tests/test_package_isolation.py` | clean — packaging/ABI/import contracts via `__file__`; no cwd fixture writes |
| `tests/test_patch_bench_docs.py` | fixed r11c-02 |
| `tests/test_pathlike_acceptance.py` | clean — Path vs str result equality; `tmp_path` |

Oracle pins in `tests/test_output_parity.py` were not relaxed.
