# R11-04 — tests chunk 04 hygiene

Owned files: `splits/tests.chunk04` (19 files, `tests/test_table_filtering.py` through `tests/transforms_reference.pyi`). No `src/` edits. `tests/transforms_reference.py` and `tests/transforms_reference.pyi` were not regenerated or hand-edited.

Performance markers: **none**. `test_table_head.py` has no timer (`"monotonically"` in `test_tablehdu_head_composes` is prose). `test_torch_boundary.py` stores `time.perf_counter()` on `Probe.elapsed_ms` and never asserts it; pass/fail is `torch_loaded` only. No `time.sleep` in the chunk. Where-clause, write-identity, quantize, and transform-state pins were not weakened.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r11e-01 | MINOR | test hygiene | `tests/test_validation.py:test_validation` | Fixture path was the relative name `test_valid.fits`, created in the process cwd and removed in `finally`. A failure before `finally` leaves the file in the repo. | Write `tmp_path / "test_valid.fits"`. `HDUList.fromfile` / `validate()` assertions unchanged. | fixed |
| r11e-02 | MINOR | test hygiene | `tests/test_writing.py:test_writing`, `test_table_write_bool_preserved`, `test_write_accepts_list_table_dict`, `test_table_write_rich_types_roundtrip`, `test_table_write_complex_tensor_roundtrip`, `test_write_compressed_image_roundtrip`, `test_write_compressed_hdulist_mixed`, `test_write_compressed_hdulist_images_roundtrip`, `test_write_compressed_image_tuple_roundtrip`, `test_compressed_write_rejects_dict_hdu_with_non_tensor_image_payload`, `test_write_cuda_tensor_copies_to_host_before_fits_write`, `test_write_mps_tensor_copies_to_host_before_fits_write` | Twelve tests assigned a relative `*.fits` name (`test_write.fits`, `test_write_hdulist.fits`, and the ten other `test_write_*.fits` literals) and unlinked it in `finally`. A failure before `finally` leaves the file in the repo cwd. | Each test takes `tmp_path` and writes `tmp_path / <same name>`. Payload, header, dtype, compression, HDU-count, and error-message assertions unchanged. Tests that already used `tmp_path` were left alone. | fixed |
| r11e-03 | MINOR | test hygiene | `tests/test_transforms.py:TestCompose.test_repr`, `TestEdgeCases.test_repr_methods`, `TestSigmaClip.test_repr`, `TestFITSHeaderNormalize.test_repr`, `TestGlobalScalarNorm.test_repr`, `TestInterquantileScale.test_repr`, `TestAsymmetricSigmaClip.test_repr`, `TestFITSScaleColumns.test_repr`, `TestTNullToNan.test_repr`; `tests/test_transforms_state.py:TestMeshBackgroundSubtract.test_repr`, `TestAffineTransform.test_repr`, `TestClipPayloads.test_asymmetric_clip_repr_includes_fill` | These twelve tests only asserted `__repr__` substrings (`"Compose" in rep`, `"bitpix=-32"`, `"q_low=0.1"`, `"fill='nan'"`, `"(3, 3)"`, class name in `repr`, and the same pattern for the other classes). A repr refactor that keeps behavior would fail them. Forward/inverse, dtype, payload, and error-type tests in the same classes already cover that behavior. | Deleted those methods only. Exception `match=` pins (where, quantize, write identity, `DataStateError`, `prior forward`) were not touched. | fixed |

## Marker collect-only

`pixi run pytest tests/test_table_head.py tests/test_torch_boundary.py tests/test_transforms.py tests/test_transforms_state.py tests/test_validation.py tests/test_writing.py -m performance --collect-only -q`

```
no tests collected (501 deselected) in 1.43s
```

pytest exit 5 (nothing selected). No `@pytest.mark.performance` was added.

## Not changed (reviewed)

- `test_torch_boundary.py` `_PROBE_TEMPLATE` times the child for the `PROBE` line. No test compares `elapsed_ms` to a bound, so it is not a performance test.
- `test_transforms_e2e.py` uses `tempfile.NamedTemporaryFile` (system temp, not repo cwd) and unlinks in `finally`. Left in place.
- `test_write_read_identity.py` already uses `TemporaryDirectory` / `tmp_path`.
- `test_table_head.py` / `test_table_squeeze_and_ref_cache.py` `source_path="dummy.fits"` is an in-memory label for slice math, not a file create.
- `test_where_and_batch_errors.py` `"a.fits"` values are mock arguments. Real files already use `tmp_path`. The `strict=True` pin (`"read_batch: failed to read" not in str(strict_err.value)`) stays.
- No `time.sleep` in the chunk.
- No product bug was papered over. Numeric tables in `transforms_reference.py` / `.pyi` were not edited.

## Chunk pytest

`pixi run pytest` on the 17 owned `test_*.py` modules, `-q --tb=line`: **653 passed, 2 skipped, 21 xfailed, 16 warnings in 15.38s**.

The two skips are the CUDA and MPS host-copy guards in `test_writing.py`. The 21 xfails are the strict torch-boundary phase markers in `test_torch_boundary.py`.

## Disposition

| File | Disposition |
|---|---|
| `tests/test_table_filtering.py` | clean; fixtures already under `tmp_path`; filter pins kept |
| `tests/test_table_head.py` | clean; no wall-clock or RSS assertion |
| `tests/test_table_squeeze_and_ref_cache.py` | clean; writes already under `tmp_path` |
| `tests/test_tablehdu_schema_cache.py` | clean |
| `tests/test_torch_boundary.py` | clean; timer does not decide pass/fail; xfail pins kept |
| `tests/test_transforms.py` | fixed (r11e-03): repr-only methods deleted |
| `tests/test_transforms_e2e.py` | clean; `NamedTemporaryFile`; quantize/round-trip pins kept |
| `tests/test_transforms_state.py` | fixed (r11e-03): repr-only methods deleted; state/scale/quantize pins kept |
| `tests/test_transforms_typing.py` | clean |
| `tests/test_truncated_table_errors.py` | clean; `tmp_path` already |
| `tests/test_upstream_parity_inventory.py` | clean; reads repo manifest paths, no fixture create |
| `tests/test_validation.py` | fixed (r11e-01): fixture moved under `tmp_path` |
| `tests/test_where.py` | clean; where-clause message pins kept |
| `tests/test_where_and_batch_errors.py` | clean; real files under `tmp_path` |
| `tests/test_write_fidelity.py` | clean; `tmp_path` already; quantize and write-identity pins kept |
| `tests/test_write_read_identity.py` | clean; `TemporaryDirectory` / `tmp_path` already |
| `tests/test_writing.py` | fixed (r11e-02): twelve cwd fixtures moved under `tmp_path` |
| `tests/transforms_reference.py` | clean; reference module, not edited |
| `tests/transforms_reference.pyi` | clean; stub, not edited |
