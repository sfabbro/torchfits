# R8 slice B — `table_types.h`, `table_ops.h`, `table_ops.cpp`, `table_bindings.cpp`

Round R8 (cpp_src TABLE family), slice B. Review+fix one-pass against the 1.2.0 rubric.
Evidence is PYTHON-level through the bindings (`torchfits._C`), failing-first against the
then-currently-built extension (pre-fix run captured 7 failures), then rebuild (both pixi
envs) and passing run. Finding prefix `r8b`.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|----|-----|-------|---------------|--------------------------|------------------|--------|
| r8b-01 | BLOCKER | 1 silent data loss (write) | `table_ops.cpp:append_rows` (TBIT branch) | `append_rows` wrote a BIT payload as ONE flat `fits_write_col(TBIT,…, num_rows*repeat, …)` run across rows. `fits_write_col(TBIT)` maps flat runs onto raw data-unit bits and IGNORES per-row byte padding when `repeat % 8 != 0` (semantics documented in `populate_rows`, verified vs astropy), so every multi-row append of a `12X`-style column shifted bits across rows: silent corruption of all appended rows. `append_rows` also accepted partial-width BIT payloads (silently zero-padded) while every other fixed-width path (incl. `populate_rows` TBIT) rejects them. Repro: `tests/test_dlpack_roundtrip.py::test_append_rows_unaligned_bit_repeat_roundtrip` — append 3 rows of (3,12) bools to a `12X` table whose 5 fixture rows carry a non-periodic bit pattern; all 8 rows must read back exactly. (Evidence methodology: the first pre-fix capture used a flawed fixture — zero fixture rows but a full-pattern expectation — so it failed for two reasons at once; the test was corrected to patterned fixture rows and the failing-first re-captured cleanly by temporarily reverting only the TBIT write hunk and rebuilding, then re-applying the fix and rebuilding.) | Wrote each row with its own `fits_write_col` call (mirrors `populate_rows`/`write_table_hdu`) + exact-repeat-width check (`repeat > 0 && user_repeat != repeat` → throw, parity with `populate_rows`). Clean capture (pre-fix TBIT hunk, corrected fixture): `Mismatched elements: 13 / 96 (13.5%)`, first mismatches at `[6, 0], [6, 2], [6, 3], [6, 5], [6, 6]` — corruption confined to the appended rows (bit-shifted by the flat run), fixture rows exact; post-fix all 8 rows exact. Companion pin `test_update_rows_unaligned_bit_repeat_roundtrip` (already-green path) added. | fixed |
| r8b-02 | MAJOR | 3 lifetime / 4 concurrency (R7-CPP2) | `table_bindings.cpp:ThreadLocalReaderCache`, `g_reader_cache`, `shared_meta_generation` | The thread-local reader cache survived shared-meta invalidation: `clear_cache()`/`clear_all_caches()`/`clear_shared_read_meta_cache()` (the documented escape hatch for stale in-process state) erase `SharedReadMeta` but nothing told `g_reader_cache`, so a cached `TableReader` kept decoding with its old column layout/bytes even after the file content was replaced and the caches were told. Deterministic repro (content swap invisible to stat so only explicit invalidation can catch it): open A (`A:J,B:E`), read `A` → int32 [1,2,3]; overwrite in place with same-length B (`A:E,B:J`) + `os.utime` restore; `torchfits.cache.clear_cache()`; read `A`. Pre-fix: `tensor([1,2,3], dtype=torch.int32)` (STALE) instead of float32 [7.5,8.5,9.5]. | uid-bump eviction on invalidation: cache entries are stamped with the path's `SharedReadMeta::uid` (minted anew on every invalidation — erase+recreate — and on stat-check rotation); `acquire` drops handles whose stamp ≠ current generation (kept the existing `file_unchanged()` stat gate as the out-of-band half). `AcquiredReader` carries the acquire-time generation so an invalidation landing mid-read still drops the handle on the next acquire. Tests: `test_reader_cache_freshness.py::test_clear_cache_reaches_thread_local_reader_cache`, `test_public_read_refetches_after_clear_cache` (public contract), `test_shared_meta_staleness.py::test_reader_cache_generation_rotates_on_shared_meta_invalidation` (raw binding), `test_concurrent_same_file_read.py::test_invalidation_reaches_every_threads_reader_cache` (cross-thread, 4 caches). All FAIL pre-fix with stale `int32 [1,2,3]`, PASS post-fix. Side benefit (no fits_bindings.cpp edit needed): `write_fits_file`/`write_hdus*` paths that call `invalidate_shared_meta` without `evict_cached_reader` are now covered by the generation check too. | fixed |
| r8b-03 | MAJOR | 3 lifetime (resource leak) / 4 cache | `table_bindings.cpp:ThreadLocalReaderCache::acquire/release` | `acquire` spliced the entry's LRU node to the back and erased only the map entry, leaving the node as a ghost: one leaked `std::list` node + `ReaderCacheKey` string per CACHED READ (hot path, unbounded), `lru_` no longer matched `map_`, `kCapacity` eviction popped ghost keys (no-op erase → `map_` could exceed capacity; or `map_.erase(ghost_key)` destroyed a LIVE entry through its stale key). Repro: `tests/test_reader_cache_freshness.py::test_reader_cache_does_not_leak_per_cached_read` — glibc `mallinfo2().uordblks` slope over 20 000 cached reads of one small table; pre-fix: `reader cache leaked 3520128 live malloc bytes over 20000 cached reads` (assert `< 512 KiB`). | `acquire` now erases the consumed entry's LRU node (`release` re-adds on return) — `lru_` 1:1 with `map_` invariant restored; capacity eviction pops real keys only. Test passes post-fix (slope ≈ 0). Capacity-corruption aspect is the same root cause and same fix (code proof; no separate pinnable observable). | fixed |
| r8b-04 | MAJOR | 1 type fallthrough on write / 5 error contract | `table_ops.cpp:write_fits_table` | Unknown `table_type` strings were silently treated as `"binary"` (only `"ascii"` was recognized) — a typo at the binding (`_C.write_fits_table`, typed `table_type: str` in `_C.pyi`) silently produced the wrong table flavor. Python's `_table/write.py` pre-validates, but the binding is an exported surface. Repro: `tests/test_dlpack_roundtrip.py::test_write_table_unknown_type_raises` — `write_fits_table(..., "asccii")`; pre-fix `Failed: DID NOT RAISE ValueError`. | `else if (kind != "binary") throw std::invalid_argument("Unknown table_type '…' (expected 'binary' or 'ascii')")` (nanobind maps to `ValueError`; the existing `catch(...)` closes the handle). Accepted spellings remain case-insensitive (`binary`/`ascii`/`Binary`/`ASCII` all pinned green). All in-tree callers pass exactly `"binary"`/`"ascii"` (grepped `write_api.py`, `_table/write.py`, `_table/mutation.py`). | fixed |
| r8b-05 | — (verify) | 1 silent data loss candidate (strided DLPack writes) | `table_reader.h:update_rows_mmap` (slice A's file; test in slice B's `test_dlpack_roundtrip.py`) | Round-named candidate: strided DLPack numeric payloads silently miswritten by `update_rows_mmap` (bool/uint8/string honored strides, numerics indexed flat). **Re-derived FALSE at HEAD**: fixed by `944f83b` — `idx = i*tensor.stride(0) + j*tensor.stride(1)` (element strides) is computed once per element and used by BYTE/SHORT/INT/LONG/FLOAT/DOUBLE/COMPLEX cases; LOGICAL/BIT/STRING use the matching stride-honoring `byte_offset`. | No fix needed. Regression pins added in `tests/test_dlpack_roundtrip.py::test_strided_update_rows_buffered_roundtrip` + `::test_strided_append_rows_roundtrip` (buffered `populate_rows` path, `t[::2]` stride-2 views round-trip exactly; green pre- and post-fix). mmap-side strided contract pinned by slice A (`tests/test_strided_update_rows.py`). | fixed |
| r8b-06 | — (verify) | 3/4 candidate (cache staleness) | `table_bindings.cpp` cache acquire; `table_reader.h:file_unchanged` | Round-named repro "open file A, read; replace with file B same path; read again — stale data" is **already guarded at HEAD** by the `file_unchanged()` stat-identity check (dev/ino/size/mtime-ns) on cache acquire (commit `7e3b1ed`, "process-wide reader eviction and stale-cache guard"). The candidate's precise claim — "stale AFTER shared-META INVALIDATION" — was real and is r8b-02. | No fix needed beyond r8b-02. Regression pins: `tests/test_reader_cache_freshness.py::test_read_sees_os_replaced_file[buffered\|mmap\|numpy\|full]` (green pre- and post-fix). | fixed |
| r8b-07 | — (verify) | 7 edge (VLA zero-length) | `table_ops.cpp:populate_rows`, `append_rows` (VLA branches) | Zero-length VLA cells (`np.array([])` rows) through `append_rows`/`update_rows` route to `fits_write_col(..., nelem=0, nullptr)` and round-trip as empty rows; empty-range `num_rows <= 0` mutations no-op without corrupting state. | No fix needed; pinned: `tests/test_dlpack_roundtrip.py::test_zero_length_vla_rows_roundtrip` (append + update, astropy ground truth `[[1,2],[],[],[]]`; green pre- and post-fix). | fixed |
| r8b-08 | MINOR | perf 3 (GIL-held blocking work) | `table_bindings.cpp:bind_table` write lambdas (`write_fits_table`, `append/insert/update/update_mmap/rename/drop/delete_fits_table_rows`), `TableReader.__init__`, `open_fits_mmap_reader` | Every table WRITE binding and the reader constructors run their CFITSIO open/write work with the GIL held (all READ bindings release it — verified), so a large table write blocks every Python thread (e.g. DataLoader workers) for its whole duration. | Deferred: the honest fix is a payload-materialization split (extract tensors with GIL, run CFITSIO IO GIL-free) — structural, well over the ~30 LOC MINOR budget, and no benchmark lane is allowed this round to size the win. Reads (the hot path) are already GIL-free. | deferred |
| r8b-09 | MINOR | 5 error contract (message masking) | `table_ops.cpp:populate_rows`, `append_rows` column loops | After a failed `fits_write_col` the loop keeps iterating with a poisoned CFITSIO `status` (CFITSIO calls return immediately when `*status != 0`), so the next `fits_get_colnum` fails too and the caller sees a misleading `"Column not found for update_rows: <later column>"` instead of the real CFITSIO error. A typed `RuntimeError` is still raised (no silent wrong result); only the diagnosis is wrong. | Deferred: per-op status checks with `fits_get_errstatus` text across the write loops exceed the MINOR LOC budget together, and repo test policy forbids pinning error wording. | deferred |
| r8b-10 | MINOR | 5/7 edge (filter value coercion) | `table_bindings.cpp:read_fits_table_filtered` | Filter values accept Python `float`/`int` instances only; `np.float64` works (float subclass) but `np.int64`/`np.int32` raise `"Unsupported filter value type"` (numpy ints are not `int` subclasses). The in-tree fallback callers pass grammar-parsed Python scalars, so the public `where=` path is unaffected. | Deferred: widening the coercion (`PyNumber_Index`/`PyNumber_Float` fallback) is cheap but needs its own dispatch-policy decision; no in-tree caller is affected today. | deferred |

## std::cerr diagnostics (assignment item 3)

`grep -n "std::cerr" src/torchfits/cpp_src/*` at HEAD: the only sites are `table_reader.h:494`
and `:521-523` — slice A's file (folded into the exception messages by slice A this round).
**Zero `std::cerr` sites exist in the four slice B files** — nothing to change here.

## Verify-only anchors (confirmed in the surrounding area — not re-fixed)

- TFORM repeat int32 truncation guard — `table_reader.h:293-297` present, and the sibling
  ASCII-width guard (`width_long` → `int`, same class) sits beside it (`table_reader.h:288-295`).
- `ensure_extent_within_file` — called before the mmap update extent (`table_reader.h:1939`
  area, inside `update_rows_mmap`, read during the stride re-derivation) and at the mmap read
  opens (`:1020`, `:1281` area). Present.
- GIL release around table reads — verified across `table_bindings.cpp`: `gil_scoped_release`
  wraps every read binding (`read_fits_table*`, `TableReader.read_rows*`,
  `read_fits_table_rows_mmap_from_reader`, `read_fits_table_filtered` read phase), with
  `gil_scoped_acquire` before result wrapping.

## Rebuild + validation protocol

- Failing-first captured against the previously built extension (no rebuild): 7 failures
  (r8b-01 pre-fix BIT corruption assert, r8b-02 ×3 stale-decode asserts incl. the concurrent
  `[[1,2,3],…]×4`, r8b-03 leak `3520128 bytes / 20000 reads`, r8b-04 `DID NOT RAISE`).
- Rebuild serialized through Main's mutex (`rebuild-start R8-TableOpsBind` … `rebuild-done`),
  `pixi run dev` + `pixi run -e test -- pip install -e . --no-build-isolation` (both envs;
  the build compiles slice A's landed `table_reader.h` changes too — combined tree green).
- Targeted runs (post-fix): `pixi run pytest tests/test_reader_cache_freshness.py
  tests/test_shared_meta_staleness.py tests/test_concurrent_same_file_read.py
  tests/test_dlpack_roundtrip.py tests/test_output_parity.py
  tests/test_truncated_table_errors.py tests/test_table_file_ops.py
  tests/test_astropy_upstream_smoke.py tests/test_fitsio_upstream_smoke.py -q`
  → **149 passed, 2 warnings** (the 2 warnings are the pinned R4
  `ReadOptions.handle_cache_capacity` deprecation, unrelated). This covers the
  four owned files, the shared oracle `tests/test_output_parity.py` (read-only,
  never edited), and the table mutation/write suites that exercise `append_rows`
  TBIT, `write_fits_table` `table_type=`, and the cache paths end to end.

## Per-file disposition

| File | Review outcome | Changed? |
|------|----------------|----------|
| `table_types.h` | Layout/packing sane (no pragma packing needed; `long byte_offset`/`storage_bytes` are CFITSIO-native widths, `int64_t unsigned_offset` correct). `ColumnInfo.repeat`/`width` int truncation guarded at parse (table_reader.h guards, verify-only above). `table_buffered_read_enabled()` env parse correct (`0/n/N/f/F` off, else on). No moved-from use; VLA-zero handled at the ops layer (r8b-07). No defects. | unchanged |
| `table_ops.h` | Declarations match definitions (`write_fits_table`, `infer_num_rows_from_payload`, `append/insert/delete/update/update_mmap_rows`, `rename_columns`, `drop_columns`); no stale prototypes, no moved-from surface. No defects. | unchanged |
| `table_ops.cpp` | Column ops, scale/TNULL math (TSCAL/TZERO handled in the reader; write paths are raw-value with the existing dtype↔TFORM map — no type fallthrough left after r8b-04), overflow guards present (`uint64_t` element-count overflow checks before `num_rows * repeat_vals`, `SIZE_MAX` BIT buffer guard, `arr.size() > LONG_MAX` VLA guard). RAII/error paths verified: `close_file_ignore_status` on every throw path (fresh status, no handle leak), `rollback_inserted_rows` closes before throw, `insert_rows` nulls `fptr` after close (no double-close). Moved-from: `padded_str.push_back(std::move(row))` is last use of `row` — safe. | `write_fits_table` (r8b-04), `append_rows` TBIT branch (r8b-01) |
| `table_bindings.cpp` | GIL coverage (reads verified above; writes = r8b-08), argument coercion (`long` row/col indices, `int hdu_num`; nanobind int casters reject out-of-range Python ints — no silent int32 truncation), error mapping (C++ exceptions propagate typed — `std::runtime_error`→`RuntimeError`, `std::invalid_argument`→`ValueError`; nothing swallowed in this file). Cache bookkeeping defective (r8b-02, r8b-03 — both fixed). `read_fits_table_filtered` value coercion gap = r8b-10. Moved-from use: `std::move(handle)`/`std::move(slot.reader)` are all terminal uses — safe. | cache struct + 3 cached read bindings (r8b-02, r8b-03) |

## Owed doc notes (for R15)

- None user-visible: the fix makes `torchfits.cache.clear_all_caches()`'s existing docstring
  claim ("After this call every subsequent read re-fetches / re-opens") TRUE for the
  thread-local table reader cache — it was false before. No doc edit required.
- Env knobs observed in slice B's area (R15 inventory): `TORCHFITS_TABLE_BUFFERED`
  (`table_types.h`, default on) and `TORCHFITS_VLA_HEAP_PREAD` (`table_reader.h`, default off,
  slice A's file).
