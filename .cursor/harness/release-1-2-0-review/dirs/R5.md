# R5 — `src/torchfits/_table/` + `src/torchfits/_table_engine/`

Slices: A `dirs/R5-read.md` (r5a-*), B `dirs/R5-write.md` (r5b-*), C `dirs/R5-interop.md` (r5c-*).

## Totals

37 findings (r5a 16, r5b 8, r5c 15): 30 fixed in-round, 7 deferred/routed.

Blockers fixed (6): r5a-01 rows=/where selections silently ignored (scatter failure returned the
FULL table; backend=torch + raw-decode dropped selections — now IndexError naming the row, all
backends honor selection); r5b-01 `table.write(quantize=)` silently no-op'd (now typed
`QuantizeError`); r5b-04 malformed NAXIS2 coerced to 0 rows → `delete_rows` silent no-op (data
loss — `_naxis2_row_count` raises); r5c-01 vector TNULL silently became float64 NaN Columns
(mask dropped, dtype widened — MaskedColumn + typed fills per astropy ground truth); r5c-14 '8B'
byte-vector columns decoded as garbage strings (routed from slice A; TFORM-aware dispatch —
'FSL' codes to fsl<uint8>[w], only 'A' char matrices take the string path).

Majors (17): r5a-02..r5a-08 (empty-result/schema dtype divergence; empty reads dropping unknown
columns; unnamed TTYPE-less columns; read-path except swallows; A-14 `_read_ranges_as_chunk`
fabricated rows/short splices; uint16/32 where= crash; complex projection guard); r5b-02/03
(KeyError contract standardization; `update_rows(mmap='auto')` IO swallow); r5c-02..r5c-06
(PathLike TypeError everywhere (= r1b-10); width-1 fsl↔scalar both directions; strict interop
kwargs; zero-batch RecordBatchReader crash; empty stream writes produced no file).

Perf (evidence rule met): r5a-10 header reads per call 2-4→1 (read −11.6%, scan −23.7%);
r5a-11 fallback-table double-open 2→1 (Main-authorized minimal `read_fallback_table` edit).

Minors/CLEANUP: r5a-09/15, r5b-08, r5c-07/08/11. Candidates RE-DERIVED as non-defects with
evidence (kept honest): `_normalize_mutation_rows` full preprocessing is load-bearing (TNULL
fills on partial appends — r5b-05 refuted); `_table/cache.py` is a live acquisition seam
(r5c-10, zero-change); DLPack lifetime sound (r5c-11).

## Deferred / routed (→ backlog at R17)

- r5a-12/r5b-07/r5c-15 `_normalize_row_slice` dedupe → **R6-B** (`_hdu/table_hdu_ref.py:158`
  method delegates to `_table/utils.py:91`).
- r5c-09 `TableHDURef.to_arrow(columns=)` kwarg collision → **R6-B** (strict-xfail pin landed,
  red-on-XPASS by design).
- r5a-13/r5c-12/r5c-13 docs drift (api-tables contracts, TDIM=(1) note) → R15.
- r5a-14 stream_table XTENSION probe re-reads header per call (out-of-set `_io_engine` perf).
- r5a-16 `fits_schema._iter_tfields_indexed` central TTYPE-less handling (compensated locally).
- r5b-06 write-path type divergences (float16 reject / 2D-uint8 2B-vs-2A / int8 inference —
  design decisions partly in write_api+C++).

## Evidence

Failing-first per fix pasted in slice files. Combined-tree verification before commit: **412
passed, 1 skipped, 1 xfailed** (the xfail is r5c-09's strict pin awaiting R6). Oracles run:
`tests/test_output_parity.py` 69 passed (after every decode-adjacent change), astropy ground
truth for TNULL/TDIM/int8-ASCII widths.

## Plan deviations / integration notes

- `chore(changelog): R5 round fixes` subject convention (as R1-R4).
- Cross-file authorization used: r5a-11 (`_read_pipeline_fallback.py:read_fallback_table` only,
  minimal single-open variant preserving the r4a-05 truncation-diagnosis chain).
- Integration-owned follow-ups: `style: ruff pass over R5 code` + `style: drop two unused
  assignments in R5 code` + mypy-strict typing fixes in `_table/interop.py` (fspath narrowing,
  numpy shape aliases) — all zero-behavior.

## Gate results

- `pixi run preflight-push` → PASS.
- `pixi run test` → PASS (1940 passed, 24 skipped, 22 xfailed, 381 warnings in 291.52s; +78 over R4's 1862; xfail+1 = r5c-09 strict pin awaiting R6).
