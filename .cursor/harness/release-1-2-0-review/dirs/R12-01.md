# R12-01 — benchmarks chunk 01

Owned: `splits/benchmarks.chunk01` (14 files, `benchmarks/bench_median_stack.py` through `benchmarks/suites.py`). IDs `r12b-NN`. No code change in this chunk.

## Findings

No measurement defect fixed here.

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| — | — | — | — | Warmup is excluded before `median_s` in `time_median`, `time_medians_interleaved`, and `bench_cfitsio_direct.c` (warmup loop, then `samples[0:runs]`). `suites.py` `gpu_transports` is `gpu_only=True`, and `run_gpu_transport_rows` returns `[]` when the device is `cpu` or the runtime is missing, so a CPU host does not record a GPU `case_id`. MegaCam, HTTP, and median-stack throughput use payload bytes. Science-pipeline `size_mb` is float32 output bytes (`* 4`), matching the module note that torchfits returns f32. ML loader CSV `unit` is `pixels/s` and the stored value is the median of those rates. | Reviewed, not edited. | clean |

## Disposition

| File | Disposition |
|---|---|
| `benchmarks/bench_median_stack.py` | clean — payload MB/s via `_payload_mb` |
| `benchmarks/bench_megacam_cutouts.py` | clean — cutout payload MB/s, `ZNAXIS` noted in docs |
| `benchmarks/bench_metadata.py` | clean |
| `benchmarks/bench_ml_loader.py` | clean — median pixels/s; printed MB/s is float32-equivalent and is not the CSV unit |
| `benchmarks/bench_science_pipeline.py` | clean — per-stage `size_mb`; `select_meta` is 0 |
| `benchmarks/bench_table.py` | clean |
| `benchmarks/bench_timing.py` | clean — warmup samples are not in the median; RSS is an untimed probe after the samples |
| `benchmarks/cfitsio_direct/CMakeLists.txt` | clean |
| `benchmarks/cfitsio_direct/bench_cfitsio_direct.c` | clean — warmup failures do not enter `samples` |
| `benchmarks/config.py` | clean |
| `benchmarks/mpl_config.py` | clean |
| `benchmarks/replays/upstream_sources.json` | clean |
| `benchmarks/run_cfitsio_direct_bench.py` | clean — summary column is `median_s` |
| `benchmarks/suites.py` | clean — `gpu_transports.gpu_only` is true |
