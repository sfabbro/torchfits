# R17 — `.cursor/` and `.dsh/`

Scope: the 33 tracked paths under `.cursor/` and `.dsh/` that had no ledger row.
No product code. No new public symbols.

## Checks

- `.cursor/skills/release-api-freeze-review/scripts/inventory_public_api.py` exists.
  `Path(__file__).parents[4]` is the repo root. `pixi run python` on it printed
  `__all__ count: 41` and `All __all__ symbols appear in docs/api.md`.
- Skill markdown path references resolve. The three non-resolving tokens are
  placeholders (`release-api-freeze-<version>.md`, a bare `` `__init__.py` ``,
  and the prose `docs/parity/examples`), not missing files.
- `.dsh/` exists (README, `cordis.patch.yml`, two skill files). Inert config;
  not executed.

## Backlog

`.cursor/post-1.0-backlog.md` gained `## 1.2 audit deferrals`. Still-open rows
are the round findings whose status stayed deferred and that a later round did
not close. Struck rows are r1c-12, r1c-14, r1b-10, r4a-15, r4b-13, r5c-09,
r5a-12/r5b-07/r5c-15, and the refuted r5b-05.

The 1.1 section now strikes the items the directory pass closed: GIL release,
mmap extent checks, strided DLPack, TFORM repeat / moved-from, unsigned
`read_full_numpy`, `TableHDU.head` / `TableHDURef.head`, `QuantizeError`, the
R7 CLI contracts, the R10/R11 test-extra and release-gate gaps, the documented
CFITSIO TOCTOU, and the PyTorch 2.10 wheel sentence (the lane is 2.13).

## Gate

- Inventory script → exit 0 (`__all__` 41, all named in `docs/api.md`).
- No runtime change. The tree gate is the R16 suite that finished on this
  commit's parent: 2238 passed, 24 skipped, 21 xfailed, 383 warnings in
  474.98s. `pixi run preflight-push` passed after that suite.
