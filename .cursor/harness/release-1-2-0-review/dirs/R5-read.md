# R5 slice A — `_table/{read,_read_scan,_read_schema,_read_where,engine}.py` + `_table_engine/`

Baseline: HEAD `1bb6958` + R1–R4 (worktree shared with slices B/C).
Method: review+fix one-pass; failing-first regression test per fix (`pixi run pytest tests/<file> -q`
against unfixed code → paste failure → fix → paste pass). Oracle `tests/test_output_parity.py` run
after decode-adjacent changes (69 passed).

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r5a-01 | BLOCKER | 1 | `_read_scan.py:_read_cpp_table_chunk`, `read.py:read` | Row selection silently ignored. `table.read(path, rows=[0, 999])` on a 10-row table returned the FULL table (10 rows) whenever the scattered read raised (`Invalid start row`); `rows=[7,2], backend="torch"` returned the full table; `rows=[0,5,999], where="ID > 2"` returned the where-filtered full table (rows dropped); raw-decode flags (`decode_bytes=False, apply_fits_nulls=False, include_fits_metadata=False`) made `prefer_torch_full_path` swallow both `rows=` and `where=` selections. Silent wrong result on valid input. | Scatter failures now propagate (no fallback can honor `rows=`); out-of-range indices raise `IndexError` naming the row (`_validate_row_selection`); `prefer_torch_full_path` requires `rows is None`; `rows=` routes through the C++ range engine for every backend; `_read_table_unfiltered` never falls through to the scan fallback with `rows=`. Tests: `test_rows_out_of_range_raises`, `test_rows_honored_with_torch_backend`, `test_rows_honored_raw_decode_flags`, `test_rows_with_where_out_of_range_raises`, `test_rows_empty_list_preserves_schema`. BEFORE: `Failed: DID NOT RAISE IndexError` / `assert [0, 1, 2, ...] == [7, 2]`; AFTER: `78 passed` (owned suites) + full sweep green. | fixed |
| r5a-02 | MAJOR | 1/7 | `_read_schema.py:_arrow_type_from_tform,_column_arrow_type,_schema_from_header` | `schema()` and empty-result reads reported dtypes that disagree with what the data readers return: unsigned conventions (TZERO=32768/2^31) `uint16/uint32` → `int16/int32`; TSCAL/TZERO-scaled columns read as `double` → header schema said `int16`/`float`; fixed-width strings `'rA'` read as `string` → schema said `fixed_size_list<string>[r]` (TFORM repeat is the char width, not a vector length). Empty read of a uint16 col: `full=uint16 empty=int16 schema()=int16`. | New `_column_arrow_type` applies the storage conventions (scaled→float64; TZERO=32768→uint16; TZERO=2^31→uint32; other non-zero TZERO→float64; TSCAL=1 keeps storage type — matches probe table incl. `scal_u16→double`); `'A'` maps to `utf8()`/`binary(width)` at any repeat. Test `test_empty_result_dtype_matches_full_read` (BEFORE: `AssertionError: U` — `int16` != `uint16`; AFTER: passed). Oracle `test_output_parity.py` 69 passed. | fixed |
| r5a-03 | MAJOR | 5/7 | `read.py:_read_prelude`, `_read_schema.py:_validate_projection_columns,_empty_table_with_schema` | Empty results silently dropped requested unknown columns while non-empty reads raised: `read(cols=["ID","NOPE"], row_slice=(0,0))` → `['ID'] rows=0` (silent), `read(cols=["ID","NOPE"], where="ID > 100")` (zero matches) → `['ID']` (silent); non-empty → `RuntimeError: Column not found`. Same signature, data-dependent contract. | Dict-like contract everywhere in the read/scan surface: `_read_prelude` validates the projection against the header (incl. `COL<i>` auto-names) and raises `KeyError(f"Column '{name}' not found")` naming the column — identical for empty/non-empty, `read`/`scan`/`read_torch`/`scan_torch`. `_empty_table_with_schema` preserves known-but-untypable requested names as null columns instead of dropping (its documented NOTE behavior). Test `test_unknown_column_raises_key_error_everywhere` (BEFORE: `RuntimeError: Column not found: NOPE` + silent drops; AFTER: KeyError ×6). Owing at R15: document `KeyError` for unknown `columns` (mutation API standardizes on the same). | fixed |
| r5a-04 | MAJOR | 7/1 | `_read_schema.py:_can_use_full_read_path,_unnamed_columns` | TTYPE-less columns were invisible to the capability check (skipped by `name` guard) and to the header schema: an unnamed `'5J'` column still qualified the scalar-only mmap fast path (`_can_use_mmap_row_path_for_full_read(...) == True`) and empty reads dropped the auto-named `COL2` (`full=['ID','COL2'] empty=['ID']`). | Capability check treats TTYPE-less columns under the reader auto-name `COL<i>` (validated by TFORM, counted for `any_selected`); `_unnamed_columns` rebuilds them as `TableColumnMeta` so `_schema_from_header` types them in file order (schema now agrees with `read_torch`'s `(n,5) int32`). Tests `test_capability_check_covers_unnamed_columns`, `test_empty_result_preserves_unnamed_columns` (BEFORE: `assert True is False`, `assert ['ID'] == ['ID', 'COL2']`; AFTER: passed). Residual routed as r5a-16. | fixed |
| r5a-05 | MAJOR | 5 | `read.py:_reject_complex_columns` (`except Exception`), `_read_scan.py:_scan_iter/_read_cpp_table_chunk/_scan_torch_iter` (`except (OSError, ValueError)` swallow-on-read) | `except Exception: return` silently skipped the complex-column guard on any header failure (incl. IO); the read-path header/metadata fallbacks swallowed `OSError` mid-read (silent degraded dtypes/metadata on IO errors). Repro: monkeypatch `torchfits.read_header` to raise `OSError` → `table.read()` silently returned degraded data. | Guard rewritten as `_read_prelude` (narrowed `except (ValueError, RuntimeError, TypeError)` for decode only — IO propagates); read-path fallbacks narrowed to `except ValueError`. The `_read_schema` *probes* (`_column_tforms_for_decode`, `_unsigned_column_dtypes`, `_schema_from_header`) keep their documented "never raise" contract (`except (OSError, ValueError)` — pinned by `tests/test_stream_table_and_cache.py::test_probe_returns_empty_on_missing_file` with an explicit intent comment). Test `test_io_errors_propagate_from_read_and_scan` (BEFORE: `Failed: DID NOT RAISE OSError`; AFTER: passed). | fixed |
| r5a-06 | MAJOR | 1 | `_table/engine.py:_read_ranges_as_chunk` | A-14 re-derived: rows of an empty segment surfaced as `zeros` (tensors) / `None` (lists); short segments silently spliced Python-list buffers (`buf[a:b] = value` with fewer items shrinks the buffer) and raised only an opaque torch `expanded size of the tensor` error for tensors. Also a dead no-op ("ponytail" placeholder pre-pass + drop). | Every non-empty range's segment is row-count validated per column (`_segment_row_count`); empty/short/missing-column segments raise `RuntimeError` naming the row window; zero-length ranges are skipped without a round-trip; placeholder pre-pass/drop deleted. Tests `test_read_ranges_short_segment_raises`, `test_read_ranges_empty_segment_raises`, `test_read_ranges_zero_length_ranges_ok` (BEFORE: `Regex pattern did not match ('The expanded size of the tensor...')`, `DID NOT RAISE`, `IndexError: pop from empty list`; AFTER: passed). | fixed |
| r5a-07 | MAJOR | 7/1 | `_read_where.py:_torch_cmp_mask` | `read_torch(where="V16 > 40000")` on a uint16 (TZERO) column crashed: `NotImplementedError: "gt_cpu" not implemented for 'UInt16'` — torch has no CPU compare kernels for uint16/uint32 and the widening fix only fired for out-of-range int literals. Every simple compare on uint16/uint32 columns through `table_api`/torch-mask paths crashed (valid input, deterministic failure). | uint16/uint32 tensors always compare at int64 (covers float literals too); out-of-range int widening unchanged. Matrix test `test_matrix_read_torch_agrees[V16 > 40000]` + `test_matrix_projection_and_predicate_columns` (BEFORE: `NotImplementedError: "gt_cpu" not implemented for 'UInt16'`; AFTER: passed). | fixed |
| r5a-08 | MAJOR | 7 | `read.py:_reject_complex_columns` | The complex-column guard rejected the whole HDU even when the projection excluded complex columns: `read(path, columns=["ID"])` on a table with one `C` column raised `NotImplementedError` while `schema(columns=["ID"])` served `ID` and `read_torch` served everything (inconsistent; astropy/fitsio allow column projection past complex columns). | Guard is projection-aware: rejects only complex columns the result would contain (projection ∪ WHERE predicates); `columns=None` still rejects any complex column. Test `test_complex_projection_columns_are_served` (BEFORE: `NotImplementedError: complex table columns...` on `columns=["ID"]`; AFTER: served; unprojectable cases still raise). | fixed |
| r5a-09 | MINOR | 7 | `read.py`, `_read_scan.py` (`columns=[]`) | `columns=[]` meant "all columns" in plain reads/scans/read_torch (engine `col_list=[]` idiom) but "zero columns" in every where path (`read(columns=[], where=...) → cols=[] rows=0` vs `read(columns=[]) → cols=[ID, LB]`) — strategy-dependent projection. | `columns=[]` projects like `columns=None` (all columns) normalized once in `_read_prelude`; every strategy agrees. Test `test_matrix_empty_projection_is_all_strategies` (BEFORE: `assert [] == ['ID', 'V16', ...]`; AFTER: passed). Owed doc note at R15. | fixed |
| r5a-10 | MAJOR(perf) | 2 | `read.py:_read_prelude`, `_read_scan.py:_iter_chunks_cpp_table,_scan_iter,_read_table_from_scan_batches,_read_cpp_table_chunk` | Redundant header reads per call: `table.read()` read the header 2×, `scan()` 3×, `read(backend="torch")` 4× (guard + `_get_hdr` + `_iter_chunks_cpp_table` + the fallback's re-entry through the public `scan()` wrapper, which re-ran guards and the header read). Deterministic count proof (mock `torchfits.read_header`, same host): BEFORE `read()=2, rows=2, where=2, scan()=3, backend=torch=4`; AFTER `read()=1, rows=1, where=1, scan()=1, backend=torch=2 (residual: `stream_table`'s XTENSION probe re-reads the header despite `total_rows=` — routed as r5a-14)`. Throwaway micro-timing (200 calls × 5 loops, 200-card header, 2000-row table; "before" = same code with each `read_header` emulated as two reads, i.e. the removed redundancy): `read() 524.8→463.7 µs (-11.6%)`, `scan() 647.2→494.0 µs (-23.7%)`; unit cost `read_header` = 188.2 µs. | Single header read per call: `_read_prelude` returns the header, threaded into `_read_cpp_table_chunk`/`_read_table_with_where`/`_scan_iter`/`_iter_chunks_cpp_table`/`_scan_torch_iter`/`stream_table(total_rows=)`; `_read_table_from_scan_batches` goes straight to `_scan_iter` (no public-wrapper re-entry). Count+timing above is the evidence. | fixed |
| r5a-11 | MINOR(perf) | 2 | `_io_engine/_read_pipeline_fallback.py:read_fallback_table` (OUT OF SLICE SET — edit authorized by Main) | "Fallback-table double-open per call" (backlog hygiene item) confirmed at HEAD: `read_fallback` opens a fresh handle per call (`get_cached_handle` = `open_fits_file`, no handle cache since Option A) and `read_fallback_table` branch 1 then called `cpp_module.read_fits_table(path, …, True)` which opens the file AGAIN inside the extension. Cache-miss count proof (distinct files, mock counters): BEFORE `{'open_fits_file': 1, 'read_fits_table(path)': 1}` = 2 opens/call. | Opportunistic single-open read: `read_fallback_table` tries `read_fits_table_rows_from_handle(file_handle, …)` (the already-open handle) before the mmap stage's path-based bindings. The attempt is NOT part of the reader-compatibility chain — failures are not recorded and the original readers run in order, preserving the r4a-05 error-identity contracts (truncated files still surface the `truncat` diagnosis; `test_read_pipeline_contracts.py` typed-dispatch / IO-not-retried / first-error pins all hold); IO errors from the attempt propagate immediately (never retried). The tl_cache-backed buffered stage stays path-based per its documented ~24 MiB reuse trade-off (comment updated honestly). Failing-first test `test_fallback_table_opens_file_once_per_call` — BEFORE: `{'open': 1, - 'path': 0, + 'path': 1}` (1 failed); AFTER: `1 passed` with `path == 0` (hidden open gone → 1 open/call). Timing (40 distinct 20k-row files ×3 medians, same host): `default 232.7→226.2 µs/call (−2.8%)`, `mmap=False 266.5→259.3 (code path untouched — noise)`; the count proof (opens 2→1) is the primary evidence per the perf evidence rule. First attempt (from_handle as the primary reader) was REVERTED after the sweep showed it regresses the truncation-diagnosis identity (`test_mmap_read_raises_clean_error`: expected `truncat`, got `error reading from FITS file`) — recorded here as the negative evidence for reordering the chain. Residual: `mmap=False` flow keeps 1 cold double-open on first touch of a path (tl_cache warm afterwards) — documented trade-off. | fixed |
| r5a-12 | CLEANUP | — | `_table/utils.py:91`, `_hdu/table_hdu_ref.py:158` (BOTH OUT OF SLICE SET) | `_normalize_row_slice` duplicated ×2 (module fn + `TableHDURef._normalize_row_slice` method; semantic near-dupes — step handling differs slightly per R5-WriteTable's r5b-07). Neither copy is in slice-A files. | Not touched (assignment: coordinate via hub). Confirmed by R5-WriteTable (recorded as r5b-07) and routed by Main to R6-B (joining r5c-09): fix = method delegates to `_table.utils._normalize_row_slice`. | deferred |
| r5a-13 | MINOR | 8 | `docs/api-tables.md` vs `_table_engine/read_policy.py`, `read.py` | Doc/code drift found while checking `_table_engine` contracts (all fences match signatures exactly — `read`/`scan`/`read_torch`/`reader`/`schema` verified): (1) backend table row `"torch"` says "Chunked path via `table.scan_torch`" but `read(backend="torch")` streams via `_io_engine.table_streaming.stream_table`; (2) `rows` documented as "Specific row indices" with no error contract — now `IndexError` for out-of-range (r5a-01) and honored for every backend; (3) unknown `columns` now `KeyError` (r5a-03); (4) `columns=[]` ≡ `columns=None` (r5a-09); (5) complex columns can be projected past (r5a-08). `read_torch` docstring matches implementation (OR/IN/NOT/IS NULL → `ValueError` — probed). `read_policy.choose_where_read_plan`/`should_skip_cpp_for_where` match the documented `"auto"`/`"cpp"` behavior (pinned by `tests/test_read_policy.py`). | Recorded; docs land at R15 per plan ("user-visible changes: record owed doc updates"). | deferred |
| r5a-14 | MINOR(perf) | 2 | `_io_engine/table_streaming.py:stream_table` (OUT OF SLICE SET) | Despite `total_rows=` being supplied (my r5a-10 threading), `stream_table` re-reads the header per call for its XTENSION probe ("The XTENSION probe must run even when the caller supplied total_rows"), so `scan_torch()`/`read(backend="torch")` still perform 2 header reads per call. | Out of set (R4 file; not in the r5a-11 authorization). Fix sketch: probe XTENSION via `cpp.read_keys(file_handle, …)` on the existing handle or accept the caller-supplied header. Recorded as routed finding. | deferred |
| r5a-15 | MINOR | 1 | `read.py:reader` | Empty-result schema taken from the WRONG HDU for `hdu="EXTNAME"`: `reader(path, hdu="TWO", row_slice=(0,0))` typed the empty result from HDU 1 (`int(hdu) if isinstance(hdu, int) else 1`). | `reader()` resolves the str hdu before building the empty fallback. Test `test_reader_empty_result_schema_matches_hdu` (BEFORE: `assert ['A'] == ['B']`; AFTER: passed). | fixed |
| r5a-16 | MINOR | 1 | `fits_schema.py:_iter_tfields_indexed` (OUT OF SLICE SET) | The shared column walker hides TTYPE-less columns entirely (`if name is None: continue`), so every `fits_schema` consumer misses them (`column_tnull_map` drops their TNULL, `_column_tforms_for_decode`/`_build_fits_metadata` skip them). Compensated inside `_read_schema._unnamed_columns` (r5a-04) but the central walker should surface `COL<i>` with its TFORM. | Out of set (R1-area file). Recorded as routed finding with the `COL<i>` naming evidence (probed: TTYPE-less col at index 1 → `COL1`, index 2 → `COL2`). | deferred |

## Defects found outside the slice (routed)

- `'rB'` byte-vector columns decoded as garbage strings in `arrow_convert.py` (BLOCKER class 1) —
  repro forwarded to Main verbatim and owned by R5-TableInterop; **fixed there as r5c-14** (plus
  their r5c-03 width-1 scalar rule; `_read_schema`'s `fsl<uint8>[w]`/scalar mapping agrees).
- `_normalize_row_slice` ×2 → r5a-12 (above), R6-B owns the dedupe.
- `read_fallback_table` double-open → r5a-11 (above), landed here under Main's authorization.
- `stream_table` XTENSION probe header re-read → r5a-14 (above).
- `fits_schema._iter_tfields_indexed` unnamed-column hiding → r5a-16 (above).

## Evidence index

- Failing-first / passing outputs are quoted in the rows; full pytest outputs:
  owned suites before: `17 failed, 41 passed` (r5a-01..09, 15 family) and `1 failed` (r5a-11 count test);
  after: `79 passed` (slice-A owned) and the final combined sweep
  (`tests/test_output_parity.py` + 30 table/read/pipeline suites): `418 passed, 1 skipped, 1 xfailed`.
- r5a-10 count proof + micro-timing: `/tmp/count_after.py` output quoted in the row.
- r5a-11 count proof: binding-call counters (`open_fits_file`, `read_fits_table`, `read_fits_table_rows`,
  `*_from_handle`) around `torchfits.read(path, 1)` on distinct files; timings from `/tmp/tim_fb.py`
  (BEFORE measured on the reverted pre-fix code, AFTER on the landed variant, same host).

## Per-file disposition

| file | depth | finding IDs | status |
|---|---|---|---|
| `src/torchfits/_table/read.py` | deep | r5a-01, r5a-03, r5a-05, r5a-08, r5a-09, r5a-10, r5a-15 | fixed (`__all__` confirmed sealed: 8 public names `dataset, read, read_torch, reader, scan, scan_torch, scanner, schema`) |
| `src/torchfits/_table/_read_scan.py` | deep | r5a-01, r5a-05, r5a-10 | fixed |
| `src/torchfits/_table/_read_schema.py` | deep | r5a-02, r5a-03, r5a-04, r5a-05(probe contracts preserved) | fixed |
| `src/torchfits/_table/_read_where.py` | deep | r5a-07 | fixed |
| `src/torchfits/_table/engine.py` | deep | r5a-06 (A-14) | fixed |
| `src/torchfits/_table_engine/__init__.py` | surface | — | clean (re-export surface matches `read_policy`/`backend_policy`) |
| `src/torchfits/_table_engine/backend_policy.py` | surface | — | clean (`validate_table_backend` error contract pinned by `test_read_policy.py`) |
| `src/torchfits/_table_engine/read_policy.py` | surface | r5a-13 (doc row only) | clean code; docs drift recorded |
