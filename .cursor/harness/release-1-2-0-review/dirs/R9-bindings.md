# R9 slice A — `cpp_src/` image/file family: `fits_bindings.cpp`

Round R9 (slice A) review+fix record. Owner: R9-FitsBindings. Scope: `src/torchfits/cpp_src/fits_bindings.cpp` (all 2627 lines) + owned tests
(`tests/test_batch_error_contracts.py`, `tests/test_read_full_numpy_parity.py`, `tests/test_hdu_count_scan.py` new; `tests/test_read_pipeline_contracts.py` extended).
Sibling slice B (`fits_file.cpp`, `fits_detail.h`, `fits_file.h`, `fits_handle.h`, `fits_rw.h`) = R9-FitsFile; cross-slice items noted per row.

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r9a-01 | MINOR | 1 wrong result (degenerate edge) | `fits_bindings.cpp:read_full_numpy` (`shape.empty()` branch) | NAXIS=0 output dtype violated the house contract: `read_full_numpy` returned `array([], dtype=uint8)` for every BITPIX while `read_tensor`/`read_full_nocache`/`read_full_unmapped` return the BITPIX-keyed empty dtype (`blank-nulval`/adjacent-API agreement). Repro: `pytest tests/test_read_full_numpy_parity.py -q` at HEAD → `FAILED ...[False-naxis0_bitpix16] - AssertionError: read_full_numpy naxis0_bitpix16 mmap=False: dtype uint8 != int16` (and `True-`). | Fixed: `shape.empty()` now switches on `bitpix` (int16/int32/int64/float32/float64/uint8-default) matching `read_tensor_canonical`'s NAXIS=0 contract. Validation: matrix cases `[False\|True-naxis0_bitpix16]` green; `test_sibling_cpp_readers_agree[*-naxis0_bitpix16]` green. | fixed |
| r9a-02 | BLOCKER | 1 wrong result + 3 uninitialized read | `fits_bindings.cpp:read_full_unmapped` | NAXIS=0 fell through to `torch::empty(at::IntArrayRef(torch_shape, 0), …)`, which builds a **0-d one-element tensor holding uninitialized memory**, returned to Python as a scalar instead of an empty array. Repro at HEAD: `FAILED ...::test_sibling_cpp_readers_agree[False-naxis0_bitpix16] - AssertionError: read_full_unmapped naxis0_bitpix16: shape () != (0,)` (and `True-`). | Fixed: early return `torch::empty({0}, BITPIX-keyed dtype)` (identical to `read_tensor_canonical`), closing the CFITSIO handle before return. Validation: both `test_sibling_cpp_readers_agree` cases green. | fixed |
| r9a-03 | — | 1/8 convention (plan pre-decision) | `fits_bindings.cpp:read_full_numpy`, `read_full_numpy_cached` | **Re-derived already-aligned at HEAD.** The scaled-integer outputs already implement the house unsigned convention (uint16 for `BZERO=32768`, uint32 for `BZERO=2^31`, int8 for `BZERO=-128`, float32 otherwise incl. BLANK promotion — `blank-nulval`), matching `read_tensor_canonical` branch-for-branch. Exact probe: `pixi run pytest tests/test_read_full_numpy_parity.py -q` against the unmodified build with astropy/handcrafted scaled fixtures (`uint16_pack`, `uint32_pack`, `int8_signed`, `blank_uint16`, `blank_identity`, `scaled_int16`) — every case passed **before** any C++ edit except the r9a-01/02 NAXIS=0 edges. (First failing-first run also showed `scaled_int16` diverging — root cause was the FIXTURE, see r9a-13.) | No behavior change needed. Contract pinned bitwise (`read_full_numpy(...) == read_tensor(...).numpy()` incl. `read_full_numpy_cached`) over the scaled fixtures + BLANK/IEEE edge pins. | fixed (pinned) |
| r9a-04 | MAJOR | 5 error contract | `fits_bindings.cpp:read_hdus_batch`, `read_hdus_sequence_last` | Per-HDU failures surfaced bare inner text that names neither the path nor the HDU (`Could not move to HDU`), violating the batch attribution contract (r4a-01 C++ half: errors name their path). Repro at HEAD: `FAILED tests/test_batch_error_contracts.py::test_read_hdus_batch_bad_hdu_raises_naming_path_and_hdu - AssertionError: assert 'good1.fits' in 'Could not move to HDU'`. | Fixed: both loops wrap `file.read_tensor` and rethrow `std::runtime_error("Error reading " + path + " HDU " + index + ": " + inner)` (inner text preserved verbatim; empty/non-std → `unknown error`). Exact contract: message always = `Error reading <path> HDU <int>: <inner-what-or-unknown-error>`. Validation: `test_read_hdus_batch_bad_hdu_raises_naming_path_and_hdu` green; existing suites (`test_output_parity.py`, `test_malformed_fits.py`, `test_truncated_table_errors.py`) green — wrapped messages contain the original text so inner-text matchers still hold. | fixed |
| r9a-05 | MAJOR | 1 silent fall-through / 5 error contract | `fits_bindings.cpp:read_images_batch` (`errors[i] = e.what()` ×3 sites) | Error-vector hole: an exception whose `what()` is **empty** left `errors[i]` empty, defeating the `!errors[i].empty()` scan → the undefined `results[i]` tensor was returned to Python as a "successful" batch entry (silent fall-through / None-typed slot). A **non-`std::exception`** throw in a worker thread escaped `catch (const std::exception&)` entirely → `std::terminate` (crash). Not reachable from Python (every in-tree throw carries text — code proof: grep of `throw` in `cpp_src/` shows no empty-message/non-std throws), so recorded as code proof per policy. | Fixed: `record_batch_error` helper (`catch (...)` → `std::current_exception()` → `what()` with non-empty fallback `"unknown error"`) at the first-read, serial, and parallel-pool sites. Exact contract: a failed batch index ALWAYS yields a non-empty `errors[i]` and the batch raises `RuntimeError("Error reading <path>: <text>")` naming the first failing path in index order (unchanged message shape). Validation: `tests/test_batch_error_contracts.py` green (corrupt/missing-in-trio pins — those already raised at HEAD for std::exceptions, probe recorded in r9a-08). | fixed |
| r9a-06 | BLOCKER | 1 wrong result / 5 error contract / 7 hostile input (r7a-08 routed) | `fits_bindings.cpp:checked_num_hdus` + `open_and_read_headers`, `read_num_hdus`, `get_num_hdus` (method + free-fn bindings) | CFITSIO `ffthdu` walks HDU headers and **discards the walk's error status**, so a file truncated mid-header (or with a hostile HDU-header tail) silently under-reports HDUs and every caller exits 0 with the wrong inventory. Repro at HEAD (3-HDU image file cut at `hdul[2]._header_offset + 100`): all five tests in `tests/test_hdu_count_scan.py` → `Failed: DID NOT RAISE RuntimeError` (`read_num_hdus`, `_cpp.read_num_hdus`, `torchfits.open`, `_cpp.open_and_read_headers`, `get_num_hdus` bindings). Astropy ground truth (R9-FitsFile probe): reports 2 + `VerifyWarning: "Error validating header for HDU #2 ... Header size is not multiple of 2880 ... There may be extra bytes after the last HDU or the file is corrupted"`. | Fixed: `checked_num_hdus` tail probe (`fits_bindings.cpp:358-437`) wired into all 4 scan surfaces. **Exact contract:** after `fits_get_num_hdus`, `fits_get_hduaddrll` on the last HDU gives `dataend` = exclusive record-aligned end (`ffghadll` = `headstart[curhdu+1]`, CFITSIO `fitscore.c:4290`); when `st_size > dataend > 0` the first ≤8 tail bytes at `dataend` are keyword-prefix-matched against `SIMPLE`/`XTENSION` (partial prefixes included) → `std::runtime_error("FITS file appears truncated or corrupt: the HDU scan parsed <N> HDU(s) but another HDU header at byte <dataend> could not be parsed: <path>")` (message contains `truncat` + the path); **non-header garbage tails are tolerated** (count unchanged — the `test_malformed_fits.py` garbage-after-end contract); **short files (data truncation) are not scan errors** (the count is honest; reads surface the error); skipped for extended-syntax paths, `://` URLs, and stat failures. CHDU side effect: moves to the last HDU via `ensure_hdu` (all Python callers re-position per-HDU — verified). Validation: all 5 `test_hdu_count_scan.py` tests green incl. false-positive guards (clean 3-HDU, clean VLA-heap table, garbage tail, data-truncation boundary). Ownership note: scan-locus handed to this slice by R9-FitsFile during coordination (their `FITSFile::get_num_hdus` stays a thin `ffthdu` wrapper); move approved by Main. | fixed |
| r9a-07 | BLOCKER | 1 wrong result (`blank-nulval` violation) | shared `d::cfitsio_float_nulval_ptr` (`fits_detail.h`, owner R9-FitsFile) consumed at `fits_bindings.cpp:read_full_numpy` (Phase 3), `read_full_unmapped`, and via `read_tensor_canonical` | Compressed float STORAGE destroyed `Inf` / `-0.0` / subnormals (fnan marks them "undefined" whenever a nulval pointer is set): `read_full_numpy`/`read_full_unmapped` returned NaN/±0 where `read_tensor`+astropy preserve the bits. Repro at HEAD: `FAILED tests/test_read_full_numpy_parity.py::test_compressed_float32_ieee_bits_survive` — `bytes differ … [0,2]: 192 (ACTUAL), 128 (DESIRED) … [0,16]: 0 (ACTUAL), 1 (DESIRED)` (NaN vs Inf, signbit lost on -0.0, subnormal zeroed) on a GZIP_1 CompImageHDU fixture. | Fix owned by R9-FitsFile in the shared helper (key nulval on STORAGE bitpix: float/double storage → nullptr always incl. compressed; NaN only for integer storage read as float/BLANK) — my 3 call sites inherit it unchanged (no duplicated nulval logic in `fits_bindings.cpp`, verified by grep). Pin: `test_compressed_float32_ieee_bits_survive` asserts `read_full_numpy`/`read_full_unmapped` == `read_tensor(...).numpy()` bitwise with Inf/-0.0/subnormal fixture sanity. Status at this slice's hand-off: test RED (their helper fix pending their rebuild round); goes green with their diff (their `tests/test_fitsfile_edges.py` pins the same across 5 paths). | fixed (cross-slice owner: R9-FitsFile) |
| r9a-08 | — | 5 error contract (r4a-01 re-derivation) | `fits_bindings.cpp:read_images_batch`, `read_hdus_batch` + Python batch surface | Re-derived already-correct at HEAD for the reachable contract: one corrupt file in a batch of three raises `RuntimeError` naming the corrupt path at the C++ level (`_cpp.read_images_batch([good, corrupt, good], 0, True)`) and at the Python surface (`torchfits.read([g1, corrupt, g2])`, per-file fallback attributes). Exact probe: `pixi run pytest tests/test_batch_error_contracts.py tests/test_read_pipeline_contracts.py -q` at HEAD — `test_read_images_batch_corrupt_in_three_raises_naming_path`, `…_missing_in_three…`, `test_read_hdus_batch_corrupt_path_raises_naming_path`, `test_read_images_batch_all_good_returns_all`, `test_read_images_batch_empty_returns_empty`, `test_read_hdus_sequence_last_empty_raises`, `test_read_list_corrupt_file_in_batch_of_three_raises_naming_path` all **passed before any C++ edit**. Residual unreachable holes = r9a-05. | No behavior change needed (contract as specified by the plan candidate already held for `std::exception` paths). Pinned by the named tests. | fixed (pinned) |
| r9a-09 | — | 4 concurrency / 3 cache staleness (r8b-02 re-derivation) | `fits_bindings.cpp:read_full_cached` `tl_cache`/`tl_lru` (`LocalKey{meta->uid, hdu_num}`) + `fits_detail.h:SharedReadMeta::uid` | **Re-derived: uid-bump eviction is SOUND (no fix needed).** Exact probe (`pixi run python` — ThreadPool(1) so one `thread_local` cache is exercised twice around each mutation): (A) explicit invalidation (`torchfits.write(..., overwrite=True)` → `invalidate_shared_meta` → fresh `SharedReadMeta` with a fresh `g_shared_meta_uid` monotonic counter) → fresh `(3,5) int16`; (B) out-of-band `writeto(overwrite=True)` + sleep 1.05s (past the validate interval) → fresh; (C) out-of-band replacement within the interval → `(2,2) float32` **stale until the window passes**, then `(3,5) int16`. C is the documented `TORCHFITS_SHARED_META_VALIDATE_INTERVAL_MS` (default 1000 ms) validate throttle in `get_shared_meta_for_path` — it gates **all** shared-meta caches identically (e.g. `meta->image_info_cache`), is env-tunable, and is not `tl_cache`-specific; uids never repeat across meta generations (global counter) so no post-invalidation aliasing is possible. | No fix required. Contract recorded for the R15 env-knob inventory; pass-2 re-verification = the A/B/C probe above. | fixed (verified) |
| r9a-10 | — | verify-only (GIL) | `fits_bindings.cpp` bindings (`read_tensor`/`read_header`/`get_num_hdus`/`get_hdu_type`/`read_subset`/`SubsetReader.read`; `read_full*` family incl. `read_full_numpy` phases 1-3; `open_and_read_headers`; `read_nrows`/`read_keys`/`read_shape`/`read_colnames`/`read_table_info`/`read_num_hdus`) | Verify-only anchor (fixed earlier, `ce94d8a`): GIL released around long C++ work. Verified at HEAD: every named binding wraps its C++ body in `nb::gil_scoped_release` (grep: `grep -n "gil_scoped_release" src/torchfits/cpp_src/fits_bindings.cpp` — one per listed entry point, including all three `read_full_numpy` phases). `open_and_read_headers` releases before `FITSFile` construction → network opens (`://` → `fits_open_file`, CFITSIO driver) do not hold the GIL; `check_fits_filename_security` runs in the `FITSFile` ctor before any open (`cfitsio-http-ssrf` intact). No change. | Verified by code read + grep; no defect. | fixed (verified) |
| r9a-11 | — | verify-only (2 overflow / 5 error contract / 7 edges) | `fits_bindings.cpp` (all sites) | Verify-only: (a) `std::cerr` fold — grep shows **zero** `std::cerr`/`std::cout` in `fits_bindings.cpp` (only `snprintf` keyname builders); nothing to fold. (b) NAXIS-product overflow — every read path computes `d::checked_nelements_product` (overflow-checked against `LONGLONG_MAX/16`) before buffer use (`read_full_unmapped` :563, `read_full_nocache` :722, `read_full_numpy` Phases 2-3) and `alloc_numpy_array` (`torch_compat.h`) rejects size_t/byte-size overflow; `write_table_hdu` guards `num_rows * col.repeat` and TFORM repeat parse with explicit overflow checks; `PyLong_AsLongLongAndOverflow` guards header ints. Note: `read_full_numpy` allocates in Phase 1 before its checked product (Phases 2/3) — hostile products still raise typed (alloc guard/bad_alloc/`NAXIS product overflow`) before any buffer write. (c) zero-pixel/zero-HDU edges — `zero_pixel` (NAXIS1=0) parity pinned; NAXIS=0 = r9a-01/02; empty batch lists pinned (`read_images_batch([]) → []`, `read_hdus_sequence_last([]) → RuntimeError`). | No defect found; pinned by the named tests. | fixed (verified) |
| r9a-12 | — | 8 docs/API faithfulness | `tests/test_read_full_numpy_parity.py:test_image_scale_accumulation_is_float32_pinned` | A-06 pin (plan pre-decision): image scale accumulation stays **float32** in 1.2.0 (`BSCALE=0.1, BZERO=32768` raw `[0, 1000, -1000]` → `[[32768.0, 32868.0, 32668.0]]` float32) on `read_full_numpy`, `read_full_numpy_cached`, `read_tensor`, `read_full_scaled_cpu`; values exact (scale actually applied), dtype never float64. | Pin only (no numeric change). 2.0 float64 backlog entry recorded below. | fixed (pinned) |
| r9a-13 | MINOR | 7 edge (test-fixture lesson) | `tests/test_read_full_numpy_parity.py` fixture builder | Probe record (Phase-2): astropy `PrimaryHDU(int16_data, header={"BSCALE": 0.1, "BZERO": 32768}).writeto(...)` **silently drops the scaling cards** — exact probe output: `BSCALE in h == False, BZERO in h == False, BITPIX == 16` (`pixi run python -c` astropy round-trip, 2026-09-23). Genuinely-scaled fixtures are therefore handcrafted byte-wise (`_write_raw_image`: explicit cards + big-endian raw data). BLANK cards DO survive astropy writes (probe: `blank_uint16` header = `BITPIX 16, BLANK 0, BSCALE 1, BZERO 32768`; `blank_identity` = `BITPIX 16, BLANK -32768`) so those fixtures stay astropy-written. | No product defect (fixture-only). `_write_raw_image` documents the trap. | fixed |

## Deferred (one line each)

- **2.0 image-scale float64:** `fits_detail.h:read_tensor_canonical` / `fits_file.cpp:read_image_raw` / `read_full_scaled_cpu` accumulate scaled images in float32 — the float64 switch must re-baseline every float-scaled parity expectation; pinned by `tests/test_read_full_numpy_parity.py::test_image_scale_accumulation_is_float32_pinned` (r9a-12) so the change is deliberate. (Plan Assumption 1 override path stays open.)

## Verify-only anchor: exact greps for pass-2 re-verification

- GIL: `grep -n "gil_scoped_release" src/torchfits/cpp_src/fits_bindings.cpp` — matches every long-C++ entry point listed in r9a-10.
- cerr-free: `grep -n "std::cerr\|std::cout\|printf" src/torchfits/cpp_src/fits_bindings.cpp` — only `snprintf` keyname builders.
- No duplicated nulval logic: `grep -n "cfitsio_float_nulval_ptr" src/torchfits/cpp_src/fits_bindings.cpp` — call sites only (plus canonical in `fits_detail.h`).
- tl_cache freshness: the A/B/C probe script is quoted inline in r9a-09.

## Per-file disposition (every file in this slice's set)

| File | Disposition |
|---|---|
| `src/torchfits/cpp_src/fits_bindings.cpp` | Deep-reviewed all 2627 lines. Fixed r9a-01, r9a-02, r9a-04, r9a-05, r9a-06 (hunks: `record_batch_error` helper + 3 catch sites; `read_hdus_batch`/`read_hdus_sequence_last` error context; `read_full_unmapped` NAXIS=0 early return; `read_full_numpy` NAXIS=0 dtype switch; `checked_num_hdus` + 4 scan-site wiring; `#include <exception>`). Re-derived already-correct + pinned: r9a-03, r9a-08, r9a-09. Verify-only clean: r9a-10, r9a-11. Cross-slice (shared helper, owner R9-FitsFile): r9a-07. No deferred items in this file. |
| `tests/test_batch_error_contracts.py` | NEW. Pins the batch attribution contract at the C++ surface (corrupt/missing-in-trio naming the path; bad-HDU naming path+HDU; happy paths; empty-list). Covers r9a-04/05/08. |
| `tests/test_read_full_numpy_parity.py` | NEW. Bitwise `read_full_numpy(_cached) == read_tensor(...).numpy()` matrix (11 fixtures × mmap on/off) + sibling-reader agreement (`read_full_nocache`, `read_full_unmapped`) + BLANK/IEEE/unsigned edge pins + compressed-IEEE preservation (r9a-07 pin) + A-06 float32 pin (r9a-12). |
| `tests/test_hdu_count_scan.py` | NEW. r9a-06 contract at all 4 scan surfaces + no-false-positive guards (clean multi-HDU, VLA-heap table, garbage tail tolerated, data-truncation honest count). |
| `tests/test_read_pipeline_contracts.py` | EXTENDED (append-only): `test_read_list_corrupt_file_in_batch_of_three_raises_naming_path` — r4a-01 end-to-end Python-surface pin. All pre-existing tests untouched and green. |
