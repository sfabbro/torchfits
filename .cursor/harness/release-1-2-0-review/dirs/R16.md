# R16 — build / CI / packaging / root config

Reviewed in-process (28 tracked paths). No C++ edits. `scripts/release_lane.py` and `tests/test_release_lane.py` were edited for findings that live in this round's files.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r16a-01 | MINOR | docs/API faithfulness | `scripts/release_lane.py:main` | `--help` and the module docstring said `--prerelease rc5` renders `1.0.0rc5`. `render()` appends the suffix to `torch_lanes.json` `torchfits_version` (`1.1.3`). Before: `test_prerelease_help_names_the_lane_base` did not see `1.1.3rc5`. | Help text is built from `load_lanes()`. The docstring no longer names `1.0.0rc5`. | fixed |
| r16a-02 | MAJOR | docs/API faithfulness | `pixi.toml` `[package.*-dependencies]` | Build, host, and run required `cfitsio >=4.6.3,<4.6.5` while `extra-args` set `TORCHFITS_USE_VENDORED_CFITSIO=ON` and `extern/VERSIONS.txt` pins `cfitsio-4.7.0`. CMake then ignores an environment CFITSIO. The host env also had no `bzip2`, so a pixi-build could leave `BZIP2_1` off. `packaging/conda/recipe.yaml` already omits conda cfitsio and depends on `bzip2`. Before: `test_pixi_package_uses_vendored_cfitsio_not_conda_4_6` saw the 4.6 pin. | Those three pins are gone. Host depends on `bzip2 >=1.0.8`. The Unreleased claim that the only remaining caps are the torch lane and the pixi pyarrow cap is true again. | fixed |

## Re-derived, not changed

- Release-gate file lists match: 20 paths, same order, in `pixi.toml` `release-gate` and `.github/workflows/ci.yml` job `release-gate`. Not a defect.
- GHA job `test` is `pytest tests/ -q -m "not performance"`. `release-gate`, `wheel-smoke`, `bench-smoke`, and `docs-contract` do not use that filter. Left that way.
- `pixi.toml` `changelog-release` stays `python scripts/update_changelog.py --release`. A version argument is appended by pixi (`pixi run changelog-release 1.2.0` and `pixi run changelog-release -- 1.2.0`). A bare run fails argparse because `VERSION` is required. The task was not rewritten.
- `sanitizer.yml` runs on every pull request (not only `cpp_src/**`) on `ubuntu-latest` and `macos-15`. Broader than a path filter; both OSes are covered.
- `extern/VERSIONS.txt` sha256 `f281ca29…c59aee` matches the GitHub `cfitsio-4.7.0` tag tarball (downloaded this round). `vendor.sh` applies every `extern/patches/cfitsio-4.7.0-*.patch`. Both patches are present in the local `extern/cfitsio` tree (`BZ2_bzBuffToBuffCompress`, the PLIO cbuf comment).
- `pyproject.toml` upper bounds are the torch lane (`>=2.13,<2.14`) and the exact `[cpu]` / `[cuda]` pins. `ruff`, `zensical`, `pyarrow`, `polars`, `duckdb`, and `mypy` are floors only. The pixi `pyarrow >=25.0.0,<26` cap remains the documented libabseil constraint.
- `[tool.ruff.lint] select = ["E4", "E7", "E9", "F"]` matches the stability comment. `.pre-commit-config.yaml` ruff rev `v0.16.7` matches the pixi and CI floors. Hooks are `ruff` (`--fix`) and `ruff-format`; pixi `lint` is `ruff check . && ruff format .`.
- `README.md` states Python 3.10–3.14 and PyTorch 2.13.x and cites no bench run-id. Published run-ids stay in `docs/benchmarks.md`.
- `CLAUDE.md` is `@AGENTS.md`. `constraints-wheel.txt` is `torch>=2.13,<2.14`. Recipe `torch_pin` is `>=2.13,<2.14`. `check-lane` passed after the pixi edit.
- `build_wheels.yml` resolves the lane with `release_lane.py --print-pins` and `check_torch_extra_pins.py`. cibuildwheel builds cp310–cp314, linux x86_64/aarch64 and macOS arm64, and skips musl, i686, win32, and free-threaded.

## Disposition

| File | Disposition |
|---|---|
| `.gitattributes` | clean (scan) |
| `.github/dependabot.yml` | clean (scan: ignores torch/pytorch) |
| `.github/workflows/bench-report.yml` | clean (scan: workflow_dispatch, lane pin, vendor) |
| `.github/workflows/build_wheels.yml` | clean |
| `.github/workflows/ci.yml` | clean (r11d-01 stands; release-gate list matches) |
| `.github/workflows/docs.yml` | clean (scan) |
| `.github/workflows/sanitizer.yml` | clean |
| `.gitignore` | clean (scan: `extern/cfitsio/` ignored) |
| `.pre-commit-config.yaml` | clean |
| `AGENTS.md` | clean (scan) |
| `CLAUDE.md` | clean (scan: includes AGENTS.md) |
| `CMakeLists.txt` | clean (scan: adds `cpp_src`) |
| `LICENSE` | clean (scan: MIT) |
| `README.md` | clean |
| `SDIST-README.txt` | clean (scan) |
| `constraints-wheel.txt` | clean |
| `extern/VERSIONS.txt` | clean (sha256 checked against the tag tarball) |
| `extern/licenses/CFITSIO-LICENSE.txt` | clean (scan: shipped via `license-files`) |
| `extern/patches/cfitsio-4.7.0-bzip2.patch` | clean |
| `extern/patches/cfitsio-4.7.0-plio-cbuf.patch` | clean |
| `extern/vendor.sh` | clean |
| `overrides/home.html` | clean (scan) |
| `overrides/main.html` | clean (scan) |
| `overrides/partials/actions.html` | clean (scan) |
| `packaging/conda/recipe.yaml` | clean (torch pin matches the lane; bzip2 host dep; no conda cfitsio) |
| `pixi.toml` | fixed (r16a-02) |
| `pyproject.toml` | clean |
| `zensical.toml` | clean (scan: nav; docs-contract already builds it) |

## Gate results

- `pixi run pytest tests/test_release_lane.py -q -p no:cacheprovider` → 9 passed. The two new tests failed first (help lacked `1.1.3rc5`; pixi still pinned cfitsio 4.6).
- `pixi run check-lane` → PASS after the pixi edit.
- `pixi run preflight-push` → PASS (ruff, format, mypy 95 files, compileall, check-lane, changelog-check).
- `pixi run test` → 2238 passed, 24 skipped, 21 xfailed, 383 warnings in 474.98s (EXIT:0). +2 over R15's 2236: the two new lane tests.
