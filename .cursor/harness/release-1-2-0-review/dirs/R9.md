# R9 — `cpp_src/` image/file family

Slices: A `dirs/R9-bindings.md` (r9a-*), B `dirs/R9-file.md` (r9b-*). The original slice-B agent
died (provider failure) after its `fits_detail.h` edit landed but before its `fits_file.cpp`
fixes/findings; `R9-FitsFile2` rescued it from the full transcript (five confirmed defects with
captured evidence, re-derived cleanly). Dual-env rebuild per batch under the mutex.

## Totals

19 findings (r9a 13, r9b 06): 12 fixed (4 BLOCKER, 4 MAJOR, 4 MINOR), 5 re-derived/pinned/
verify-only, 2 deferred.

Blockers fixed: r9a-02 `read_full_unmapped` built a 0-d one-element tensor of UNINITIALIZED
memory for NAXIS=0 and returned it as a scalar; r9a-06 CFITSIO's `ffthdu` walk discards its error
status — truncated/hostile-tail files under-reported HDUs with exit 0 at all five scan surfaces
(= r7a-08, `checked_num_hdus` + no-false-positive guards); r9a-07/r9b-01 compressed float RICE
destroyed `Inf` / `-0.0` / subnormals whenever a nulval pointer was set (shared
`cfitsio_float_nulval_ptr` IEEE-aware selection; -0.0 signbit + subnormals pinned bitwise vs
astropy); r9b-02 hostile-NAXIS mmap product/bounds overflow SIGSEGV (rc=-11 repro);
r9b-03 concurrent `FITSFile` operations/close raced (rc=-11 repro with a 3×500 harness) —
persistent-handle ops now serialized; r9b-04 `SubsetReader` open-time snapshot incomplete (it
reacquired the replacement fd after a cache clear) — generation-checked acquisition.

Majors: r9a-04 batch HDU failures named neither path nor HDU (`Error reading <path> HDU <int>`);
r9a-05 empty-`what()` slipped through the batch error vector and non-`std::exception` throws
escaped to `std::terminate` (`record_batch_error`, `catch (...)`); r9b-05 dead `current_fits_hdu`
removed (repo-wide grep clean).

## Re-derived / pinned (candidates refuted or verified — evidence quoted in slice files)

- r9a-03 the plan's `read_full_numpy` unsigned-alignment pre-decision: ALREADY ALIGNED at HEAD,
  pinned bitwise over 11 scaled fixtures × mmap on/off.
- r9a-08 batch corrupt-path contract: already correct at the C++ surface (pinned).
- r9a-09 `tl_cache` uid-bump eviction: SOUND (3-case probe; the remaining in-window staleness is
  the documented 1s validate throttle, cache-layer-agnostic).
- r9a-10/11 GIL-release + overflow-guard verify-only anchors (exact greps recorded for pass 2).
- r9a-12 A-06 image scale accumulation stays float32 — pinned deliberately for the 2.0 float64
  switch (backlog entry recorded).
- r9a-13 astropy silently drops BSCALE/BZERO on write (fixture lesson recorded; genuinely-scaled
  fixtures are handcrafted raw bytes).

## Deferred (→ backlog at R17)

- r9b-06 `open_fits_for_write` hardening: pure defense-in-depth (probe `FDDELTA_FAILING_OPENS 0` —
  CFITSIO leaks no partial handles on failed write-opens).
- 2.0 image-scale float64 switch (r9a-12's pinned backlog entry).

## Evidence

Failing-first per fix (r9b: two rc=-11 subprocess repros, byte-mismatch pins, the snapshot-gap
regression found post-rebuild-1). Passing: owned 30 / slice-A 79 / parity oracle 69 / adjacent
image 114 / explicit pins 5. Integration combined sweep recorded below.

## Plan deviations / integration notes

- Slice A committed separately (`b4ffe66`) before the rescue agent's final rebuild.
- Rebuild mutex had 3 serialized rounds this round (A r1, A r2 for r7a-08, rescue r1+r2) — zero
  concurrent-build windows.
- No integration-owned follow-ups needed (lint/format clean).

## Gate results

- `pixi run preflight-push` → PASS.
- `pixi run test` → PASS (2218 passed, 24 skipped, 21 xfailed, 383 warnings in 450.63s; +79 over R8's 2139). Integration combined sweep: 273 passed.
