# R11-01 — tests chunk 01

Agent `R11-Chunk01`. Chunk: `splits/tests.chunk01` (20 files, `tests/test_compression_matrix.py` through `tests/test_header_duplicate_keys.py`). IDs `r11b-NN`.

No `src/` edits. No performance markers added: nothing in this chunk fails or passes on wall-clock time or RSS. `test_cutout_performance_api.py` checks cutout values, dtypes, and shapes only.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r11b-01 | MINOR | test hygiene | `tests/test_data_datasets.py:test_resolve_local_path_waits_for_inflight_prefetch`, `test_concurrent_resolve_local_path_downloads_once` | Prefetch overlap was ordered with `time.sleep(0.3)` inside the mock download, `time.sleep(0.05)` before `resolve_local_path`, and `time.sleep(0.5)` after it. A slow scheduler can let the prefetch finish before resolve, or let resolve start a second download; the assertion then does not pin the in-flight join. The sibling test slept `0.05s` after starting the second resolver and before `release.set()`, which is the same kind of scheduling race. | Both tests block the mock download on `threading.Event` and release only after the overlap is observed. Prefetch: `started` fires inside `_download`, then `resolve_local_path` must observe the live `torchfits-prefetch` thread (`Thread.is_alive`) before `release`. Concurrent resolve: the second caller must enter `_download_once` before `release`. Both still require `len(calls) == 1`. Neither test calls `time.sleep`. Focused loop before the chunk run: 8/8 `..` (`2 passed`). | fixed |
| r11b-02 | MINOR | test hygiene | `tests/test_data.py:TestFitsImageDataset.test_repr` | `assert "n=8" in repr(ds)` pins the `n=` spelling. Dataset length is already `assert len(ds) == 8` in `test_file_list`. | Dropped only the `n=8` line. `assert "FitsImageDataset" in r` stays. `test_repr` passed in the follow-up `pixi run pytest` of that test plus the two download tests (3 passed). | fixed |
| r11b-03 | MINOR | test hygiene | `tests/test_hdu_file_ops.py:test_external_overwrite_invalidates_cached_handle` | `time.sleep(1.1)` sits between the first read and an external overwrite. It is not a thread-ordering race. `SharedReadMeta` skips re-stat while `(now - last_stat_check_ns) < kSharedMetaValidateIntervalNs`, and that interval defaults to 1000 ms (`TORCHFITS_SHARED_META_VALIDATE_INTERVAL_MS` in `src/torchfits/cpp_src/fits_detail.h`). The following `time.sleep(0.01)` only polls `torch.equal` until a 3 s `monotonic` deadline. | Sleeps kept. Comment above the 1.1 s sleep records the throttle and that the 0.01 s loop is not an ordering sleep. | fixed |

## Marker list

None. No test in this chunk asserts wall-clock time or RSS, so none received `@pytest.mark.performance`.

## Chunk pytest

`pixi run pytest` on the 20 files in `splits/tests.chunk01`, `-q --tb=line`: **354 passed, 6 skipped, 19 warnings in 115.13s**. A second run of the same file list finished **354 passed, 6 skipped, 19 warnings in 134.24s**. Before that, the two download tests were **2 passed** on eight consecutive runs. `test_resolve_local_path_waits_for_inflight_prefetch` does not call `time.sleep`.

## Not changed (reviewed)

- Fixture creates already use `tmp_path`, `tmp_path_factory`, `tempfile.mkdtemp`, `tempfile.mkstemp`, or `tempfile.NamedTemporaryFile`. None write a relative path into the repo cwd. `test_docs_code_snippets.py` chdirs into `mock_fits_workspace` (`tmp_path_factory`), not the repo. `test_examples_runner.py` runs `examples/test_examples.py` with `cwd=ROOT` and asserts exit 0; it does not create a fixture via a relative path.
- `test_data_datasets.py` repr check is the class name only (`"FitsImageDataset" in repr`). `test_data.py` table repr check is the class name only. `test_hdu.py:test_hdu_repr_html` still checks type names, escaped EXTNAME, and the `(2, 3)` shape; those are the HTML contract, not a duplicate of another assertion.
- Compression, dtype, where-clause, checksum, and duplicate-keyword pins were not relaxed or deleted.
- No product bug found that needs an R19 repro.

## Disposition

| File | Disposition |
|---|---|
| `tests/test_compression_matrix.py` | clean; lossless/quantization/fpack pins kept; writes under `tmp_path` |
| `tests/test_concurrent_same_file_read.py` | clean; `mkstemp` plus `teardown_module` unlink; no sleep |
| `tests/test_cutout_performance_api.py` | clean; value/shape/dtype only, no performance marker; `NamedTemporaryFile` / `tmp_path` |
| `tests/test_data.py` | fixed (r11b-02): dropped incidental `n=8` repr pin; class name stays; fixtures are `mkdtemp` / `NamedTemporaryFile` |
| `tests/test_data_datasets.py` | fixed (r11b-01): prefetch and concurrent-resolve overlap use `threading.Event`; `len(calls) == 1` kept; no `time.sleep` |
| `tests/test_data_ml.py` | clean; writes under `tmp_path` |
| `tests/test_differential_astropy.py` | clean; bitwise/astropy pins kept; writes under `tmp_path` |
| `tests/test_dlpack_roundtrip.py` | clean; strided/VLA/table-type pins kept |
| `tests/test_docs_code_snippets.py` | clean; snippet workspace is `tmp_path_factory` |
| `tests/test_docs_integrity.py` | clean; reads repo docs, no cwd fixture writes |
| `tests/test_examples_runner.py` | clean; exit-code pin on `examples/test_examples.py` |
| `tests/test_fits_schema.py` | clean; in-memory schema only |
| `tests/test_fitsio_upstream_smoke.py` | clean; `NamedTemporaryFile`; numeric/dtype pins kept |
| `tests/test_hdu.py` | clean; module fixture uses `tmp_path_factory`; HTML repr contract kept |
| `tests/test_hdu_close_and_overflow.py` | clean; writes under `tmp_path` |
| `tests/test_hdu_file_ops.py` | fixed (r11b-03): 1.1 s sleep kept as the shared-meta validate interval; 0.01 s poll kept |
| `tests/test_hdu_str.py` | clean; `tmp_path` |
| `tests/test_hdu_table_contracts.py` | clean; where/quantize/VLA pins kept; `tmp_path` |
| `tests/test_header_ascii_strictness.py` | clean; ASCII rejection pins kept |
| `tests/test_header_duplicate_keys.py` | clean; first-wins duplicate-key pins kept |
