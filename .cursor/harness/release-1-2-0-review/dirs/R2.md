# R2 — `src/torchfits/transforms/`

Slices: A `dirs/R2-state.md` (r2a-*), B `dirs/R2-stats.md` (r2b-*), C `dirs/R2-maskrgb.md` (r2c-*).

## Totals

40 findings (r2a 17, r2b 12, r2c 11): 36 fixed, 4 deferred.
Blockers fixed (6): r2a-01 double-scaling via `state=` masking payload state; r2a-02 TNullToNan
float32 sentinel compare NaNed valid int rows; r2a-03 FITSScaleColumns float32 vs reader float64
convention; r2a-07 FITSHeaderNormalize inverse-limits cross-thread race; r2b-01 ±inf counted
valid (one +inf pixel wiped clip frames to 0); r2b-02 complex tensors silently lost imaginary
parts.

Majors fixed (18): r2a-04/05/06/08/09 (identity-scaler error swallow; broken inverse round-trip;
`__call__` instance mutation; PayloadView state fabrication; AsModule bypassing state stamping);
r2b-03..r2b-08 (empty-input crashes; GlobalScalarNorm integer truncation; min_tile_pixels never
applied; per-thread `_last_*` caches via `helpers._ThreadedAttr`; zero-batch crashes; f16
threshold underflow → div-by-zero); r2c-03..r2c-09 family (SqrtStretch.inverse int overflow;
int `dtype=` all-zero RGB; empty-path dtype ignore; write_rgb_image NaN→uint8 UB + invalid PNG;
Lupton Qmax=1e10 parity cap; mask int-promotion rounding >2^24; mask_from_dq silent all-valid).

Minors (10) + cleanups (2): see slice files. Notable cleanup derivation: the plan's "unreachable
inf-guard in clip.py" candidate was RE-DERIVED as reachable (all-clipped group → NaN median →
guard maps to 0) and kept; genuine dead code deleted (r2b-11).

## Deferred (→ `.cursor/post-1.0-backlog.md` "1.2 audit deferrals" at R17)

- r2a-12 docs/api-transforms.md mismatches (5 items) — R15.
- r2a-16 redundant `check_state` in FITSHeaderScale.forward — perf evidence rule not met, not landed.
- r2a-17 inverse-state-in-Payload redesign — blocked by frozen dict-output contracts (same family
  as r2b-12). Minimal correct behavior landed (per-(instance,thread) caches, `-J`-safe).
- r2b-12 container-carried inverse stats redesign.

## Evidence

Failing-first outputs pasted in slice files; 50 state pins (18 failed pre-fix), 8 library defects
captured failing in C's two pre-fix runs, stats pins per r2b-10. Combined-tree targeted run
before commit: **534 passed** (transforms suites). Cross-slice coordination: `helpers._ThreadedAttr`
convention shared B↔A; instance-safety e2e pin (C) validated against A's base.py fix.

## Plan deviations / integration notes

- Same `chore(changelog): R2 round fixes` subject convention as R1 (see `dirs/R1.md`).
- Integration-owned follow-ups: `style: ruff pass over R2 tests; widen FITSHeaderNormalize
  header to Mapping` (formatter pass + one mypy-strict variance fix: read-only `dict` →
  `Mapping` widening at `fits_meta.py:463`).
- Commit-subject churn during integration (amended once to avoid a generated "Rgb()" changelog
  bullet) — final subjects authoritative in `git log`.

## Gate results

- `pixi run preflight-push` → PASS (ruff check, ruff format --check 348 files, mypy 95 files,
  compileall, check-lane, changelog-check).
- `pixi run test` → PASS (1755 passed, 24 skipped, 21 xfailed, 385 warnings in 300.71s; +143 net tests over R1's 1612).
