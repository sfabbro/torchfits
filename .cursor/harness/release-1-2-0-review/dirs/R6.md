# R6 — `src/torchfits/_hdu/`

Slices: A `dirs/R6-header.md` (r6a-*), B `dirs/R6-views.md` (r6b-*).

## Totals

22 findings (r6a 9, r6b 13): 18 fixed in-round (+1 fusion half fixed at integration), 4 deferred.

Blockers fixed (5): r6a-01 = **r4b-13 root** — LONGSTRN '&'+CONTINUE chains reassembled at
`HDUList.fromfile` construction via shared `_hdu/card._reassemble_longstr_cards` (open/read_header
agree; >68-char values survive write/insert/replace/delete byte-identically); r6b-03 id()-keyed
columns cache served stale data after id reuse (cache dropped — 23.1µs/100-card recompute,
metadata walk not hot); r6b-04 `TensorHDU.to_tensor()`/`chunks()` reopened `source_path`
BYPASSING the SSRF guard (guard before every reopen); r6b-13 `_refresh_file_view` truncated long
strings on every mutation refresh (wrapped with the shared helper — cross-slice from A).

Majors (8): r6b-01 = r5c-09 to_arrow-family kwarg collision (typed reject + docstrings + pin
flipped); r6b-02 = r5c-15 row-slice dedupe; r6b-05 vla_lengths IO swallow; r6b-06 `head(-n)`
silent tail truncation (ValueError); r6a-02/03 (Header(gen/str/scalar) silent empty header;
AttributeError legacy-fallback reroute).

Minors/CLEANUP (9): r6a-04 stale EXTNAME name cache (wrong HDU returned — live scan); r6a-05
HDUList numpy-int indexing (operator.index + typed TypeError); r6b-07 caller-dict aliasing;
r6b-08 post-close ValueError→typed RuntimeError; r6b-09 dead fallthrough; r6b-10 quantize test
flake made deterministic; r6b-11 DataView A-08 + head() composition VERIFIED fixed at HEAD with
boundary pins (`tests/test_views_dataview_index.py` new).

## Integration-landed (r6a-06)

The r4c-15 wrapper in `_io_engine/hdu_api.py` fused malformed orphan CONTINUE cards into
unrelated keywords (`NAXIS` → `"0= 'orphan segment'"`; failing-first repro captured, green after
the `card._is_string_typed` gate). Landed at integration because the wrapper file was frozen
during the round. Verified across three paths (fromfile/open/read_header).

## Deferred (→ backlog at R17)

- r6a-06 residual: `HDUList` header values carry the raw batch-triple STRING convention
  (`'7'` str) while `read_header` types them (7 int) — PRE-EXISTING and uniform (verified on
  files without long strings; batch triples carry raw quoted strings; existing tests accommodate
  with `int(...)` wraps). Unifying the typing conventions is API-visible → backlog redesign;
  docs faithfulness check for R15 (what do `docs/api*.md` claim about `header.get` types?).
- r6a-07 `read_header_fast` slow fallback + `_read_pipeline_fallback.py Header(...)` sites build
  from un-reassembled triples on error/option paths (all 6 public routes verified joined);
  `card._reassemble_longstr_cards` is import-ready for a follow-up.
- r6b deferred pair: `TableHDURef` `num_rows`/`_is_ascii_table` broad excepts +
  `_refresh_file_view` error-path close swallow (zero-observable narrowing); `TableHDURef.head(-n)`
  keeps view tail-truncate semantics (assignment scoped ValueError to the in-memory class;
  symmetric contracts → API review).

## Verified fixed at HEAD (candidates re-derived, no finding)

- r6b-12 `TableHDU._cached` is sound (`(id(header), Header._version)` + strong-ref guard;
  `Header._version` bumps on every mutator — the planned `Header._mutation_seq` fallback was NOT
  needed; no cross-slice Header change landed).
- DataView A-08 IndexError + `head()` window composition already correct at HEAD (boundary pins
  added).
- A-01 numpy-scalar header round-trip coverage present (r6a pins extended).

## Evidence

Failing-first per fix pasted in slice files (A: 6+3 failing captures; B: per-row before/after).
Combined-tree verification before commit: 429 passed, 3 skipped (25 suites incl. output_parity,
writing, write_fidelity, hdu_file_ops, differential_astropy); post-integration chain suites:
137 passed.

## Plan deviations / integration notes

- `chore(changelog): R6 round fixes` subject convention (as R1-R5).
- Integration-owned follow-ups: `style: ruff pass over R6 code` + card.py signature typing
  (tuple/Sequence generics) — zero-behavior.

## Gate results

- `pixi run preflight-push` → PASS.
- `pixi run test` → PASS (1969 passed, 24 skipped, 21 xfailed, 381 warnings in 295.55s; +29 over R5's 1940; the r5c-09 strict pin flipped to a normal contract test).
