# R3 slice B — `src/torchfits/data/remote.py`, `src/torchfits/data/__init__.py`

Agent `R3-RemoteData` · baseline HEAD `c725ea6` (worktree shared with R3-Datasets, who owns `data/datasets.py`; no commits made).
Finding prefix: `r3b`. Depth: full review of both files (download state machine `.partial` → promote → cache, prefetch/join concurrency, `make_loader`, `FitsTableDataset`/`FitsTableIterableDataset`/`FitsCutoutDataset`/`fits_collate_fn`, sharding math, docstrings/env vars). R1's fetch/guard lines (`http_open` pinned per-hop re-guard call, `_read_resume_validators` narrowing) reviewed around, not re-edited.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r3b-01 | MAJOR | 4 Concurrency (shared `.partial` race) | `data/remote.py:_download_vos` | On platforms without `fcntl` the cross-process lock is a no-op and writer isolation is supposed to come from unique per-attempt temp names (`_cross_process_download_lock` docstring), but `_download_vos` hard-coded the shared canonical `dest.suffix + ".partial"` name: two unsynchronized downloaders (e.g. DataLoader worker processes on Windows) unlink/write/promote the same in-flight file — one promotes a mixed/stolen partial or dies with `FileNotFoundError`/`OSError: vos copy produced empty file`. Repro: `tests/test_remote_resume.py::test_concurrent_vos_downloads_use_private_temporaries` (two threads, barrier-synchronized `vos.Client.copy`) — before: `AssertionError: assert [FileNotFoundError(2, 'No such file or directory')] == []` (the peer's `tmp.replace` stole the shared temp). | `tmp = _partial_path_for(dest)` (same helper `_download_http` uses; fcntl keeps the canonical resumable name, no-fcntl gets `pid.tid`-unique names) + per-attempt cleanup (r3b-04). Test fails before / passes after (`1 passed`); no `*.partial*` remains. | fixed |
| r3b-02 | MAJOR | 1 Silent data loss / poisoned cache | `data/remote.py:_download_http` | A zero-byte response body with connection-close framing (no Content-Length, not chunked) was silently promoted to the permanent cache (`wrote > 0` gated the warning too): `resolve_local_path` then trusts any existing file forever, so a connection dropped before the first chunk became a permanent empty "FITS file" — silent wrong result for every later consumer. Repro: `tests/test_remote_resume.py::test_empty_body_is_not_promoted_to_cache` — before: `Failed: DID NOT RAISE OSError` (0-byte dest created). | Fresh-download empty body (`not append and wrote == 0`) raises typed `OSError(f"{url}: empty download")` and removes tmp+meta — mirrors `_download_vos`'s existing "vos copy produced empty file" contract. Test fails before / passes after (`1 passed`). | fixed |
| r3b-03 | MAJOR | 7 Edge inputs / 5 Error contract (wedged resume) | `data/remote.py:_download_http` | A crash between the last byte written and `.partial → dest` leaves a *complete* partial; the next attempt resumes with `Range: bytes=<size>-` which the server answers **416** (offset at EOF). The bare `urllib.error.HTTPError` propagated and the partial was kept, so every later attempt retried the same doomed Range — permanent, deterministic failure for that URL until manual cache surgery. Repro: `tests/test_remote_resume.py::test_416_on_resume_restarts_instead_of_wedging` — before: `urllib.error.HTTPError: HTTP Error 416: Range Not Satisfiable` (retry wedges identically). | On `HTTPError 416` when a resume `Range` was sent: drop the dead partial + validator meta and re-fetch clean (bounded recursion — the retry never sends `Range`, so it cannot 416-loop; a 416 on a plain GET still raises). Test fails before / passes after (`1 passed`); asserts request 1 carried `Range: bytes=N-` and request 2 carried none. | fixed |
| r3b-04 | MAJOR | 3 Lifetime/RAII (leak on error path) | `data/remote.py:_download_http, _download_vos` | Token-named per-attempt temporaries (`_partial_path_for` when `_fcntl is None`, plus their `.meta`) leaked on every failed transfer — they are unique per attempt and can never be resumed, so they accumulate forever in the cache dir on no-fcntl platforms. Repro: `tests/test_remote_resume.py::test_failed_download_leaves_no_temporaries_without_fcntl` (mid-body `read()` raises `OSError`, response carries ETag so `.meta` is written) — before: `assert [PosixPath(...partial.meta), PosixPath(...partial)] == []` (2 leftovers). | `finally` cleanup of tmp+meta whenever `_fcntl is None` (no-op on success: tmp already promoted, meta removed). fcntl canonical partials deliberately KEEP resume state — pinned by `test_failed_download_keeps_resume_state_with_fcntl` (green before and after). Both fail-first/fail-after pairs recorded in the test file. | fixed |
| r3b-05 | MINOR | 5 Error contract (hostile headers) | `data/remote.py:_download_http` | Malformed/duplicated `Content-Length` (`"not-a-number"`, `"12, 12"`) crashed the download with a raw `ValueError: invalid literal for int()...` with no URL context — while `http.client` itself treats a malformed length as unknown framing and reads to EOF. Repro: `tests/test_remote_resume.py::test_malformed_content_length_treated_as_unverifiable` — before: `Failed: DID NOT WARN. No warnings of type (<class 'RuntimeWarning'>,) were emitted` (ValueError escaped first). | Length parsed leniently: on `ValueError`, `expected = None` → falls into the existing connection-close framing path (warn + promote + completeness unverifiable). Warning text generalized to "server sent no usable Content-Length" (covers omitted and malformed). Test fails before / passes after (`1 passed`). | fixed |
| r3b-06 | MAJOR (perf) | Perf 2 Redundant IO (double download) | `data/__init__.py:make_loader` | `make_loader`'s `optimize_cache` branch pre-fetched every remote in `dataset.files` into the shared remote cache (`prefetch_urls` + blocking `resolve_local_path`, cache_dir=None) — but `FitsStagedCutoutIterableDataset` stages its **own** copies under `staging_dir`/`ephemeral_scratch_dir()` and never reads the remote cache: each staged remote was downloaded **twice** (once to `~/.cache/torchfits/remote/`, dead weight forever; once to staging) and `make_loader` blocked for the useless first copy. Repro: `tests/test_staged_prefetch.py::test_make_loader_staged_remote_downloads_once` — before: `AssertionError: [...('…/remote_cache/b93a8…fits'), ...('…/scratch/b93a8…fits')] assert 2 == 1`. | Exclude `FitsStagedCutoutIterableDataset` from the full-file branch alongside `FitsCutoutDataset` (comment updated). **Evidence rule (perf):** deterministic count proof downloads per URL 2→1 (test above) + throwaway same-host micro-timing (`/tmp/micro_make_loader_r3b06.py`, 200 ms/GET mock latency, `make_loader` + first epoch, 5 reps, medians): BEFORE 405 ms (2 downloads) → AFTER 203 ms (1 download). Regression pin for the map-style path (unchanged): `tests/test_data.py::TestMakeLoader::test_remote_files_download_once` — prefetch + resolve share exactly one fetch (green before and after). | fixed |
| r3b-07 | MINOR | 8 Docs/API faithfulness | `data/__init__.py:make_loader` (docstring), `_shard_row_range` (docstring) | (a) `make_loader` docstring claimed `optimize_for_dataset` "pre-warm[s] handle and file caches" — the handle cache no longer exists (R1 cross-file note, `data/__init__.py:616-618`) and `optimize_for_dataset` only tunes policy hints (`cache.py:278-290`); `avg_file_size_mb` "used for cache sizing" was imprecise (it drives the fits-in-disk-cache heuristic). (b) `_shard_row_range` docstring typo "this is even even when". | Docstrings rewritten to the truth (download warm-up + policy hints; cutout exclusion; `avg_file_size_mb` estimates whether the dataset fits the disk cache); typo fixed. Validation: `pixi run pytest tests/test_docs_integrity.py tests/test_data_ml.py -q` → `57 passed` (no doc-sync or signature-fence coupling to the changed text). | fixed |
| r3b-08 | BLOCKER | 3/4 Lifetime + concurrency (cleanup race) | `data/datasets.py:FitsStagedCutoutIterableDataset._generate` (:1477 `cleanup_downloaded_file(local_path)`; src OUTSIDE slice B's file set) | Plan candidate "staged-cutout cleanup races" re-derived: REAL. `cleanup_downloaded_file` unlinks the staged file unconditionally after one iterator finishes it, while `resolve_local_path` only existence-checks before returning the path and `open_subset_reader`/`cpp.SubsetReader` opens it a few hundred µs later. Two concurrent consumers of the same remote + shared staging dir (default `ephemeral_scratch_dir()` is one shared `TMPDIR/torchfits_staging`; two datasets/threads in one process, or two jobs sharing `TMPDIR`) → the deleter yanks the file inside the other's resolve→open window → mid-epoch death. Open fds/mmaps survive POSIX unlink and Windows unlink fails safely (`cleanup_downloaded_file` returns False), so the window is narrow but hit eventually with default `cleanup=True`. **Deterministic repro landed (non-strict xfail):** `tests/test_staged_prefetch.py::test_concurrent_staged_iterators_do_not_yank_each_others_files` wedges iterator A's first `open_subset_reader` until iterator B has completed (and deleted) the shared staged file — `--runxfail` shows `AssertionError: concurrent cleanup yanked A's staged file: [RuntimeError('Could not open FITS file: …/scratch/<digest>.fits')]`. Same-thread interleaving is provably safe (no yield between resolve and open), so thread/process-unique staging identity or delete-only-what-I-created both fix it. | Fix site is `datasets.py` (slice A's exclusive file) — HANDED to `R3-Datasets`, who confirmed the fix shape in writing: per-iterator private staging dir `<staging_root>/it-<pid>-<uuid8>` scoped to `cleanup=True` (deletion is the only hazard, so `cleanup=False` keeps the shared root and its cross-epoch reuse), per-file cleanup on private paths, duplicate-URL delete-skip, empty private dir rmdir at generator close. I did not edit `datasets.py`. **Related contract change flagged by R3-Datasets (their file):** `_resolve_rank_and_world_size` raises `ValueError` for `rank >= world_size` / negative rank (previously such args silently mis-shard); `FitsTableIterableDataset._row_window` inherits the raise — no slice B test relies on the old clamping (all use `rank < world_size`; `_shard_row_range` is exercised only with valid slots). | **fixed by R3-Datasets** in `datasets.py` (per-iterator `mkdtemp(prefix="it-", dir=root)` staging scoped to `cleanup=True` + `rmtree` at generator close; `cleanup=False` keeps the shared root and cross-epoch reuse). Verified in the shared worktree with the pinned repro: `--runxfail` failed before with `RuntimeError('Could not open FITS file: …/scratch/<digest>.fits')`, passes after (`1 passed`); xfail marker removed — plain regression test now in `tests/test_staged_prefetch.py`. Post-fix targeted suite `95 passed`. | fixed |
| r3b-09 | MINOR | 5 Error contract (broad except on IO) | `data/__init__.py:_eager_table_columns` | `except Exception` around `cpp.read_fits_table` falls back to pyarrow for any failure, including IO errors (`table_api`-class issue; R5's exact-semantics decision names the same pattern for `_table/`). Not fixable by type-narrowing here: the extension raises bare `RuntimeError` for BOTH IO ("Failed to open FITS file") and schema problems ("Column not found: NOSUCH") — decoding them needs `str(e)` matching (forbidden) or a typed decode exception from the C++ layer (R5/R8 scope). Not silent today: the fallback re-runs the read and its error propagates (verified: missing file and unknown column both raise `RuntimeError` from either engine). | Deferred to the R5 typed-decode-exception standard; when that lands, narrow the clause here identically. No behavior change made. | deferred |

## Verified at HEAD — reviewed, no defect (evidence)

- **Resume-validators state machine (plan candidate "resume without If-Range/ETag"): fixed before this round and pinned.** A leftover partial with no/blank validators is discarded, never appended to (`test_download_http_partial_without_validator_is_discarded`); a validator mismatch (If-Range → 200) restarts from scratch, no hybrid bytes (`test_download_http_stale_partial_with_validator_restarts_cleanly`); matching validator appends exactly (`test_download_resume_appends_partial`); incomplete 206 rejects and keeps the prefix (`test_download_resume_rejects_incomplete_206`) — all in R1's `tests/test_remote_http_range.py`, green over my code (`94 passed` full run below). r3b-03 closes the remaining wedge in the same state machine.
- **Content-Length-less truncation promoted (plan candidate): honest at HEAD.** Unverifiable completeness promotes with an explicit `RuntimeWarning` naming the URL ("completeness could not be verified") — the documented trade-off for connection-close-framed servers; pinned by `test_body_without_content_length_promotes_with_warning`. The silent case (empty body) is now refused (r3b-02).
- **Multiprocess `.partial` race (plan candidate): correct at HEAD.** `_download_once` = per-key thread lock + `flock` + `if dest.is_file(): return` recheck under the lock; canonical partial shared only under flock; `_download_locks` deliberately never popped (race documented in `_cleanup_cache_key`). Pinned by `test_resolve_local_path_multiprocess_single_download` (6 processes, exactly 1 GET) + `test_concurrent_resolve_local_path_downloads_once` / `test_resolve_local_path_waits_for_inflight_prefetch` (in `tests/test_data_datasets.py`), all green. r3b-01 was the one remaining hole (vos path, no-fcntl).
- **Sharding math (round emphasis; code lives in `data/__init__.py`):** `_shard_row_range` is an exact partition — sizes differ by ≤1, ranges tile `[0, total_rows)` with no gap/overlap; `total_slots > total_rows` yields empty windows (no crash, no duplicate rows: rows 0..n-1 to slots 0..n-1, the rest empty); `_row_window` maps `slot_id = rank * num_workers + worker_id` over `total_slots = world_size * num_workers`. Covered by `tests/test_data_ml.py::test_shard_ranges_are_contiguous_and_even` / `test_rank_shards_partition_every_row_exactly_once` / `test_where_shards_cover_every_matching_row_once` (green over my `__init__.py`; slice A owns those tests). `FitsTableIterableDataset` has **no** `__len__` (IterableDataset protocol) — yielded-count agreement is what exists and is asserted (`test_yields_all_rows`, multi-worker `test_multiprocess_table_iterable_sees_all_rows`); no `len()` to disagree. No defect found; no source change.
- **Env vars documented in these files all exist in `src/` (bug class 8):** `TORCHFITS_CACHE_DIR` (`cache.py:54`), `TORCHFITS_REMOTE_CACHE` (`cache.py:65`), `SLURM_TMPDIR`/`TMPDIR` (`remote.py:64`) — resolution claims in the module/`ephemeral_scratch_dir` docstrings match `cache_root()`/`remote_cache_root()` exactly. `data/__init__.py` documents no env vars.
- **`read_images_batch` / `NAXIS2→0` swallow (round directive):** `grep -n "read_images_batch\|NAXIS2" src/torchfits/data src/torchfits/_io_engine` — no occurrence in slice B's files (`data/remote.py`, `data/__init__.py`); the swallow sites are in `_io_engine/` → recorded for R4, untouched here.
- **vos_uri / CANFAR staged paths:** `is_vos_path`/`normalize_vos_uri` delegation is honest (R1 verified); `_download_vos` raises a typed `ImportError` naming the optional package, refuses empty copies (mirrored by r3b-02 on the HTTP side), and promotes via atomic `tmp.replace`. The only defect was the shared temp name (r3b-01).
- **`fits_collate_fn` / `CutoutSpec` / `FitsCutoutDataset`:** collate raises `ValueError` naming a custom `collate_fn` for non-tensor columns (VLA/list columns included) and `TypeError` for unsupported sample types — matches the docstring; `CutoutSpec` half-open bounds documented and consistent with `read_subset`. No defect.

## Doc text owed at R15 (verbatim; docs/ not edited this round)

1. `docs/api-data.md` (`make_loader` prose near :69-73 and the params table :488-489):
   > When the dataset exposes a `files` list, remote files are fetched **once** into the shared on-disk cache (`TORCHFITS_REMOTE_CACHE`) and `cache.optimize_for_dataset(paths, avg_file_size_mb=…)` tunes the cache policy hints. Cutout datasets are excluded from whole-file prefetch: `FitsCutoutDataset` streams HTTP byte ranges and `FitsStagedCutoutIterableDataset` stages its own ephemeral copies under `staging_dir` (`SLURM_TMPDIR`/`TMPDIR` scratch), so `make_loader` downloads each of their remotes exactly once.
   Table row: `| avg_file_size_mb | float | 10.0 | Average file size estimate for the fits-in-disk-cache heuristic |`.
2. Behavior changes to note in the changelog (`Fixed`): empty HTTP downloads are refused instead of cached (r3b-02); a resume offset past EOF (HTTP 416) self-heals by re-fetching instead of wedging (r3b-03); `vos:` downloads no longer share an in-flight temp file across unsynchronized writers (r3b-01); `make_loader` no longer double-downloads staged remotes (r3b-06).

## Validation

Targeted runs (this host, pixi default env):
- Failing-first (against unfixed HEAD worktree): `pixi run pytest tests/test_remote_resume.py -q` → `5 failed, 3 passed` with the exact failures pasted in the table; `pixi run pytest tests/test_staged_prefetch.py::test_make_loader_staged_remote_downloads_once -q` → `assert 2 == 1`.
- After fixes: `pixi run pytest tests/test_remote_resume.py tests/test_staged_prefetch.py tests/test_data.py tests/test_remote_http_range.py -q` → **`94 passed, 6 warnings in 16.10s`** (warnings are the pre-existing `fork()`-in-multithreaded-process notice from R1's multiprocess test).
- Per-fix after-proof: `6 passed in 1.65s` (`test_416_on_resume_restarts_instead_of_wedging`, `test_empty_body_is_not_promoted_to_cache`, `test_malformed_content_length_treated_as_unverifiable`, `test_failed_download_leaves_no_temporaries_without_fcntl`, `test_concurrent_vos_downloads_use_private_temporaries`, `test_make_loader_staged_remote_downloads_once`).
- Doc/sibling safety: `pixi run pytest tests/test_docs_integrity.py tests/test_data_ml.py -q` → `57 passed`; `pixi run pytest tests/test_data_datasets.py -q` → `14 passed` (slice A's remote-prefetch pins stay green over my `remote.py`).
- r3b-08 repro (cross-file, fix by R3-Datasets in `datasets.py`): `--runxfail` → `1 failed` with `RuntimeError('Could not open FITS file: …/scratch/<digest>.fits')` pre-fix (deterministic, predicted mechanism) → `1 passed` post-fix (`_stage_root` private staging dirs); xfail marker removed.
- Perf evidence (r3b-06): count proof in the regression test (2→1) + `/tmp/micro_make_loader_r3b06.py` micro-timing medians 405 ms → 203 ms (same host, 5 reps, 200 ms/GET mock).
- Final targeted suite (all fixes + the r3b-08 regression test plain): `pixi run pytest tests/test_remote_resume.py tests/test_staged_prefetch.py tests/test_data.py tests/test_remote_http_range.py -q` → **`95 passed, 6 warnings in 15.89s`**.

Test files touched (owned): `tests/test_remote_resume.py` (new, 8 tests), `tests/test_staged_prefetch.py` (+2: make_loader count proof, r3b-08 regression test), `tests/test_data.py` (+1). No edits outside `src/torchfits/data/remote.py`, `src/torchfits/data/__init__.py`, and these three test files.

## Per-file disposition

| file | depth | finding IDs | status |
|---|---|---|---|
| `src/torchfits/data/remote.py` | deep — full download state machine (partial→promote→cache), prefetch/join concurrency, lock/temp naming, vos path, all docstrings; R1 fetch/guard lines reviewed around, untouched | r3b-01, r3b-02, r3b-03, r3b-04, r3b-05 | fixed |
| `src/torchfits/data/__init__.py` | deep — `make_loader`, `FitsTableDataset`, `FitsTableIterableDataset` (+`_shard_row_range`), `FitsCutoutDataset`, `fits_collate_fn`, `_eager_table_columns`, `__all__` | r3b-06, r3b-07, r3b-09 | fixed (r3b-09 deferred) |

Out-of-set pointer for the LEDGER: `src/torchfits/data/datasets.py` — r3b-08 (staged-cutout cleanup race) fixed there by `R3-Datasets` (per-iterator staging identity, scoped to `cleanup=True`), verified with slice B's pinned repro before/after; attribute the fix commit to their round code.
