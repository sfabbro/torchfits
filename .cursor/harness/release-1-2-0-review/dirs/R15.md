# R15 — docs/

Slices: `dirs/R15-00.md` (first 23 paths), `dirs/R15-01.md`, `dirs/R15-02.md`. No `src/` edits. `scripts/release_lane.py` still says `rc5 -> 1.0.0rc5` in its `--prerelease` help; that string is R16.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r15a-01 | MAJOR | docs/API faithfulness | `docs/compatibility.md` | The MPS bullet said no warning is emitted on float64/complex128 downcast. The debug-knobs bullet called the `TORCHFITS_*` inventory undocumented. `to_device` warns with the two `UserWarning` strings in `_io_engine/device.py`. | Both warning strings are quoted. Runtime knobs point at `architecture.md#environment-variables`. `KMP_DUPLICATE_LIB_OK` setdefault and the CFITSIO residual TOCTOU are stated. Image float32 vs table float64 was already present. | fixed |
| r15a-02 | MINOR | docs/API faithfulness | `docs/architecture.md` | The image-function list named `read_full_numpy` / `read_full_numpy_cached` without the unsigned bitwise-parity contract pinned in `tests/test_read_full_numpy_parity.py`. | The list now says those two are a bitwise match of `read_tensor(...).numpy()`, including unsigned `BZERO` values that stay integers. | fixed |
| r15a-03 | MINOR | docs/API faithfulness | `docs/api-tables.md` | `where=` docs did not state SQL three-valued logic. NaN / `None` rows are excluded from comparisons and from `NOT`, so `NOT (X == v)` matches `X != v`. | Paragraph after the `==` admonition. `== NULL` stays rejected; use `IS NULL` / `IS NOT NULL`. | fixed |
| r15a-04 | MAJOR | docs/API faithfulness | `docs/cli.md`, `docs/examples.md` | `cutout` said the write preserves WCS reference pixels. `_cutout_one` copies the source header; `CRPIX*` is not shifted. | CLI section says the header is a copy and `CRPIX*` is not shifted. The WCS example says the caller translates `CRPIX` and that torchfits does not own a WCS engine. Behavior unchanged. | fixed |
| r15a-05 | MINOR | docs/API faithfulness | `docs/release.md` | The stamp command was `pixi run changelog-release -- 1.1.0`. The pixi task forwards extra args; bare `changelog-release` fails argparse because `--release` requires `VERSION`. | Documented as `pixi run changelog-release -- <version>`. The pixi task itself is an R16 fix. | fixed |
| r15a-06 | MINOR | docs/API faithfulness | `docs/api-tables.md` | `quantize=` was in the signature table but not the failure contract: ASCII tables raise `ValueError`, and a request that matches no float column raises `QuantizeError` instead of writing unpacked. | Two sentences under the parameter table, matching `torchfits.table.write`. | fixed |
| r15a-07 | MINOR | docs/API faithfulness | `docs/api.md` | `TableHDU.head` and `TableHDURef.head` disagree on negative `n` and neither contract was on the HDU page. | `TableHDU.head(n)` requires `n >= 0` (`ValueError`). `TableHDURef.head(n)` counts a negative `n` from the end of the current row window. | fixed |
| r15a-08 | MINOR | docs/API faithfulness | `docs/release.md` | The 2.13 lane row said torchfits **1.1.0**, and the prerelease example rendered `1.0.0rc5`. `scripts/torch_lanes.json` has `torchfits_version` `1.1.3`; `--prerelease rc5` appends to that base. | Table cell is **1.1.3**. The example no longer names `1.0.0rc5`. `test_owed_behavior_notes_match_the_implementation` reads the lane file. | fixed |

## Already matching (not changed)

- CLI exit `5` and `stats`/`table` JSON `null` for non-finite floats were already in `docs/cli.md`.
- `read_batch(..., strict=False)` in `docs/api-core-io.md` matches the default that skips failures.
- Env tables in `docs/architecture.md` are the inventory. `test_architecture_md_env_tables_cover_every_source_getenv` already requires every source `TORCHFITS_*` read. Not copied into `compatibility.md`.
- Mermaid labels in `docs/*.md` have no unquoted special characters.
- Run-ids cited from `docs/benchmarks.md` and `README.md` are directories under `docs/assets/bench/`.
- `docs/parity.md` Supported rows were not re-executed this pass. Evidence gaps belong in the R18 REPORT.

## Disposition

| File | Disposition |
|---|---|
| `docs/api-core-io.md` | clean (scan: `read_batch` strict default) |
| `docs/api-data.md` | clean (scan) |
| `docs/api-tables.md` | fixed (r15a-03, r15a-06) |
| `docs/api-transforms.md` | clean (scan) |
| `docs/api.md` | fixed (r15a-07) |
| `docs/architecture.md` | fixed (r15a-02) |
| `docs/assets/bench/**` | clean (scan: cited run-ids exist) |
| `docs/assets/gallery/**` | clean (scan: existence) |
| `docs/assets/katex/**` | clean (scan: existence) |
| `docs/benchmarks.md` | clean (scan: run-ids) |
| `docs/changelog.md` | clean (generated bullets only) |
| `docs/cli-recipes.md` | clean (scan) |
| `docs/cli.md` | fixed (r15a-04) |
| `docs/compatibility.md` | fixed (r15a-01) |
| `docs/contributing.md` | clean (scan) |
| `docs/denoise-pipeline.md` | clean (scan) |
| `docs/examples-ml.md` | clean (scan) |
| `docs/examples-transforms.md` | clean (scan) |
| `docs/examples.md` | fixed (r15a-04) |
| `docs/index.md` | clean (scan) |
| `docs/install.md` | clean (scan: `KMP_DUPLICATE_LIB_OK` already documented) |
| `docs/javascripts/katex.js` | clean (scan) |
| `docs/logo.svg` | clean (scan) |
| `docs/migration_astropy.md` | clean (scan) |
| `docs/migration_fitsio.md` | clean (scan) |
| `docs/parity.md` | clean (scan: Supported rows not re-proven; R18 REPORT) |
| `docs/python-workflows.md` | clean (scan) |
| `docs/quickstart.md` | clean (scan) |
| `docs/release.md` | fixed (r15a-05, r15a-08) |
| `docs/roadmap.md` | clean (scan) |
| `docs/stylesheets/extra.css` | clean (scan) |
| `docs/torchfits-logo-hero.png` | clean (scan) |
| `docs/torchfits-logo-mark.png` | clean (scan) |
| `docs/torchfits-logo.png` | clean (scan) |

## Gate results

- `pixi run pytest tests/test_docs_integrity.py::test_owed_behavior_notes_match_the_implementation` → 1 passed. First run failed on `No warning is emitted` before the compatibility edit.
- `pixi run docs-contract` → PASS (zensical clean, 27 integrity/isolation tests) before the lane-table follow-up. The follow-up re-ran the owed-notes test only.
- `pixi run docs-links` → PASS. No broken local links and no first-party 404s. Warnings left as-is: `fonts.gstatic.com` 404, gnuastro timeout, legacysurvey 400.
- `pixi run preflight-push` → PASS after both changelog commits.
- `pixi run test` → PASS (2236 passed, 24 skipped, 21 xfailed, 383 warnings in 476.45s). +1 over R14's 2235: `test_owed_behavior_notes_match_the_implementation`.
