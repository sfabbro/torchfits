# R20 — second-opinion defect hunt

Pass 3. Differential checks against astropy (and fitsio for TDIM), hostile headers (duplicate TTYPE, NaN predicates, BLANK/BSCALE, unsigned images, VLA, ASCII), and a re-read of every still-open `## 1.2 audit deferrals` item. No new public symbols.

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r20a-01 | BLOCKER | 1 | `_read_where.py:_cmp_mask,_eval`; `table_reader.h` float/double `FilterOp::NE` | `X != 1` on `[1, NaN, 3, NaN, 5]` kept the NaN rows when the mmap scan or `backend="cpp"` ran, and dropped them on the torch mask. `NOT (X == 1)` kept them on every engine. Arrow's `not_equal` follows IEEE (`NaN != x` is true), and `NOT` inverted a comparison that had already filled unknown to false. Docs require SQL three-valued logic: both forms leave NaN out, and the engines must agree. Before: `test_float_neq_and_not_exclude_nan_on_every_strategy` saw mmap=True select ids `[1, 2, 3, 4]`. | Float/double `!=` requires `val == val`. Arrow comparisons AND out `is_nan`. `NOT` of a comparison drops rows whose referenced columns are null or NaN. | fixed |
| r20a-02 | BLOCKER | 1 | `table_bindings.cpp:tensor_map_to_python,table_result_to_python` | A full read of a repeated `TTYPE` inserted every column into a dict, so the later column replaced the first (`A` was `[3, 4]`). `columns=["A"]` and `update_rows` already keep the first card (`A` was `[1, 2]`). The same name returned different values. Before: `test_duplicate_ttype_column_is_not_null` expected the second column; a projection probe returned the first. | The dict keeps the first name. The duplicate-name tests now expect `[1, 2]`, and `test_duplicate_ttype_projection_matches_full_read` pins the agreement. | fixed |

## Re-derived, not changed

- Vector columns stay `(N, repeat)`. `docs/api-tables.md` says so. A `TDIM=(3,2)` column is `(4, 6)` here and `(4, 2, 3)` in astropy and fitsio. Reshaping would break that contract. Not a defect against the documented shape.
- These probes matched astropy: uint16 image, `BLANK` plus `BSCALE`/`BZERO`, logical/complex/bit columns, ASCII table, Q-format VLA, table write/read of uint16/bool/string/NaN, row slices `(0,3)` / `(7,10)` / empty, int64 image `BSCALE=2` on `{2**62, -2**62, 3}` (delta 0). `X NOT IN` and `X NOT BETWEEN` leave NaN rows out on mmap on/off and both backends.
- Column names in `where=` stay case-sensitive (`Mag` matches, `mag` does not). `TTYPE` values are case-sensitive.
- r5a-16 stays deferred. `_iter_tfields_indexed` still skips a missing `TTYPE`; the read path restores `COL<i>` (`test_empty_result_preserves_unnamed_columns`). The residual is metadata consumers, not a dropped data column.
- Still-open class-1 deferrals, each already explicit: image `BSCALE`/`BZERO` accumulation stays float32 (2.0 float64 path; `docs/compatibility.md`); r7c-19 `BITPIX=64` multiply stays in int64 and can wrap; r7c-25 non-finite pixels cast to an integer output are platform-dependent. None of these is an unrecorded silent result.
- r6a-06 typing split (`NAXIS` is `"0"` on `HDUList`, `0` on `read_header`) stays deferred. r7c-21 `{"HISTORY": ["h1", "h2"]}` still raises `RuntimeError` from `std::bad_cast`. r8b-08 GIL across table writes stays deferred. r12a-05 throughput accounting stays deferred.

## Gate

- `pixi run preflight-push` → PASS (426 files already formatted, mypy 95 files, check-lane, changelog-check).
- `pixi run test` → 2241 passed, 24 skipped, 21 xfailed, 383 warnings in 476.64s (EXIT:0). +2 over R19's 2239: `test_float_neq_and_not_exclude_nan_on_every_strategy` and `test_duplicate_ttype_projection_matches_full_read`.
