# R7 — `src/torchfits/cli/`

Slices: A `dirs/R7-core.md` (r7a-*), B `dirs/R7-tablestats.md` (r7b-*), C `dirs/R7-arith.md` (r7c-*).

## Totals

48 findings (r7a 10, r7b 13, r7c 25): 35 fixed, 3 verified-correct + pinned (r7c-13/14 + r7a-02
half), 10 deferred/recorded.

Blockers fixed (6): r7b-01 diff computed float64 means through float32 → different files reported
identical; r7c-01 `convert` never refused OUTPUT == INPUT (png case replaced the FITS source with
PNG bytes, exit 0); r7c-02 integer arith mul wrapped silently in int64; r7c-03 setkey replayed
full headers duplicating HISTORY/COMMENT on every edit (setkey-no-rewrite strengthened to
fits_update_key/fits_delete_key per-card edits); r7c-04 `--rename X=X` deleted keys and `A=B`
clobbered existing B. (+ r7b/r7c majors as listed in slice files.)

## Plan decisions landed (with failing-first evidence)

1. Unexpected non-`CliError` → traceback to stderr + **exit 5** (`EXIT_INTERNAL`) + the new
   `docs/cli.md` exit-table row (r7a-01; pre-fix: 15/15 matrix rows failed).
2. `KeyboardInterrupt` → **130** everywhere, never 2 (r7a-02: re-derived already-correct + pinned
   ×15 + real-SIGINT subprocess; pre-dispatch gap hardened).
3. `stats`/`table` `-f json`/`jsonl` non-finite floats → `null` (r7b-02/07 + r7a-03 `_json_safe`
   numpy handling; empty-region stats → `json.loads` yields None; verbatim docs note landed).
4. `compress`/`convert`/`decompress` same-path refusal verbatim (r7c-01 fixed for `convert`;
   compress/decompress already refused at HEAD — pinned r7c-13).
5. `-J` per-file transform instances — re-derived ALREADY correct at HEAD (fresh
   `_build_transform` per file in the worker lambda + `_ThreadedAttr` state) and pinned (r7c-14).
6. 68-row exit matrix over all 15 commands (`tests/test_cli_exit_matrix.py`, r7a-06).
7. `cli-j-vs-J` (workers cap ATen at 1) and `is_remote_path` ftp verified (each slice).

## Deferred / recorded (→ backlog at R17)

- r7a-08 truncated/hostile headers silently under-report HDUs with exit 0 (engine HDU-scan locus)
  → routed to R8/R9.
- r7a-09 `_probe_vos` handle.close() swallow (finally re-raise would mask the primary).
- r7b-12 json_default stringifies bytes/complex (lossy but valid — serialization policy).
- r7b-13 `cmds_copy._copy_remote` leaves partial OUTPUT on failed transfer (matches shutil
  semantics; exit 3 signals).
- r7c-15 `arith -o <same>` permits in-place rewrite while copy/compress/transform refuse
  (policy call beyond decision 4).
- r7c-16 partial OUTPUT on mid-write failure across rewrite commands (cleanup policy = atomic
  tmp+rename vs unlink — cross-command decision).
- r7c-17 compress/decompress round-trip keeps the empty primary (2 HDUs vs fpack's 1; data
  preserved, structure differs — writer convention).

## Evidence

Failing-first per fix pasted in slice files (A: 34 failed/120 pre-fix incl. 15/15 exit-5 rows;
B: 14 failed pre-fix; C: pasted per-row). Combined-tree verification before commit: **227
passed** (10 suites incl. docs-integrity, public-boundary). Perf: r7b-10 verify opens 17→9 +
749.5→613.2µs (count proof + micro-timing).

## Plan deviations / integration notes

- `chore(changelog): R7 round fixes` subject convention (as R1-R6).
- Sole-editor `docs/cli.md` protocol worked (A landed B/C's verbatim texts — zero conflicts).
- Integration-owned follow-ups: `style: ruff pass over R7 code` + cmds_setkey `_parse_value`
  typing guard (defensive pair-check, unreachable path becomes typed UsageError) — zero-reachable-
  behavior.

## Gate results

- `pixi run preflight-push` → PASS.
- `pixi run test` → PASS (2101 passed, 24 skipped, 21 xfailed, 381 warnings in 481.44s; +132 over R6's 1969).
