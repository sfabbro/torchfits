# R11-00 — tests chunk 00

Owned: the 22 paths in `.cursor/harness/release-1-2-0-review/splits/tests.chunk00` (`tests/conftest.py` through `tests/test_compression.py`, including `tests/cpp/test_bracket_detection.cpp`). No `src/` edits. `tests/test_security.py` not touched (it compiles the bracket probe; the cpp filename is unchanged).

## Findings

| ID | Sev | Class | Files:symbols | Description (with repro) | Fix & validation | Status |
|---|---|---|---|---|---|---|
| r11a-01 | MINOR | edge | `tests/test_ascii_table.py:test_ascii_table` | Fixture was created as relative `test_ascii.fits` in the process cwd, then deleted twice. A crash before the `os.remove` left the file in the repo, and parallel workers sharing a cwd could clobber each other. | Write `tmp_path / "test_ascii.fits"`. Drop the manual deletes and the dead `pass`. `pixi run pytest tests/test_ascii_table.py -q` (included in the chunk run below). | fixed |
| r11a-02 | MINOR | edge | `tests/test_complex_header.py:test_complex_header` | Same cwd write: `filename = "test_complex_header.fits"`, removed only in `finally`. A `__main__` block re-entered pytest via `pytest.main`. | `tmp_path` fixture; close the HDUList; the `__main__` block is deleted. Chunk pytest below. | fixed |
| r11a-03 | MINOR | perf | `tests/test_bench_suites.py:test_time_median_reports_peak_rss`, `test_rss_sampler_sees_transient_peak`; `tests/test_compression.py:TestCompressionPerformance.test_large_compressed_image`, `test_compressed_subset_performance` | Pass/fail asserts RSS (`peak_rss > 0`, `peak_mb > 0`) or a wall-clock budget (`read_time < 60`, `subset_time < 10`). Unmarked, so the GHA PR matrix would still run them. | `@pytest.mark.performance` on those four tests only. Collect-only pasted below. | fixed |
| r11a-04 | MINOR | concurrency | `tests/test_bench_suites.py:test_rss_sampler_sees_transient_peak` | `time.sleep(0.02)` / `time.sleep(0.01)` raced the background `_RssPeakSampler` (poll interval 1 ms) so a sample might land while the 8 MiB blob was alive. Not a product interval. | Monkeypatch `benchmarks.bench_timing._rss_mb` and `threading.Event.wait` until a sample is taken while the blob is held (skipped when `psutil` is absent and `_PROC is None`). No `time.sleep` left in the chunk. | fixed |
| r11a-05 | MAJOR | overflow | `tests/test_cli_arith_stats.py:test_arith_mul_saturates_int16` | The test claimed int16 saturation but only checked `min`/`max` of the result. `200 * 200` and `-200 * 200` wrap inside int16 to `-25536` and `25536`, which still satisfy those bounds, so a wrapping arith would pass. Probe: `np.array([40000, -40000], dtype=np.int64).astype(np.int16)` → `[-25536, 25536]`, `in_range True`. | Assert the rails `[[32767, -32768]]`. Product already saturates (`cmds_arith._saturate_to`); the chunk pytest covers this test. | fixed |
| r11a-06 | MINOR | edge | `tests/test_byteswap.py:test_float32_special_values`, `test_float64_special_values`; `tests/test_compressed_nulls.py:test_uncompressed_float_nan_still_nan` | Docstrings and the nulls `err_msg` claim signed-zero preservation. `np.testing.assert_array_equal` uses `==`, and `-0.0 == 0.0`, so a sign-bit wipe still passed. Probe of the reader: mmap on and off, `signbit(+0)=False`, `signbit(-0)=True`, payload `00000000000000800000803f0000c07f`. | Added `np.signbit` checks at `[0, 0]` / `[0, 1]` on both mmap modes. Chunk pytest covers them. | fixed |
| r11a-07 | CLEANUP | docs | `tests/test_bench_suites.py:test_fitstable_scan_count_uses_nrows_not_column`, `test_fitstable_filter_has_dense_and_selective_regimes` | `inspect.getsource` pins (`"read_nrows" in src`, `"predicate_filter"` string literals) break on a behavior-preserving move. The predicate helpers are also checked by return value; the source pins are the only check that scan_count / `_bench_case` stay wired to `read_nrows` and those case ids. Deleted only when that behavior is already covered elsewhere — it is not. | Left in place. | deferred |
| r11a-08 | MINOR | edge | `tests/test_cache.py:TestCaching.test_cache_clearing` | After `clear_file_cache()` the test calls `get_cache_performance()` and asserts nothing, so a clear that does not reset state still passes. | Not rewritten: a correct assertion depends on which counters the engine resets, and `test_clear_cache` / `test_cache_stats_surface_engine_errors` already cover the stats contract. | deferred |
| r11a-09 | MINOR | docs | `tests/test_clear_all_caches.py:test_clear_cache_disk_true_parameter` | Name and docstring say `clear_cache(disk=True)`; the body calls `clear_cache(disk=False)` and asserts nothing. Calling `disk=True` without redirecting `TORCHFITS_CACHE_DIR` would delete the real cache root. `disk=True` removal is already asserted by `test_clear_all_caches_removes_disk_cache` (`clear_all_caches` → `clear_cache(disk=True)`). | Left in place. | deferred |
| r11a-10 | CLEANUP | edge | `tests/test_api.py:TestAPI.test_error_handling` | The `mode="invalid"` `pytest.raises(ValueError)` block is duplicated back-to-back (`test_api.py` around the two `torchfits.read(..., mode="invalid")` calls). Zero extra coverage. | Left in place (not on the dead-code deletion list; behavior unchanged either way). | deferred |

No `src/` product bug reproduced in this chunk. Integer saturation and IEEE signed zero match the strengthened oracles. `test_deficits_require_external_peer` expects `compute_deficits(...) == []` because both rows are torchfits (no external peer), not because an under-floor lag was dropped; the under-floor cases in the same file expect `significance == "noise"` / `"significant"`.

### Performance marker decisions

Marked (pass/fail is wall-clock or RSS):

- `tests/test_bench_suites.py::test_time_median_reports_peak_rss` (`peak_rss > 0`)
- `tests/test_bench_suites.py::test_rss_sampler_sees_transient_peak` (`peak_mb > 0`)
- `tests/test_compression.py::TestCompressionPerformance::test_large_compressed_image` (`read_time < 60`)
- `tests/test_compression.py::TestCompressionPerformance::test_compressed_subset_performance` (`subset_time < 10`)

Read and not marked:

- `test_cache.py::test_cache_performance_tracking` counts `total_requests`; no clock or RSS assert.
- `test_cache.py::test_for_environment_no_psutil` and `test_cache_config.py::test_optimal_cache_config_no_psutil` mock `os.sysconf` failure and assert cache-config numbers. The `psutil` in the name is the old fallback label; they do not sample RSS.
- `TestCompressionPerformance.test_large_integer_rice_roundtrip_exact` asserts bitwise equality, not time.
- `test_bench_ranking_mmap.py` feeds synthetic `time_s` values into `compute_deficits`; it does not time the process.

`time.sleep` in this chunk: only the two sampler sleeps, replaced (r11a-04). No mtime / cache-throttle sleeps were present.

Incidental-wording deletions: none. `test_fitspolarsframe_repr` is the only check that `FITSPolarsFrame.__repr__` includes metadata; `test_read_polars_basic` covers the attributes, not the repr. CLI help / header-card text asserts are the user-visible command contract. Kept.

`tests/cpp/test_bracket_detection.cpp` reviewed, not modified (security wrapper owns the compile). Bracket cases still distinguish a trailing CFITSIO section from a `[` inside a directory component.

## Validation

Wrap probe (old oracle is blind):

```
np.array([40000, -40000], dtype=np.int64).astype(np.int16) -> [-25536, 25536], in_range True
```

Signed-zero probe (`read_tensor`, mmap True and False): `signbit(+0)=False`, `signbit(-0)=True`.

Chunk pytest (21 Python files; cpp file not run): `366 passed, 1 skipped, 333 warnings in 127.33s`. The warnings are existing `torchfits.cpp`, `ReadOptions.handle_cache_capacity`, and pyarrow feather deprecations.

Marker collect-only:

```
pixi run pytest tests/test_bench_suites.py tests/test_compression.py tests/test_cache.py tests/test_cache_config.py -m performance --collect-only -q
tests/test_bench_suites.py::test_time_median_reports_peak_rss
tests/test_bench_suites.py::test_rss_sampler_sees_transient_peak
tests/test_compression.py::TestCompressionPerformance::test_large_compressed_image
tests/test_compression.py::TestCompressionPerformance::test_compressed_subset_performance
4/66 tests collected (62 deselected) in 1.37s
```

`test_cache.py` and `test_cache_config.py` were on that command and only showed up as deselected.

## Disposition

| File | Disposition |
|---|---|
| `tests/conftest.py` | clean — autouse clears `CACHE_ENV_SENTINELS`; no timer, sleep, or cwd write |
| `tests/cpp/test_bracket_detection.cpp` | clean — reviewed, not edited |
| `tests/test_api.py` | r11a-10 deferred (duplicate invalid-mode block). Other asserts are API contracts |
| `tests/test_arrow_table_api.py` | clean — repr test kept (sole repr contract) |
| `tests/test_ascii_table.py` | r11a-01 fixed |
| `tests/test_astropy_upstream_smoke.py` | clean — fixtures already under `tmp_path` |
| `tests/test_bench_ranking_mmap.py` | clean — synthetic times; `[]` only when there is no external peer |
| `tests/test_bench_suites.py` | r11a-03, r11a-04 fixed; r11a-07 deferred |
| `tests/test_bug_table_duplicate_names.py` | clean — byte-level duplicate TTYPE fixture, values asserted |
| `tests/test_byteswap.py` | r11a-06 fixed |
| `tests/test_bz2.py` | clean — capable and incapable paths, `tmp_path_factory` |
| `tests/test_cache.py` | r11a-08 deferred. Performance-named tests do not assert wall-clock or RSS |
| `tests/test_cache_config.py` | clean — config fallback, not an RSS test |
| `tests/test_changelog_tooling.py` | clean — changelog-tool contracts, temp dirs |
| `tests/test_check_torch_extra_pins.py` | clean — pin/index contracts |
| `tests/test_checksum.py` | clean — datasum/checksum status contracts, `tmp_path` |
| `tests/test_clear_all_caches.py` | r11a-09 deferred |
| `tests/test_cli.py` | clean — outputs under `tmp_path`; stdout checks are CLI contracts |
| `tests/test_cli_arith_stats.py` | r11a-05 fixed |
| `tests/test_complex_header.py` | r11a-02 fixed |
| `tests/test_compressed_nulls.py` | r11a-06 fixed |
| `tests/test_compression.py` | r11a-03 fixed (two methods). Exact RICE round-trip unmarked |
