# R8 — `cpp_src/` table family

Slices: A `dirs/R8-reader.md` (r8a-*), B `dirs/R8-opsbind.md` (r8b-*). First C++ round; dual-env
rebuild per fix batch under the hub rebuild-mutex (zero build races).

## Totals

21 findings (r8a 11, r8b 10): 12 fixed (5 BLOCKER, 5 MAJOR, 2 MINOR), 4 verified-at-HEAD +
pinned (strided DLPack ×2 angles, VLA/empty edges, os.replace staleness guard), 5 deferred.

Blockers fixed (5): r8a-01 0-dim DLPack payloads (NULL strides) segfaulted in `update_rows_mmap`;
r8a-02 scaled-column mmap guard was a NO-OP — default `mmap='auto'` wrote PHYSICAL values as raw
bits (`[3,5,7,9,11]` → `[7,11,15,19,23]`, silent corruption; now scale-resolved guard + buffered
inverse-scaling fallback); r8a-03 scaled VLA columns truncated physical values into raw ints AND
scaled the TNULL sentinel (now raw reads + `apply_scale_and_nulls`: float64 physical, TNULL→NaN
per `tnull-read-torch`); r8a-04 duplicate column-name requests double-moved map entries into
moved-from None tensors (relocated duplicate-TTYPE UB; dedup at 3 sites); r8b-01 TBIT ('X')
appends used one flat `fits_write_col` run that ignores per-row byte padding when repeat % 8 ≠ 0 —
every appended row of a 12X-style column silently bit-shifted (per-row loop + exact-repeat width
check; failing-first via temporary TBIT-hunk revert + rebuild).

Majors (5): r8b-02 thread-local reader cache stale after shared-meta invalidation (R7-CPP2:
uid-bump eviction + acquire-time generation check); r8b-03 acquire/release LRU ghost leak
(mallinfo2 proof: 3,520,128 bytes / 20,000 cached reads → ~0); r8b-04 unknown `table_type` fell
through to binary (typed std::invalid_argument → ValueError); r8a-05 std::cerr diagnostics folded
into exception messages (A-07); r8a-06 empty-table unknown-column silent `{}` / filtered-gather
skip; r8a-07 duplicate-TTYPE update resolved LAST while reads resolve FIRST (first-wins emplace).

Verified-at-HEAD + pinned (candidates re-derived, no fix needed): r8a-10/r8b-05 strided-DLPack
numeric writes (fixed by 944f83b; both mmap and buffered sides pinned: t[::2], F-order, negative
strides, 2-D, torch views); r8a-11/r8b-07 VLA zero-length/repeat-0 edges; r8a-09 verify-only
anchors (repeat guard, ensure_extent ×3); r8b-06 os.replace staleness (stat-identity guard 7e3b1ed)
— the candidate's real half was r8b-02.

## Deferred (→ backlog at R17)

- r8a-12/13 (in R8-reader.md deferrals) + r8b-08 table writes/reader opens run GIL-held (needs
  payload-materialization split, >30 LOC structural — perf evidence rule not met this round).
- r8b-09 populate_rows/append_rows column-loop redundancy (perf, no evidence lane).
- r8b-10 (minor edge, see file).
- r8a-08 landed as guards + code proof (CFITSIO rejects absurd TFORMs first — no feasible
  failing-first).

## Evidence

Failing-first per fix pasted (A: 9 failed/18 passed pre-fix; B: revert-based recapture for r8b-01
+ 4 staleness tests). Combined-tree verification before commit: **334 passed, 1 skipped** (14
suites incl. output_parity, table_file_ops, mutation_errors, write_fidelity, arrow/stream
consumers). Rebuilds: `pixi run dev` + `pixi run -e test -- pip install -e . --no-build-isolation`
(both envs, mutex-serialized; compile proof implicit).

## Plan deviations / integration notes

- `chore(changelog): R8 round fixes` subject convention (as R1-R7).
- Rebuild mutex protocol (Main-serialized acks) worked — zero concurrent-build windows.
- The plan's headline strided-DLPack BLOCKER candidate was RE-DERIVED already-fixed at HEAD by
  both slices independently (944f83b) — the effort landed as pins instead (evidence rule kept).
- Integration-owned follow-up: `style: ruff pass over R8 tests` (1 lint fix + 4 reformats).

## Gate results

- `pixi run preflight-push` → PASS.
- `pixi run test` → PASS (2139 passed, 24 skipped, 21 xfailed, 381 warnings in 463.33s; +38 over R7's 2101).
